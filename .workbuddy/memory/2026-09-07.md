# 2026-09-07

## 发布自动化工具落地（脚本 + Skill 混合架构）

**背景**：用户要自动化整合包发布（参考 Phobos-v0.5.0.0-Recya1.zip），问"脚本还是 skill"。经量化拆解：发布 8 步中 5 步纯机械（版本递增/日期回填/构建/打包/开新占位小节）、2 步语义（changelog 起草、已知问题维护）。用户拍板：脚本+Skill 混合，全自动 commit+tag。

**关键发现**：
- 发布包 = Release 产物（dll+pdb）+ `整合包说明/` 内容平铺，共 11 条目；zip 名 = `Phobos-v{四段版本}-Recya{EX_PATCH}.zip`
- 版本号唯一定义点 `src/Phobos.version.h`（EX_PATCH = RecyaN），version.rc 全引用宏
- `更新改动说明.md` 占位小节循环：`### YYYY.X.XX` 开发期攒条目 → 发布日回填日期。文档头 hint 说"仅记录最近四个 build"但实际从 2024.12 全量保留，打包即原样复制
- changelog 的 upstream 合并行是**一句汇总**（不是逐条翻译）：`> - \`( 0 )\` 合并 Phobos-develop 近期所有改动（*{日期} - \`commit {hash}\`*）`，日期 = merge commit 日期、hash = 第二父（upstream HEAD），git 可全量机械提取
- 参考 zip 的中文文件名是 **GBK 编码、无 UTF-8 flag、含 stored 目录条目**（Windows 右键压缩产物）——为老版本 Windows 兼容需复刻

**产出**：
- `scripts/release_pack.py`：check / mergeline / prepare / build / pack / finish / all 七个子命令 + --dry-run。已完成隔离测试（mergeline 幂等插入、prepare 回填+递增+BOM/CRLF 保留、pack 与参考包**逐条目二进制级一致**、finish 新占位小节插入位置）
- `.workbuddy/skills/phobos-release/SKILL.md`：agent 编排层（changelog 起草 review、调用脚本、present zip）

**技术坑（Python 3.13 zipfile）**：
- `ZipInfo.__init__` 对 bytes 文件名直接 TypeError（旧版支持），bytes arcname 路线不通
- `_open_to_write` **无条件** `zinfo.flag_bits = _MASK_UTF_FILENAME`，monkeypatch `_encodeFilenameFlags` 时必须显式 `& ~0x800` 清位，否则 GBK 名+UTF-8 flag 组合会让解压器乱码
- 3.13 `_encodeFilenameFlags` 语义反转：flag 含 0x800 时才尝试 ascii，否则 cp437，失败 fallback UTF-8+flag

**未实测**：build 子命令的真实施工（需 VS 环境，数分钟）与 finish 的真实 git commit/tag（避免误提交，用桩验证了命令构造）。下次真实发布 Recya2 时首跑验证。

## build 真实施工验证（15:05 完成）

- **首次失败**：MSB6001 —— Git Bash + 代理环境下环境块同时含 `HTTPS_PROXY`/`https_proxy`（仅大小写不同），MSBuild 用大小写不敏感 Hashtable 传环境给 CL.exe 时崩溃。**修复**：cmd_build 增加 `_msbuild_safe_env()`（os.environ 合并视图重建环境块去重），永久健壮性增强
- **二次失败**：`TypeConvertGroup.cpp` C2065 `pTargetFoot` 未声明 —— merge 后未适配的真实源码错误，用户自行修复（17bbf123b "Update Body.h" + d32bc3d67 "Update TypeConvertGroup.cpp"，把参数 `FootClass* pTargetFoot` 改成 `TechnoClass* pTarget`）后增量构建 7m45s 通过
- **构建成功**：`Release/Phobos.dll + Phobos.pdb`，ProductVersion = `v0.5.0.0-Recya1 @ d32bc3d67-dirty @ refs/heads/Mix-ECpack`，版本戳校验（startswith 前缀）通过
- **版本戳差异（待用户决策）**：参考包 dll 是无 git 环境构建（ProductVersion = `v0.5.0.0-Recya1 sp @CrimRecya`，走 version.h 的 fallback 签名分支）；仓库内构建天然带 git 戳。FileVersion/FileDescription 两边完全一致
- reg.exe 被 WorkBuddy 沙箱黑名单阻止的警告不影响构建结果（只是 vsdevcmd/vcpkg 的旁路注册表查询）

## 版本戳格式定案（复刻参考包）

- 用户拍板：发布 dll 的 ProductVersion 复刻参考包格式 `v0.5.0.0-RecyaN sp @CrimRecya`
- 实现：`Phobos.props` ComputeGitInfo 的 4 个 Exec 加 `SkipGitStamp` 纯开关条件（不传时行为不变，CI 不受影响）；`release_pack.py build` 直接调 `run_msbuild.bat` 传 `/property:SkipGitStamp=true`，BuildType 留空保持 special build
- 实测通过：构建 7m15s，ProductVersion = `v0.5.0.0-Recya1 sp @CrimRecya`，与参考包逐字段一致
- 日常开发构建（build_release.bat）不受影响，仍带 git 戳便于溯源
- 发布流水线至此全部实测就绪：check / mergeline / prepare / build / pack 均已真实验证；仅 finish 的真实 git commit+tag 留待首次真实发布验证

## 工具入库与发布前状态（15:30）

- 工具已提交：`826d4fb63 Add mix release packer (release_pack.py) and SkipGitStamp build switch`（只含 Phobos.props + scripts/release_pack.py；.workbuddy/ 保持未跟踪）
- 清理了 release_pack.py 里已无使用者的 BUILD_SCRIPT 常量、更新 docstring
- `mergeline --insert` 已把 09-07 的 upstream merge 记入占位小节（`*2026.9.7 - commit 2617d0725...*`），与用户手写的 UI 框架条目并列，共 2 条，格式与历史逐字段对齐
- 当前状态：分支 Mix-ECpack ahead 4 未推送（core、两个编译修复、工具提交）；changelog 含 mergeline 改动未提交（预期：归入发布 commit）；说"发布"即走 prepare → build → pack → finish

## 版本戳最终定案（15:45，覆盖"复刻参考包"决策）

- 用户改主意：**Phobos.props 原样不动**（"原来怎样现在就怎样"），发布 dll 版本戳就用原生构建形态（带 git 戳 `@ commit-dirty @ ref`），不加 SkipGitStamp 开关
- 执行：props 从 d32bc3d67 恢复原样；cmd_build 回退到调 build_release.bat（保留 `_msbuild_safe_env()` 环境去重——修 MSB6001 的必需补丁）；amend 原提交（未推送，无风险）为新提交 `3e31765a8 Add mix release packer (release_pack.py)`，只含脚本，props 零改动
- 带 git 戳的构建路径此前已实测通过（`v0.5.0.0-Recya1 @ d32bc3d67-dirty @ refs/heads/Mix-ECpack`），无需重构建验证
- 教训：给上游工程文件加东西前，先确认用户对"侵入工程文件"的接受度——量化方案时把"改动范围"也列为决策维度
