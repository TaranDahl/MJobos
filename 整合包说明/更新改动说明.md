# 更新改动说明

> - Modified by **[绯红热茶/CrimRecya](https://github.com/CrimRecya)**
> - 平台仓库：**[Mix-ECpack](https://github.com/CrimRecya/Phobos-Mix/tree/Mix-ECpack)**
> - 公版地址：**[Phobos](https://phobos.readthedocs.io/en/latest/#)**
> - 公版仓库：**[Phobos-develop](https://github.com/Phobos-developers/Phobos)**

```{hint}
- 该文档内容仅记录**最近三个**build版本内的更新内容，若为初次使用，直接查看《额外功能说明》即可
```

---

## 使用须知

- 本平台仅在**凛冬将至**群内发布最新版本，并授权EC整合包开发组合使用
- 除制作任务包、模组演示外，**不允许**以任何形式在未获得作者许可的情况下，对本平台一个或多个独有功能进行独立展示
- 本平台仅作内部测试使用，不对外宣传，除待公版合并的内容外，**不允许**任何人以任何形式直接对外宣传平台开发内容
  > 为什么不对外宣传？-本质是测试，其功能并不如公版稳定，bug发出去了没人修
- 本平台可以在上述条件下自行使用，但请注意公开场合使用时（直播、视频等），必须在合适位置注明使用的本平台**并非公版**
- 如果需要反馈问题，请在确认为**最新版本**后，在群内联系作者以寻求解决方案
- 如只需在模组使用部分功能，请自行合并需要的内容，并**标明来源**
  > 严禁冒名顶替作者的行为，请尊重他人的劳动成果
- 平台试用期自发布起计算，共30天，自用时请自行更新最新版本，若需要发布任务包、模组使用，请联系作者

```{note}
- **`注意`** 平台并非所谓新版、开发版Phobos
  - 就目的而言，平台属于测试版，其中部分功能*可能*会被公版合并
  - 就结果而言，平台属于私改版，与公版并无直接关系
```

```{note}
- **`注意`** 凛冬将至（WIC/486835964）群并非本平台作者的群，仅作为传统在其中进行发布
  - 凛冬将至群为WIC平台/模组的官方群，请保证基本的礼貌和尊重
  - 本平台不会与WIC平台发生直接冲突，但如在游戏过程中发现此类问题，请及时反馈
```

---

## 温馨提醒

- 平台仍在持续更新中，新增内容较多，如果找不到更新改动说明中的内容，请善用查找搜索
- 当非大型功能被公版合并，此说明书将不再保留其功能介绍，而为方便使用，大型功能的说明仍会保留
- 使用某些功能出现小问题时，请不要觉得没什么关系或者问题不大就算了，只要是说明中没有明确提到的，都可以提出
- 提出建议、反馈问题时，请不要在群里直接说，善用 **@** 成员、私聊（或 `凛冬将至` 群在线文档），否则容易被遗忘或者根本没注意到
  - 如果出现弹窗需要反馈，提供根目录下 `debug` 文件夹中当局的 `snapshot` 文件夹内的 `debug.log` 、`except.txt` 和 `syringe.log` ，如果你在弹窗时点了 `确认` ，那么该文件夹中还会生成一个 `.dmp` 文件，除特殊情况外，该文件**不**需要提供
  - 你也可以通过查看 `debug.log` 底部的 `EIP` 或 `except.txt` 顶部的 `EIP` 自行检查错误类型，当 `EIP` 为六位数时（ `00` 开头的八位数），通常为原版问题，你可以自行通过 `modenc` 等网站自行查阅常见 `EIP` 类型；如果为正常的八位数，你可以通过 `debug.log` 顶部的数个 `Module XXX.dll  base address` 看到所有拓展的基地址，打开计算器的程序员模式，使用十六进制减法将 `EIP` 减去各个基地址，当结果为正数（不是一长串 `F` 开头的数）且最小时，通常能够表明是这个拓展出现的问题，如果仍不能确定，请使用上一种方法

```{hint}
- 如果在刚更换完dll时发生问题，请先尝试重启电脑，可能是dll未成功完全完成替换导致的，重启后若问题仍然存在，请反馈
```

---

## 特别感谢

- 感谢 **`Phobos`** 的所有开发者和贡献者
- 感谢 **`Sidoupiar和水菌`** 给予的额外功能开发方面的帮助
- 感谢 **`钢铁之锤`** 制作了适配的EC开发组件
- 感谢 **`航味麻酱`** 制作了更多的拓展功能并帮助维护平台
- 感谢 **`凛冬将至`** 的**群友们**给予的思路并帮忙进行各种测试
- 感谢 **`pbs测试群`** 的**群友们**给予的思路并帮忙进行各种测试

---

## 更新细节

### 目前已知问题

 > - `( - )` 拖动即将进入坦克碉堡的坦克会导致永久性的位置错误
 > - `( - )` 选中小队能够使得选中单位可以进入其它单位的路径点
 > - `( 0 )` `Spawner` 发射子机后有极小概率导致ATC错误（未知触发条件）
 > - `( 0 )` `Jumpjet` 坠毁落地有极小概率导致绘制错误（未知触发条件）
 > - `( 0 )` `Image` 为巨幅 SHP 时无法显示
 > - `( 0 )` `SW.Next` 在联机环境下被超时空二段超武使用时会导致除发射者外的玩家游戏崩溃
 > - `( 6 )` `VHPScan.Enhanced` 会违规访问内存，请先不要使用，等待逻辑重构

### 2025.9.7  `Phobos-build-47+6_14` -> `Phobos-build-47+6_15`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.9.7 - `commit c6b29398da74e607f2401ebe1758c73da99a7b1f`*）
 >   - *`commit 15caefbd...`* -> *`commit 32d96c21...`* 修正说明书的内容和格式，修复 `IsSimpleDeployer` 在路径中不能部署的问题
 >   - *`commit 0d05ab93...`* 新增 `Strafing.TargetCell` 选项，并修复扫射飞机在朝右上角飞行时可能状态卡死的问题
 >   - *`commit 3bee24f8...`* 重构 `IsSimpleDeployer` 逻辑，并新增 `DeployDir` 等相关自定义选项
 >   - *`commit 042b74c7...`* 修复 `ArmorMultiplier.AllowWarheads` 重复计算的问题
 >   - *`commit 4ea0f700...`* 修复额外弹头引爆可能使用上次引爆使用的武器类型的错误
 >   - *`commit d3863ea0...`* 修复单位上激光尾迹的位置错误，和变形时可能无法刷新的问题，现在单位变形能够刷新炮管角度和制退数据
 >   - *`commit 31aba26d...`* -> *`commit e1ac302a...`* 优化禁止移动、护盾逻辑、武器超武、部署逻辑等功能的代码
 >   - *`commit c3e72020...`* 拓展原本只能使用一种动画的逻辑，现在可以使用列表随机选择，并修复了自定义 `WarpAway` 的错误用法
 >   - *`commit 87a60707...`* 新增 `Respawn.RestartInCombatDelay` 等相关自定义选项，拓展了原本的护盾逻辑
 >   - *`commit 06fd9e7e...`* 修复 `Jumpjet` 类单位移动时会受到 `MovementRestrictedTo` 影响的问题
 >   - *`commit 89b74af8...`* 修复一个寻路错误
 >   - *`commit c6b29398...`* 修复 `Jumpjet` 类单位在空中无法发射子机的问题
 > - `( 6 )` 弹道抛体的部分通用化功能可被原版抛射体使用，并删除了前缀，详细说明请见《额外功能说明》，已更新《抛体案例参考》
 >   - 更改 `Trajectory.Duration` 键名为 `LifeDuration`
 >   - 更改 `Trajectory.NoTargetLifetime` 键名为 `NoTargetLifeTime`
 >   - 更改 `Trajectory.CreateCapacity` 键名为 `CreateCapacity`
 >   - 更改 `Trajectory.ApplyRangeModifiers` 键名为 `ApplyRangeModifiers`
 >   - 更改 `Trajectory.PeacefulVanish` 键名为 `PeacefulVanish`
 >   - 更改 `Trajectory.RetargetRadius` 键名为 `RetargetRadius`
 >   - 更改 `Trajectory.RetargetInterval` 键名为 `RetargetInterval`
 >   - 更改 `Trajectory.RetargetHouses` 键名为 `RetargetHouses`
 >   - 更改 `Trajectory.Synchronize` 键名为 `Synchronize`
 >   - 更改 `Trajectory.PassDetonate` 键名为 `PassDetonate`
 >   - 更改 `Trajectory.PassDetonateWarhead` 键名为 `PassDetonateWarhead`
 >   - 更改 `Trajectory.PassDetonateDamage` 键名为 `PassDetonateDamage`
 >   - 更改 `Trajectory.PassDetonateDelay` 键名为 `PassDetonateDelay`
 >   - 更改 `Trajectory.PassDetonateInitialDelay` 键名为 `PassDetonateInitialDelay`
 >   - 更改 `Trajectory.PassDetonateLocal` 键名为 `PassDetonateLocal`
 >   - 更改 `Trajectory.ProximityImpact` 键名为 `ProximityImpact`
 >   - 更改 `Trajectory.ProximityWarhead` 键名为 `ProximityWarhead`
 >   - 更改 `Trajectory.ProximityDamage` 键名为 `ProximityDamage`
 >   - 更改 `Trajectory.ProximityRadius` 键名为 `ProximityRadius`
 >   - 更改 `Trajectory.ProximityDirect` 键名为 `ProximityDirect`
 >   - 更改 `Trajectory.ProximityMedial` 键名为 `ProximityMedial`
 >   - 更改 `Trajectory.ProximityAllies` 键名为 `ProximityAllies`
 >   - 更改 `Trajectory.ProximityFlight` 键名为 `ProximityFlight`
 >   - 更改 `Trajectory.ThroughVehicles` 键名为 `PassThroughVehicles`
 >   - 更改 `Trajectory.ThroughBuilding` 键名为 `PassThroughBuilding`
 >   - 更改 `Trajectory.DamageEdgeAttenuation` 键名为 `DamageEdgeAttenuation`
 >   - 更改 `Trajectory.DamageCountAttenuation` 键名为 `DamageCountAttenuation`
 >   - 更改 `Trajectory.UseDisperseCoord` 键名为 `UseDisperseCoord`
 >   - 更改 `Trajectory.DisperseWeapons` 键名为 `DisperseWeapons`
 >   - 更改 `Trajectory.DisperseBursts` 键名为 `DisperseBursts`
 >   - 更改 `Trajectory.DisperseCounts` 键名为 `DisperseCounts`
 >   - 更改 `Trajectory.DisperseDelays` 键名为 `DisperseDelays`
 >   - 更改 `Trajectory.DisperseCycle` 键名为 `DisperseCycle`
 >   - 更改 `Trajectory.DisperseInitialDelay` 键名为 `DisperseInitialDelay`
 >   - 更改 `Trajectory.DisperseEffectiveRange` 键名为 `DisperseEffectiveRange`
 >   - 更改 `Trajectory.DisperseSeparate` 键名为 `DisperseSeparate`
 >   - 更改 `Trajectory.DisperseRetarget` 键名为 `DisperseRetarget`
 >   - 更改 `Trajectory.DisperseLocation` 键名为 `DisperseLocation`
 >   - 更改 `Trajectory.DisperseTendency` 键名为 `DisperseTendency`
 >   - 更改 `Trajectory.DisperseHolistic` 键名为 `DisperseHolistic`
 >   - 更改 `Trajectory.DisperseMarginal` 键名为 `DisperseMarginal`
 >   - 更改 `Trajectory.DisperseDoRepeat` 键名为 `DisperseDoRepeat`
 >   - 更改 `Trajectory.DisperseSuicide` 键名为 `DisperseSuicide`
 >   - 更改 `Trajectory.DisperseFromFirer` 键名为 `DisperseFromFirer`
 >   - 更改 `Trajectory.DisperseFaceCheck` 键名为 `DisperseFaceCheck`
 >   - 更改 `Trajectory.DisperseForceFire` 键名为 `DisperseForceFire`
 >   - 更改 `Trajectory.DisperseCoord` 键名为 `DisperseCoord`
 > - `( 6 )` 新增 `ExtendedAircraftMissions` 的区域警戒功能，并优化了无机场坠毁逻辑

### 2025.8.30  `Phobos-build-47+6_13` -> `Phobos-build-47+6_14`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.8.30 - `commit ed82230f2fab099f0fdb866279df34c32079d6e0`*）
 >   - *`commit 6904ab0f...`* -> *`commit ed82230f...`* 修正说明书的内容和格式，优化飞机攻击、护盾逻辑、弹头引爆等功能的代码
 > - `( 6 )` 更新 `Locomotor=AdvancedDrive` 的功能，修改了原本的键名并添加了新的功能
 >   - 新增 `AdvancedDrive.Reverse` 选项，用于定义单位是否能够倒车
 >   - 更改 `AdvancedDrive.ReverseSpeed` 键名为 `AdvancedDrive.Reverse.Speed`
 >   - 更改 `AdvancedDrive.ConfrontEnemies` 键名为 `AdvancedDrive.Reverse.FaceTarget`
 >   - 更改 `AdvancedDrive.FaceTargetRange` 键名为 `AdvancedDrive.Reverse.FaceTargetRange`
 >   - 更改 `AdvancedDrive.MinimumDistance` 键名为 `AdvancedDrive.Reverse.MinimumDistance`
 >   - 更改 `AdvancedDrive.RetreatDuration` 键名为 `AdvancedDrive.Reverse.RetreatDuration`
 >   - 新增 `AdvancedDrive.Hover` 等相关自定义选项，用于控制单位是否能够类似 `Locomotor=Hover` 悬浮
 > - `( 6 )` 修复带有附加单位的建筑在取消建造时会导致游戏崩溃的问题
 > - `( 6 )` 修复电磁脉冲不会停止AI工厂生产的问题
 > - `( 6 )` 修复 `LaserFencePost.Fence` 判断错误的问题
 > - `( 6 )` 优化 `ExtendedBuildingPlacing` 疏散部队的逻辑
 > - `( 6 )` 新增 `Strafing.TargetCell` 选项，用于让飞机扫射不会因目标死亡被打断

### 2025.8.22  `Phobos-build-47+6_12` -> `Phobos-build-47+6_13`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.8.21 - `commit 872a1c50f0880848f957bd35645aa7b29fa947d5`*）
 >   - *`commit 6a9fb6bd...`* 修复超武侧栏的图标重复问题
 >   - *`commit 7679315a...`* 拓展 *`commit 00ec62e0...`* 的修复
 >   - *`commit f1ec0730...`* 修复 `CanBeBuiltOn` 的物体可能卡死 `UndeploysInto` 建筑摆放和部署的问题
 >   - *`commit 0a9bbc32...`* 新增 `AIAutoDeployMCV` 等相关自定义选项，用于拓展AI建造逻辑
 >   - *`commit a88a203f...`* 修复一个随机崩溃问题
 >   - *`commit 5a4750f3...`* 拓展 `TurretRecoil` 逻辑，现在可以让车辆单位使用
 >   - *`commit 2f1fc0e4...`* 新增 `JumpjetClimbPredictHeight` 和 `JumpjetClimbWithoutCutOut` 选项，增强悬浮空军单位的抬升动作
 >   - *`commit e8e77c9d...`* 修复寻路的一些崩溃
 >   - *`commit b17211ac...`* -> *`commit ff63a0cd...`* 修正说明书的内容和格式，修复一个特殊状况下的崩溃，应用默认任务加载文件
 >   - *`commit 20dc9184...`* 新增 `VerticalInitialFacing` 选项，用于更改部分抛射体的初始朝向
 >   - *`commit 872a1c50...`* 新增 `ShowType` 等相关自定义选项，用于拓展数显和血条显示
 > - `( 6 )` 更改 `Trajectory.TolerantTime` 键名为 `Trajectory.NoTargetLifetime`
 > - `( 6 )` 现在 `Trajectory.Missile.CruiseAlongLevel` 能够预测前方的高度
 > - `( 6 )` 现在 `Trajectory.RecordSourceCoord` 直接开启，不再需要选项控制
 > - `( 6 )` 现在 `Trajectory.BulletROT` 的部分使用负数的做法由 `Trajectory.BulletFacingOnPlane` 替代
 > - `( 6 )` 现在 `Trajectory.Engrave.IsLaser` 等相关选项由武器上的 `IsLaser` 等相关选项替代
 > - `( 6 )` 现在 `ExtendedBuildingPlacing` 在处理部署时，能让单位在等待时先转向部署方向
 > - `( 6 )` 修复 `ExtendedBuildingPlacing` 在疏散单位时若格子上存在不占据位置的附加单位会导致放置失败的问题
 > - `( 6 )` 修复 `InvisoBlockageFix` 功能失效的问题
 > - `( 6 )` 修复 `Locomotor=AdvancedDrive` 的单位在被超时空传送后会尝试返回原位的问题
 > - `( 6 )` 更新 `CrimViewerEx` 的调试功能，现在能够显示基地节点建筑并检测鼠标位置附近空气墙

### 2025.8.15  `Phobos-build-47+6_11` -> `Phobos-build-47+6_12`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.8.15 - `commit 00ec62e09491eb44e292df122eaaf8b23053740b`*）
 >   - *`commit 437444f5...`* -> *`commit d9c37262...`* 修正说明书的内容和格式，优化部分代码及风格，修复火力增幅问题、多武器攻击声音问题
 >   - *`commit c528a1a7...`* 新增 `Parasite.GrappleAnim` 选项，用于自定义乌贼捆绑动画
 >   - *`commit a4a3eb17...`* 新增 `InfantryAutoDeploy` 选项，用于让电脑会在攻击时自动部署大兵
 >   - *`commit 23dc9f96...`* 修正说明书的内容和格式
 >   - *`commit 937822b6...`* 适配 `YRpp spawner` 的多人存档机制
 >   - *`commit 73c22089...`* 优化踢出重工内卡住单位的行为
 >   - *`commit 0fd1f49d...`* 修复车辆移动受限时的一些错误行为
 >   - *`commit f23649b7...`* 修复 *`commit 73c22089...`* 的一个小问题
 >   - *`commit 8f27ae56...`* 新增 `ReverseEngineer` 选项，用于逆向目标单位类型的科技
 >   - *`commit 275266de...`* 重构 *`commit 73c22089...`* 的实现方式
 >   - *`commit 00ec62e0...`* 修复 *`commit 0fd1f49d...`* 的代码问题
 > - `( 6 )` 修复 `CanBeBuiltOn` 的物体可能卡死 `UndeploysInto` 建筑摆放和部署的问题
 > - `( 6 )` 修复 `Trajectory.RetargetRadius` 在面对非建筑目标时跳过了条件判断的问题
 > - `( 6 )` 现在 `WreckageType` 改为可填写列表，若填入多项，则会在生成残骸时随机选择其中一种
 > - `( 6 )` 新增 `SmudgeUpdateTime` 选项，用于自定义弹坑逐渐消失的时间
 > - `( 6 )` 新增 `AllowSwitchNoMoveCommand` 等自定义选项，用于拓展目标分配模式
 > - `( 6 )` 新增 `DefaultPlacingDirection` 选项，用于设置摆放 `PlaceBuilding.Extra` 建筑时的默认方向
 > - `( 6 )` 新增 `ExtendedWeaponsFactory` 等相关自定义选项，用于解除各种重工相关的硬编
 > - `( 6 )` 增加一倍的单位寻路的缓存空间

### 2025.8.3  `Phobos-build-47+6_10` -> `Phobos-build-47+6_11`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.8.2 - `commit 36a7afedfb420fcd6e554af9c8115c2d244c8014`*）
 >   - *`commit 6eaea910...`* 修正说明书的内容和格式
 >   - *`commit 5203408e...`* 现在 `ID > 255` 的覆盖物更改为写入相同的 `[OverlayPack]` 中
 >   - *`commit 49e42d4d...`* -> *`commit 485ada97...`* 修复 `AmbientDamage.Warhead` 和 `AmbientDamage.IgnoreTarget` 不能对波逻辑生效的问题
 >   - *`commit c7a49850...`* -> *`commit 9f63f939...`* 优化联机事件和游戏初始化部分的代码
 >   - *`commit acef7a35...`* 修复按钮丢失的析构函数
 >   - *`commit 13248f91...`* -> *`commit b42d10f5...`* 优化部分代码
 >   - *`commit 1c121d04...`* 修复 `SkirmishUnlimitedColors` 在 Ares 未激活时也会生效的问题
 >   - *`commit 36a7afed...`* 修复玩家颜色与原版略有不同的问题
 > - `( 6 )` 新增 `MessageDisplayInCenter.LabelsCount` 和 `MessageDisplayInCenter.RecordsCount` 选项，并重写了整个功能

### 2025.7.27  `Phobos-build-47+6_9` -> `Phobos-build-47+6_10`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.7.26 - `commit 36334b4548d58e296b652635eef9e9123e631971`*）
 >   - *`commit 042bca4f...`* 修复 `OpenTopped=yes` 载具被超时空弹头冻结时，其乘客的超时空武器攻击不会停止的问题
 >   - *`commit 50612eeb...`* 优化步兵部署武器部分的代码
 >   - *`commit 4fda9e93...`* 修正说明书的内容和格式
 >   - *`commit 6d8a886c...`* 修复五个问题：
 >     - 修复电脑的载具在隐形时会乱跑的问题；
 >     - 修复潜艇在通过地图触发修改所属后总会朝向左方的问题；
 >     - 修复可驻扎建筑在按下部署热键释放全部驻兵后不会重绘图像的问题；
 >     - 修复驻军建筑在周围没有空地的情况下卸载占领者时会导致他们消失的问题；
 >     - 修复当攻击者受到 `Temporal=yes` 弹头影响时其 `IsLocomotor=yes` 弹头不会停止工作的问题
 >   - *`commit b65ae51b...`* 移除脚本相关的日志
 >   - *`commit 4e0f6734...`* 修复 *`commit 60a5181c...`* 中飞机降落相关的一个早期问题
 >   - *`commit 14bc97ff...`* 修复 *`commit 0a3b6508...`* 中攻击移动任务相关的一个问题
 >   - *`commit 735cd7f0...`* 修复三个问题：
 >     - 修复 `IsLocomotor=yes` 弹头会导致高架桥上的悬浮车辆无法选中与无法被伤害的问题；
 >     - 修复除车辆以外的单位使用 `IsLocomotor=yes` 弹头时停火不会停止拖拽目标的问题；
 >     - 修复桥上的悬浮车辆被摧毁会沉没而不是爆炸的问题
 >   - *`commit c8ad0c0d...`* 修复 `Convert.Deploy` 会在单位移动或转向时重复触发的问题
 >   - *`commit 761b661d...`* 修复了当选定的单位处于武器冷却状态时会对目标使用攻击鼠标指针的问题
 >   - *`commit 5e3c5eb9...`* 修复并优化空袭指引线绘制
 >   - *`commit 6ee0c389...`* 更新维护人员名单和归属条款
 >   - *`commit e89e4122...`* 修复 `DeployingAnim.AllowAnyDirection=yes` 不对 `DeployToLand=yes` 悬浮空军生效的问题
 >   - *`commit a1078c09...`* 优化 *`commit 630f05e9...`* 中获取动画位置的方式
 >   - *`commit 5dee5dc7...`* 修复特殊情况下炮塔军衔或乘客军衔导致崩溃的问题
 >   - *`commit f85ca005...`* -> *`commit 36334b45...`* 优化超武侧边栏的按钮底层，并修复内存泄漏的问题和功能失效的问题
 > - `( 6 )` 修复子单位可能导致父单位无法出厂的问题
 > - `( 6 )` 修复 `InheritTarget` 且非 `InheritTarget.Force` 的附加类型会导致附加单位以不可攻击的目标为目标的问题
 > - `( 6 )` 优化 `Trajectory` 抛射体类型相关的代码
 > - `( 6 )` 优化所有按钮底层

### 2025.7.19  `Phobos-build-47+6_8` -> `Phobos-build-47+6_9`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.7.19 - `commit 491e15adb11fedc8fb5b20d62d64e26523d80c6b`*）
 >   - *`commit 3050d8d9...`* 现在获得护盾时也会先更新染色效果
 >   - *`commit f044227c...`* 新增 `Image.ConditionYellow` 和 `Image.ConditionRed` 支持飞机使用
 >   - *`commit f26c3d00...`* 修正说明书的内容和格式
 >   - *`commit d3a48174...`* 优化选择部分的代码
 >   - *`commit 8536ff18...`* 修复 `GainSelfHealFromPlayerControl` 会让玩家作战方的治疗效果提供给所有阵营的问题
 >   - *`commit 630f05e9...`* 新增 `AttachedAnimPosition` 替换原有的 `UseCenterCoordsIfAttached` 功能，增加可选的动画位置
 >   - *`commit b19ac4b6...`* 优化护盾部分的代码
 >   - *`commit 92ef4bb9...`* 新增 `SkipCrushSlowdown` 选项，用于直接禁用碾压减速
 >   - *`commit 25a51e56...`* 新增 `KeepRange.EarlyStopFrame` 选项，用于让单位在距下次开火多长时间内不会使用 `KeepRange`
 >   - *`commit 92a31d47...`* -> *`commit 96c36a9b...`* 修复单位在桥底停止时自爆的问题和动画导致的崩溃问题
 >   - *`commit 9b3ea770...`* 新增 `SHP.RefreshAfterDelay` 等 `BannerType` 相关标签
 >   - *`commit 491e15ad...`* 修复负伤害在护盾逻辑中处理错误的问题
 > - `( 6 )` 更新 `Trajectory.Tracing.ChasableDistance` 的逻辑
 > - `( 6 )` 更改 `Trajectory.Tracing.TraceTheTarget` 键名为 `Trajectory.Tracing.TrackTarget`
 > - `( 6 )` 修复 `Trajectory.ProximityDamage` 在 `Trajectory.DamageEdgeAttenuation` 为默认值时不会计算发射者火力加成的问题
 > - `( 6 )` 修复 `Trajectory.CreateCapacity` 会导致使用负伤害的单位在射击达到最大抛体数量时不会停止攻击满血单位的问题
 > - `( 6 )` 修复 `AttackMove.StopWhenTargetAcquired` 的单位有时不会退出 `AttackMove` 状态的问题
 > - `( 6 )` 现在 `InheritTarget` 支持父单位被选中时启用子单位的 `Turret.PointToMouse`
 > - `( 6 )` 新增 `InheritExperience` 选项，用于让子单位的击杀信息被记录到其父单位上（如经验）
 > - `( 6 )` 新增 `AINodeWallsOnly` 选项，用于让电脑不在非围墙节点上放置连续围墙
 > - `( 6 )` 新增 `AICleanWallNode` 选项，用于让电脑不能在周围不含 `ProtectWithWall=yes` 建筑的位置放置围墙，并自动清理其节点
 > - `( 6 )` 新增 `CanBuildUnderUnits` 选项，用于控制建筑是否能在有步兵、车辆和飞机的格子上直接放置

### 2025.7.13  `Phobos-build-47+6_7` -> `Phobos-build-47+6_8`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.7.13 - `commit 66dea339e48c850f2c045d6b2d727a95fdf6d297`*）
 >   - *`commit 1155b2d1...`* -> *`commit 66dea339...`* 优化和修正少量代码，修正说明书内容
 > - `( 6 )` 修复 `Phobos-build-47+6_7` 的更新后，建造 `LimboBuild=yes` 的建筑可能会崩溃的问题
 > - `( 6 )` 修复 `PlaceBuilding.Extra` 创建初始建造事件不正确的问题

### 2025.7.13  `Phobos-build-47+6_6` -> `Phobos-build-47+6_7`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.7.13 - `commit 870d103b6e28cbcf6c5ba77aff7e4aef038503ca`*）
 >   - *`commit 327b7e2a...`* -> *`commit 1675d7aa...`* 优化和修正少量代码，修正说明书内容
 >   - *`commit cfd66d46...`* 新增 `AINormalTargetingDelay`  等相关自定义选项，用于单独定义索敌延迟
 >   - *`commit ccbf8503...`* 新增 `DamageOwnerMultiplier.NotAffectsEnemies` 等选项，用于定义仅友伤弹头的默认值
 >   - *`commit 0a3b6508...`* 修复 `AttackMove` 的单位如果因为某些原因进入了其它任务后无法自动恢复的问题
 >   - *`commit 4048dfa4...`* 修复并拓展与步兵相关的很多行为
 >   - *`commit 0f2b30a5...`* 优化 `JumpjetTilt` 的性能表现
 >   - *`commit ca5686f4...`* -> *`commit 870d103b...`* 更新项目内容，修复一个错误
 > - `( 6 )` 修复 `Trajectory.ProximityImpact` 对正面方向的大型建筑判断距离时丢失补正的问题
 > - `( 6 )` 新增 `PlaceBuilding.Extra` 等相关自定义选项，用于更改不同方向下放置的建筑

### 2025.7.9  `Phobos-build-47+6_5` -> `Phobos-build-47+6_6`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.7.9 - `commit d29d61844259e07718eb28e78523fda49576fbfa`*）
 >   - *`commit 48cbd72c...`* -> *`commit e778dc08...`* 优化和修正少量代码，修正说明书内容和格式
 >   - *`commit 949ce1e9...`* 新增 `DelayedFire.Duration` 等相关自定义选项，用于制作武器充能前摇
 >   - *`commit d29d6184...`* 新增 `Burst.NoDelay` 选项，用于同时发射多发武器
 > - `( 6 )` 修复 `VHPScan.Enhanced` 会导致读取存档崩溃的问题
 > - `( 6 )` 现在 `VHPScan.Enhanced` 会判断 `AffectsEnemies` 和计算 `DamageEnemiesMultiplier`
 > - `( 6 )` 优化 `DistributeTargetingFrame` 的实现方式
 > - `( 6 )` 更改 `Laser.IsTracking` 的部分逻辑

### 2025.7.6  `Phobos-build-47+6_4` -> `Phobos-build-47+6_5`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.7.6 - `commit fce18f34fdeeae0bd70b1109dfe4527190439f35`*）
 >   - *`commit 71622be6...`* -> *`commit 28367af4...`*  修正代码注释、说明书内容和格式
 >   - *`commit c8955a5a...`* 新增 `DamageSourceHealthMultiplier` 和 `DamageTargetHealthMultiplier` 选项，用于增加生命伤害倍率
 >   - *`commit 71d13298...`* 修复单位初始 AE 的一些错误优化
 >   - *`commit 280b1c8c...`* 修复动画伤害来源的少量错误优化
 >   - *`commit b6de2f62...`* 修复 `Locomotor=Jumpjet` 的矿车的一些问题
 >   - *`commit c7f00e54...`* 现在 `MessageApplyHoverState` 也能影响居中的字幕
 >   - *`commit 3046d3d7...`* -> *`commit 5f8bf9ef...`* 优化计算距离部分的代码
 >   - *`commit 24346933...`* 新增触发条件 `606` ，当特定的 `AttachEffectType` 被赋予到指定单位上时触发
 >   - *`commit 11ba1482...`* -> *`commit ed1ac64f...`* 修正部分说明书和功能对应错误的问题
 >   - *`commit 338132e8...`* 修复子机在母舰被EMP时会往地图边缘移动的问题
 >   - *`commit 88ddabad...`* -> *`commit 6fa89483...`* 优化 `BombParachute` 、`DropPod` 和 `Anim` 部分的代码
 >   - *`commit a297af8d...`* 修复 `AffectsBelow/AbovePercent` 等类似选项判断区间的问题，并新增 `Crit.AffectsAbovePercent` 选项
 >   - *`commit f15e6832...`* 新增 `SW.Link` 等相关自定义选项（替换了原本的 `SW.GrantOneTime`）
 >   - *`commit 37802d9c...`* 新增 `AffectsNeutral` 选项，并优化血量检测部分的代码
 >   - *`commit e2d12c4b...`* 新增 `Parachuted.FallRate` 和 `Parachuted.MaxFallRate` 选项，并修复了 `Parachuted` 的已知问题
 >   - *`commit ef0be967...`* -> *`commit fce18f34...`* 优化拦截抛体部分的代码
 > - `( 6 )` 新增 `TargetExtraThreat` 等相关自定义选项，用于设置方向威胁系数
 > - `( 6 )` 新增 `DecloakDamagedTargets` 等相关自定义选项，用于解除解除隐身相关硬编码
 > - `( 6 )` 新增 `AIAdjacentMax` 和 `AIAdjacentMax.Campaign` 选项，用于限制电脑的放置建筑范围
 > - `( 6 )` 新增 `InvisoBlockageFix` 选项，用于修复 `Inviso=yes` 的抛射体可能被阻挡问题
 > - `( 6 )` 新增 `InheritTarget` 和 `InheritTarget.Force` 选项，用于让 `AttachmentTypes` 中的子单位继承父单位的攻击目标
 > - `( 6 )` 修复 `UnifiedRadarColor` 不能正确关闭统一单位雷达颜色功能的问题
 > - `( 6 )` 修复 `AttackMove` 的单位如果因为某些原因进入了其它任务后无法自动恢复的问题
 > - `( 6 )` 修复 `Phobos-build-47+6_3` 的更新后，部分情况下 `AttachmentTypes` 中的父单位死亡会崩溃的问题
 > - `( 6 )` 现在 `AttachmentTypes` 不完全相同的单位在变形后也会自动移除并赋予新的附加单位
 > - `( 6 )` 现在 `AttachmentTypes` 中的子单位的侵略模式和停火模式会遵从父单位的设置
 > - `( 6 )` 优化 `VHPScan.Enhanced` 的计算方式
 > - `( 6 )` 优化 `Trajectory` 类抛射体的逻辑

### 2025.6.29  `Phobos-build-47+6_3` -> `Phobos-build-47+6_4`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.6.29 - `commit 323648f52f8bb96777bd4b0f2b9c07f1820ed6d0`*）
 >   - *`commit 6d70f9d0...`* 修正说明书的内容和格式
 >   - *`commit 75b447a6...`* 新增 `AttackMove.StopWhenTargetAcquired` 等相关自定义选项，用于改善移动攻击行为
 >   - *`commit cb3a2815...`* 修复 `MindControlRangeLimit` 距离检查的一个错误
 >   - *`commit 1be2d46e...`* 新增 `GroundShape` 等相关自定义选项，用于改进自定义选择框的绘制功能
 >   - *`commit 11205db8...`* 新增 `MultiWeapon` 等相关自定义选项，用于让单位能够使用多种武器
 >   - *`commit c3ac33b1...`* 新增 `ForceWeapon.Naval.Units` 等相关自定义选项，用于让单位强制对特定类型单位使用特定武器
 >   - *`commit 50e65635...`* 新增 `AttackMove.Follow.IfMindControlIsFull` 选项，用于将心控数已满的单位视作 `AttackMove.Follow`
 >   - *`commit 4f27ca04...`* 更改 `AttackMove.Follow` 跟随单位的计算方式
 >   - *`commit ed807781...`* 优化 `RadSite` 的计算
 >   - *`commit 74d3082c...`* 修复 `OpenToppedWarpDistance` 与射程的计算方式不一致的问题
 >   - *`commit 2c9e3287...`* 新增 `MessageDisplayInCenter` 等相关自定义选项，用于在屏幕中间显示字幕
 >   - *`commit 323648f5...`* 移除 `PR#1629` 的修复以避免其在部分情况下产生空气墙
 > - `( 6 )` 退回 `Phobos-build-47+6_3` 中 `ThisIsAJumpjet` 填写布尔值的修改，现在需要填写车辆注册名
 > - `( 6 )` 修复 `Phobos-build-47+6_2` 的更新后，`IsSingleColor` 在 `Laser.IsTracking` 未启用时失效的问题
 > - `( 6 )` 修复 `Phobos-build-47+6_3` 的更新后，某些进入行为可能导致崩溃的问题
 > - `( 6 )` 修复 `AttachmentTypes` 在变形后无法被自动赋予和移除的问题
 > - `( 6 )` 修复 `KeepWarping.Distance` 与射程的计算方式不一致的问题
 > - `( 6 )` 修复 `Locomotor=Jumpjet` 的矿车在玩家操作令其同时返回矿场时会重叠并导致错误的问题
 > - `( 6 )` 修复 `AU` 无法自动攻击潜地单位的问题
 > - `( 6 )` 修复 `JustHasRallyPoint` 有时失效并导致错误的问题
 > - `( 6 )` 新增 `DamageWallRecursivly` 选项，用于禁用围墙递归受伤
 > - `( 6 )` 新增 `AutoTargetWalls` 和 `EndAutoTargetingIfFindWalls` 选项，用于更改自动攻击围墙的行为
 > - `( 6 )` 新增 `ReverseEngineer` 选项，用于逆向目标单位类型的科技
 > - `( 6 )` 新增 `DebugToolEnabled` 选项，用于启用原本的调试功能及快捷键
 > - `( 6 )` 现在 `ShowCurrentInfo` 快捷键更名为 `CrimViewerEx`
 > - `( 6 )` 现在使用完整的 EC 组件可以屏蔽信息提示和版本提示

### 2025.6.22  `Phobos-build-47+6_2` -> `Phobos-build-47+6_3`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.6.22 - `commit 8bd666c277200bf28b3e76bc3ea739437540502d`*）
 >   - *`commit 2938be7a...`* 优化获取单位类型部分的代码
 >   - *`commit ff4e0aac...`* 优化反抛射体功能部分的代码
 >   - *`commit 8bd666c2...`* 新增 `DebrisMinimums` 和 `DebrisTypes.Limit` 选项，可以进一步控制碎片数量
 > - `( 6 )` 修复 `Laser.IsTracking` 有时未能正确清除导致崩溃的问题
 > - `( 6 )` 修复 `ThisIsAJumpjet` 可能导致崩溃的问题，且现在其只需要填写布尔值
 > - `( 6 )` 修复 `AttachmentTypes` 导致步兵无法正确进入某些类型的建筑的问题

### 2025.6.19  `Phobos-build-47+6_1` -> `Phobos-build-47+6_2`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.6.19 - `commit ae15a03ad7f61b35db9c9353361769d97a26d7fc`*）
 >   - *`commit f64c5f5c...`* 新增 `EngineerRepairAmount` 和 `BuildingRepairedSound` 选项，用于定义工程师维修建筑时的修复量和声音
 >   - *`commit 1f842e12...`* 修复 `VoxelAnim` 的 `TrailerAnim` 位置不正确的问题，并新增 `Trailer.SpawnDelay` 选项，用于自定义尾迹动画生成间隔
 >   - *`commit 4dd3c7d0...`* -> *`commit 68748799...`* 优化 AE 、护盾和染色的部分代码
 >   - *`commit ad05c751...`* 修正说明书的内容和格式
 >   - *`commit e195a836...`* 新增 `BannerType` 相关功能
 >   - *`commit e18cdc09...`* 优化所有容器类增加元素的代码
 >   - *`commit ae3e44ac...`* 修正说明书的内容和格式
 >   - *`commit c722b452...`* 新增 `ExpireWeapon.UseInvokerAsOwner` 等自定义选项
 >   - *`commit b205830a...`* 新增 `Refinery.UseNormalActiveAnim` 选项，可以让矿场使用常规的工作动画
 >   - *`commit 1ecd2815...`* 优化获取单位类型部分的代码
 >   - *`commit b21f0f88...`* 新增 `Damage.ApplyFirepowerMult` 选项，用于让动画伤害继承原单位火力加成
 >   - *`commit e901028c...`* 修复 `ExtendedAircraftMissions` 不能让飞机在攻击结束但仍有弹药时继续前往下一路径点的问题
 >   - *`commit d86c1596...`* 修复 `Shield.SelfHealing.RestartInCombatDelay` 未正确生效的问题
 >   - *`commit d3b814d2...`* 修复和优化部分ini的读取
 >   - *`commit 3728a080...`* 新增 `DrawType` 等相关自定义选项，用于绘制电弧尾迹和辐射尾迹，并重写了自定义电弧相关功能
 >   - *`commit ae15a03a...`* 修正说明书的格式
 > - `( 6 )` 修复 `Laser.IsTracking` 有时未能正确清除导致崩溃的问题和存档问题
 > - `( 6 )` 修复 `DrawAboveTechno=false` 的选择框无法被其它建筑和树木等遮挡的问题，并新增 `GroundShape` 等相关自定义选项
 > - `( 6 )` 修复 `DrawAdjacentBoundary` 在显示区域边界位置绘制出错的问题

### 2025.6.14  `Phobos-build-47+6_0` -> `Phobos-build-47+6_1`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.6.14 - `commit 6f61cbf4c7cd413c07db7c5f4b2f776ba86cb506`*）
 >   - *`commit 81f29ff9...`* -> *`commit 3798f3f3...`* 修正说明书的内容和格式
 >   - *`commit f060ec96...`* 新增 `FiringForceScatter` 选项，用于控制扫射飞机攻击的目标是否会提前躲避
 >   - *`commit 69f10842...`* 修复 `AlphaImage` 在单位进入隧道后仍然存在的问题
 >   - *`commit 38f5ef27...`* 新增 `BunkerDamageMultiplier` 等相关自定义选项，用于微观定义建筑加成属性
 >   - *`commit d0a94e42...`* 新增 `Crater.DestroyTiberium` 选项，用于自定义 `Crater=yes` 的动画是否摧毁所在位置的矿物
 >   - *`commit 44600046...`* 优化获取护甲类型部分的代码
 >   - *`commit 72bbf8bb...`* 修复 `Message.Color` 会输出大量无用debug信息的问题
 >   - *`commit f4439a13...`* 新增 `CanTarget.MaxHealth` 和 `CanTarget.MinHealth` 选项，用于定义武器可攻击对象的血量条件
 >   - *`commit 7934631d...`* 优化武器选择的部分代码
 >   - *`commit 52e3a48b...`* 新增 `SuperWeaponSidebar.Pyramid` 选项，用于控制侧边栏形状
 >   - *`commit 5724493f...`* 修复 `ElectricAssault` 会自动选择到不可攻击的可充能建筑而导致卡住的问题
 >   - *`commit a6a67920...`* 修复 `Convert.ResetMindControl` 的问题并补充说明
 >   - *`commit 02f017a5...`* 新增 `FireUp` 和 `FireUp.ResetInRetarget` 选项，并适配 `Burst.FireWithinSequence` ，用于让 SHP 车辆实现攻击前摇
 >   - *`commit 2592d1d9...`* 修正说明书的内容和格式
 >   - *`commit 094b10af...`* 现在 `IsGattling` 支持步兵使用，并修复了 `RateDown.Reset` 未正确重置开火音效的问题
 >   - *`commit 71e2a9d0...`* 修复原版中大范围伤害的错误逻辑
 >   - *`commit a61143dd...`* 新增 `InfoType` 类型和相关自定义选项，用于拓展数显功能
 >   - *`commit a1a18c0f...`* -> *`commit fc4a19a8...`* 优化飞机相关功能代码
 >   - *`commit dbe57c82...`* -> *`commit 5c18f73e...`* 修复 `DropPodTrailer` 的默认值问题
 >   - *`commit 8bac242d...`* -> *`commit af6b641c...`* 更新工具集
 >   - *`commit 27e87350...`* 修复 `CanKill` 忽略 `AffectsXXX` 阵营的问题
 >   - *`commit 5a0f504d...`* 更新贡献文档
 >   - *`commit baf292c8...`* 优化护盾条绘制的部分代码
 >   - *`commit 19c15f65...`* 修复 `ReturnWeapon` 在命中地面目标后无法正确发射 `ROT` 抛射体的问题
 >   - *`commit 53fff4fa...`* 优化分裂武器逻辑的部分代码
 >   - *`commit 6c2af207...`* 修复超武侧边栏无法重复添加同种超武的问题
 >   - *`commit 38c4f8a3...`* 修复 `DamageSelf` 和 `AllowDamageOnSelf` 对空军无效的问题
 >   - *`commit d594920c...`* 修复 `BunkerWallsUpSound` 和 `BunkerWallsDownSound` 未正确保存的问题
 >   - *`commit e47ca8ee...`* 优化子机限制距离的部分代码
 >   - *`commit f7d7591c...`* 新增 `BerzerkTargeting` 选项，用于自定义混乱时的索敌
 >   - *`commit fea4401e...`* 新增 `DigitalDisplay.Health.FakeAtDisguise` 选项，用于让单位数显显示伪装对象的生命值
 >   - *`commit fc7d6a40...`* 新增 `AffectsAbovePercent` 和 `AffectsBelowPercent` 选项，用于让弹头仅影响血量符合条件的单位
 >   - *`commit 5438ff38...`* 修复 `SuperWeaponSidebar.RequiredSignificance` 读取类型错误的问题
 >   - *`commit 6f61cbf4...`* 移除单位变形时血量修复的多余判断
 > - `( 6 )` 修复 `Squad.IsInitAsTeam` 在队员转移所属后存在的问题
 > - `( 6 )` 修复 `ShowCurrentInfo` 开启后在 SHP 单位经过某些桥头时会崩溃的问题，并修复寻路显示
 > - `( 6 )` 修复 `ExtraTargeting` 会导致 `KeepTargetOnMove` 失效的问题
 > - `( 6 )` 修复 `ForceWeapon.Naval` 默认值未使用 `ForceWeapon.Units` 的问题
 > - `( 6 )` 修复 `VoxelAnim` 的 `TrailerAnim` 位置不正确的问题，并新增 `Trailer.SpawnDelay` 选项，用于自定义尾迹动画生成间隔
 > - `( 6 )` 修复 `ProximityImpact` 在抛射体会转向时，有时无法获取到范围内目标的问题
 > - `( 6 )` 优化 `MultiWeapon` 的判断逻辑，且现在支持 `NoSecondaryWeaponFallback` 和 `NoSecondaryWeaponFallback.AllowAA`
 > - `( 6 )` 现在 `StrafingTargetScatter` 已改为由公版的 `FiringForceScatter` 来实现功能
 > - `( 6 )` 现在 `AnimCraterReduceTiberium` 和 `Crater.ReduceTiberium` 已改为由公版的 `Crater.DestroyTiberium` 来实现功能
 > - `( 6 )` 现在 `Trajectory.Parabola.BounceOnWater` 由更全面的 `Trajectory.Parabola.BounceOnTarget` 替代
 > - `( 6 )` 现在 `ExtraTargeting` 会让玩家单位受到伤害且 `PlayerReturnFire=yes` 时重新索敌，这会替换本来的覆盖任务行为
 > - `( 6 )` 新增 `Trajectory.Parabola.BounceOnHouses` 选项，用于控制抛体可以在哪些所属的单位上发生弹跳
 > - `( 6 )` 更新使用须知

### 2025.6.5  `Phobos-build-46+3_31` -> `Phobos-build-47+6_0`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.6.5 - `commit 5e144c969420c209f596199f308e41ff2adb5fa5`*）
 >   - *`commit e3e86fbb...`* -> *`commit c8d2f3cc...`* 修正说明书的内容和格式
 >   - *`commit a46eecb7...`* 版本号更新（[build-46] -> [build-47]）
 >   - *`commit 1dba2a4c...`* 现在 `HarvesterScanAfterUnload` 不会影响 `Weeder`
 >   - *`commit 751a94e2...`* 修复单位在即将被碾压或即将被飞机攻击时会不受控制散开的问题
 >   - *`commit deecb4c2...`* 新增 `ForceWeapon.InRange.TechnoOnly` 选项，用于控制其是否仅在攻击非科技类型时能够生效
 >   - *`commit 722eb64b...`* 新增 `SuperWeaponSidebar` 等相关自定义选项，用于启用超武左侧边栏（仍保留在《额外功能说明》）
 >   - *`commit 5e144c96...`* 修复单位变形的各种问题
 > - `( 6 )` 现在 `IsGattling` 支持步兵使用
 > - `( 6 )` 现在 `ForceWeapon.Units` 等选项的优先级高于 `ForceWeapon.InRange`
 > - `( 6 )` 现在 `ExtraTargeting` 替换了原本的三个子选项
 > - `( 6 )` 新增 `Squad.Members` 和 `Squad.IsInitAsTeam` 选项，用于生产和选择小队
 > - `( 6 )` 新增 `VHPScan.Enhanced` 选项，用于增强 `VHPScan=Strong` 的功能
 > - `( 6 )` 更改 `ExtendedAircraftActions` 三个子选项前缀名为 `ExtendedAircraftMissions`
 > - `( 6 )` 修复 `Phobos-build-46+3_26` 的更新后，自动散开带有树等非科技类型的格子上的单位时会崩溃的问题
 > - `( 6 )` 修复 `Phobos-build-46+3_31` 的更新后，`Trajectory=Bombard` 的抛体下落速度异常的问题
 > - `( 6 )` 修复飞机执行警戒任务时可能会卡在盘旋状态的问题
 > - `( 6 )` 更新《额外功能说明》格式

### 2025.6.1  `Phobos-build-46+3_30` -> `Phobos-build-46+3_31`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.5.30 - `commit adffe1259823046f3344d888cb2e937104555204`*）
 >   - *`commit 4c514596...`* -> *`commit d7a5bc7e...`* 修正说明书的内容和格式
 >   - *`commit a27cb662...`* 修复 *`commit 0fc65841...`* 后出售带有成员的生化反应炉会崩溃的问题，修复 `UnitAbsorb` 的建筑在乘客使用 `AutoDeath` 消失后不刷新电力的问题，修复 `NoQueueUpToEnter` 与 `Passengers.BySize` 共同使用时无视 `SizeLimit` 的问题
 >   - *`commit 862ac2f9...`* 修复辐射类型的错误伤害衰减问题
 >   - *`commit 7a1dfa1c...`* -> *`commit 218c199e...`* 优化部分代码，并新增 `GainSelfHealFromPlayerControl` 等相关自定义选项，用于共享玩家/盟友的回复效果
 >   - *`commit 75ecfd98...`* 改进简报功能并改进与 `Spawner` 的自定义任务的交互逻辑
 >   - *`commit 0698da6b...`* -> *`commit adffe125...`* 优化部分代码，并修复触发 75、77 在同一帧内只会触发一次的问题
 > - `( 3 )` 现在 `SuperWeaponSidebar` 支持 `AnchoredToolTips` 设置，并新增 `SuperWeaponSidebarKeysEnabled` 等相关自定义选项，详细说明请见《额外功能说明》
 > - `( 3 )` 现在 `Locomotor=AdvancedDrive` 的单位支持 `ExtendedScatterAction`
 > - `( 3 )` 修复 `Phobos-build-46+3_26` 的更新后，目标平均分配模式无法正确选中非中立目标的问题，以及载具在和其它物体重叠时被摧毁会崩溃的问题
 > - `( 3 )` 新增 `Trajectory.LeadTimeMaximum` 选项，用于计算提前量时让抛体不会飞得太远
 > - `( 3 )` 新增 `CeaseFireStance` 等相关自定义选项，用于启用单位的停火模式
 > - `( 3 )` 新增 `SkipFrameDelay` 选项，用于减少绘制帧率，将CPU资源留给逻辑运算
 > - `( 3 )` 新增 `MultiWeapon` 等相关自定义选项，用于让单位能够使用多种武器
 > - `( 3 )` 新增 `ForceWeapon.Units` 等相关自定义选项，用于让单位强制对特定类型单位使用特定武器
 > - `( 3 )` 增加部分用于定位问题的调试日志内容，现在在开启调试功能后，遇到部分已知问题时，会进入帧步进模式，并聚焦屏幕显示

### 2025.5.25  `Phobos-build-46+3_29` -> `Phobos-build-46+3_30`

 > - `( 3 )` 现在 `ShowCurrentInfo` 能够显示当前鼠标指向的单位
 > - `( 3 )` 修复 *`commit 4149958b...`* 的更新后辐射伤害会无视上限且可能存在除零错误的问题
 > - `( 3 )` 修复 `Message.Color` 会输出大量无用debug信息的问题
 > - `( 3 )` 修复 `Phobos-build-46+3_26` 的 `AttachmentTypes` 的更新后，脚本小队中乘客无法全部进入载具进而导致内存泄漏的问题
 > - `( 3 )` 修复 `AttachmentTypes` 中部分与 `ExtendedBuildingPlacing` 、`ExtendedScatterAction` 和 `NoQueueUpToEnter` 之间存在的交互问题
 > - `( 3 )` 更新《抛体案例参考》
 > - `( 3 )` 更新使用须知，**请所有使用者认真阅读注意事项**

### 2025.5.25  `Phobos-build-46+3_28` -> `Phobos-build-46+3_29`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.5.25 - `commit 4188bf533dbdad03b50f78b86359c804b566b146`*）
 >   - *`commit 1d99368c...`* 修复 *`commit 85669c3d...`* 导致快捷键部署载具时会RE的问题
 >   - *`commit 4188bf53...`* 修复 *`commit 6171fe04...`* 导致在进入游戏加载时闪退的问题

### 2025.5.24  `Phobos-build-46+3_27` -> `Phobos-build-46+3_28`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.5.24 - `commit 6254d22ef86c3e143dc4a498f0ffaaa596490ca5`*）
 >   - *`commit 9133a1b5...`* 脚本行为代码优化
 >   - *`commit 79fa59e2...`* 现在当抛射体为任意 `Trajectory` 类型时，`Strafing` 的默认值为否
 >   - *`commit b1fb2e25...`* 修复空袭逻辑在罕见情况下导致的崩溃
 >   - *`commit 95aa763b...`* 新增 `AirstrikeLineColor` 等相关自定义选项，用于自定义空袭激光和染色的颜色
 >   - *`commit 6254d22e...`* 修复 *`commit d0add0f9...`* 未正确生效的的问题
 > - `( 3 )` 修复 *`commit 65b7502c...`* 后残骸无法生成的问题
 > - `( 3 )` 修复 `Locomotor=AdvancedDrive` 的单位在碾压沙袋后会陷入永久减速状态的问题
 > - `( 3 )` 新增 `TunnelSimpleDistTooFar` 和 `TunnelPathingDistTooFar` 选项，用于自定义 `Locomotor=Tunnel` 的单位在目的地距离多远时会选择钻地移动
 > - `( 3 )` 新增 `BerzerkTargeting` 选项，用于自定义混乱时的索敌
 > - `( 3 )` 新增 `DefeatedBehavior` 选项，用于扩展作战方被击败时的行为

### 2025.5.19  `Phobos-build-46+3_26` -> `Phobos-build-46+3_27`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.5.19 - `commit 9dccf0081387f3351724f71185d525b962318c69`*）
 >   - *`commit 2bba7c30...`* -> *`commit 65b7502c...`* 代码优化
 >   - *`commit 44976f58...`* 修复 `OmniFire.TurnToTarget` 和 `NoTurret.TrackTarget` 的组合不能让单位在武器冷却时转向的问题
 >   - *`commit 1297c0e6...`* 修正说明书的内容和格式
 >   - *`commit 9dccf008...`* 修复 *`commit 65b7502c...`* 后的存档崩溃问题
 > - `( 3 )` 现在 `AttachmentTypes` 相关自定义选项部分更换了位置，详细说明请见《额外功能说明》
 > - `( 3 )` 优化 `ShowCurrentInfo` 显示的信息格式和内容
 > - `( 3 )` 新增 `Laser.IsTracking` 选项，用于让激光一直持续并跟随开火者和目标单位
 > - `( 3 )` 新增 `AnimCraterReduceTiberium` 和 `Crater.ReduceTiberium` 选项，用于自定义 `Crater=yes` 的动画是否摧毁所在位置的矿物
 > - `( 3 )` 修正《抛体案例参考》中部分过时的用法，并更新了两个案例

### 2025.5.16  `Phobos-build-46+3_25` -> `Phobos-build-46+3_26`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.5.16 - `commit fce64118da6c0b5a9830ce2d5e86127270d02863`*）
 >   - *`commit 0036b273...`* 修复原版中船可以在高架桥上移动的问题
 >   - *`commit 0fc65841...`* 修复 `PassengerDeletion` 和 `AutoDeath` 让单位从生化反应炉内消失时不会更新反应炉所属方电力的问题
 >   - *`commit 74ec72ce...`* 修正说明书的内容和格式
 >   - *`commit 3263ad26...`* 现在 `AirburstWeapon` 和 `ReturnWeapon` 支持绘制 Ares 的自定义颜色电弧
 >   - *`commit a212003e...`* 修复很多情况下未将单位护盾护甲代入条件检查的问题，并优化部分护盾相关代码实现
 >   - *`commit f523f539...`* 修复 `Arcing.AllowElevationInaccuracy` 未正确对弹道进行修复的问题
 >   - *`commit d0add0f9...`* 现在 `OverlayTypes` 允许超过 255 种
 >   - *`commit d6307cdc...`* -> *`commit fce64118...`* 代码优化
 > - `( 3 )` 现在 `Trajectory.DisperseWeapons` 支持绘制 Ares 的自定义颜色电弧
 > - `( 3 )` 现在 `BalloonHoverPathingFix` 默认开启且会影响追赶敌人时的寻路
 > - `( 3 )` 现在弹道抛体不会在发射者死亡后像原版抛体一样加快坠落速度
 > - `( 3 )` 修复 `Trajectory=Missile` 使用 `Inaccurate` 时会有大概率攻击中心位置的问题
 > - `( 3 )` 修复 `AttackMove.StopWhenTargetAcquired` 生效时会导致 `Locomotor=Jumpjet` 的单位朝向右下的问题
 > - `( 3 )` 修复 `ExtraTargeting` 部分选项对子机类等生成物仍然有效的问题
 > - `( 3 )` 修复 `Trajectory.RecordSourceCoord` 在 `Trajectory=Tracing` 时默认值错误的问题
 > - `( 3 )` 修复 `Trajectory.CreateCapacity` 在连续两次读档后能够超过限制的问题
 > - `( 3 )` 新增 `AdvancedDrive.MinimumDistance` 选项，用于控制最小自动倒车距离
 > - `( 3 )` 新增 `AttachmentTypes` 等相关自定义选项，用于在单位上附加单位，详细说明请见《额外功能说明》

### 2025.5.11  `Phobos-build-46+3_24` -> `Phobos-build-46+3_25`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.5.11 - `commit f0c0f4b2660aba19d1281dfe70a14ae5a3dc569e`*）
 >   - *`commit f00c9a10...`* 修复 `CanKill=no` 会在致命的友军伤害减免为非致命后直接将单位生命置1的问题
 >   - *`commit 41c191b1...`* 修复 *`commit f61f3455...`* 的更新后当奴隶矿场类单位被移除时会弹窗的问题
 >   - *`commit 36a3b565...`* -> *`commit ccc2df99...`* 修复触发条件 600（护盾被击碎）无条件生效的问题
 >   - *`commit 9afc212f...`* 修复 `CanKill=no` 会在致命的范围伤害衰减为非致命后直接将单位生命置1的问题
 >   - *`commit 85669c3d...`* -> *`commit f0c0f4b2...`* 修复 `VoiceDeploy` 在使用快捷键时不会播放的问题
 > - `( 3 )` 修复 `Trajectory.Missile.PreAimCoord=0,0,0` 时抛射体无视 `Inaccurate` 和 `Trajectory.OffsetCoord` 的问题

### 2025.5.8  `Phobos-build-46+3_23` -> `Phobos-build-46+3_24`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.5.7 - `commit 03e42a101fd34bf6b2b57fe68da599edf110c253`*）
 >   - *`commit eaccfff6...`* 修正说明书的内容和格式
 >   - *`commit 5e6e4d47...`* 修复 `AirburstWeapon` 无法正确发射 `Arcing` 抛射体武器的问题
 >   - *`commit 9af8d8df...`* 新增 `TiberiumEater` 等相关自定义选项，用于让单位可以实时将矿石转换为资金
 >   - *`commit b1afa62b...`* 修复 `ReturnWeapon` 无法正确发射 `Arcing` 抛射体武器的问题，以及无法绘制激光电弧等视觉效果的问题
 >   - *`commit 5c71e8ab...`* 修复 `AirburstWeapon` 和 `ReturnWeapon` 发射的 `Arcing` 抛射体武器无视 `Lobber` 的问题
 >   - *`commit 72e8bb2d...`* 新增 `FallingDownDamage` 等相关自定义选项，用于自定义坠落伤害
 >   - *`commit 03e42a10...`* 修复机场上的飞机在连续接受命令并不断取消时可能会错误地悬停在机场上空的问题
 > - `( 3 )` 现在 `Trajectory.Bombard.Height` 会随 `Trajectory.Bombard.FallPercent` 线性变化
 > - `( 3 )` 现在 `ExtendedAircraftMissions` 能够同时控制飞机的降落行为
 > - `( 3 )` 现在 `ExtraTargeting` 等选项对子机类等生成物无效
 > - `( 3 )` 现在 `CanManualReload.DetonateWarhead` 的伤害不再固定为1，而是与重新装弹前的弹药量相同
 > - `( 3 )` 新增 `CanManualReload.WhenFull` 选项，用于让单位能在满弹药时手动重新装填
 > - `( 3 )` 新增 `BalloonHoverPathingFix` 选项，用于修正 `BalloonHover=yes` 的单位的寻路问题

### 2025.5.5  `Phobos-build-46+3_22` -> `Phobos-build-46+3_23`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.5.5 - `commit b31b73b45af6a93b5961cfec04b0c35b0841b75f`*）
 >   - *`commit 73d34867...`* -> *`commit c8c2554b...`* 修正说明书的内容和格式
 >   - *`commit da3f31f5...`* 修复建筑在消失后重新放置在地图上时的不正确行为
 >   - *`commit c00e2601...`* 修复建筑在未被摧毁时被移动会清空动画的问题
 >   - *`commit 6171fe04...`* 新增 `InsigniaType` 相关功能
 >   - *`commit 23ba9a3b...`* 修复生化反应炉能够超额装载的问题
 >   - *`commit bed263a5...`* 新增 `DrawInsignia.UsePixelSelectionBracketDelta` 选项，用于控制军衔绘制是否遵从血条偏移
 >   - *`commit 7990138f...`* 还原建筑指令的不完全修复？
 >   - *`commit 1ab7c994...`* 修复 `Insignia.Passenger` 在载具超载时的崩溃
 >   - *`commit 27806d34...`* 修复超时空采矿车在返回矿场时会进入无敌状态的问题
 >   - *`commit b31b73b4...`* 修复 `NoQueueUpToEnter` 会使得幸存者返回上车地点的问题
 > - `( 3 )` 现在 `Trajectory=Missile` 能够正确代入重力进行计算
 > - `( 3 )` 修复 `Phobos-build-46+3_21` 的更新后 `ShowCurrentInfo` 会在选择建筑时崩溃的问题

### 2025.4.26  `Phobos-build-46+3_21` -> `Phobos-build-46+3_22`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.4.26 - `commit 2c5c07128d6a654d9f89c3941de8782767290cc4`*）
 >   - *`commit e07e36f0...`* 修复并优化 `AttachEffect` 相关功能
 >   - *`commit 1dfca41d...`* 修复 Ares 在更新所属方威胁值时未进行健全性检查导致的崩溃
 >   - *`commit 2fa8d37b...`* 修复 `Strafing=yes` 的航母子机在返回时可能会一直盘旋的问题
 >   - *`commit 268b08f2...`* 修正说明书的内容
 >   - *`commit 3412541f...`* 新增 `Harvester.CanGuardArea` 选项，用于让矿车能够真正进入区域警戒状态
 >   - *`commit 2c5c0712...`* 新增 `HarvesterScanAfterUnload` 选项，用于让矿车在返回后搜寻距离矿场最近的矿石所在地而非原位置
 > - `( 3 )` 修复 `Phobos-build-46+3_21` 的更新后在不开启 `TurretRecoil` 的情况下无法正确绘制多炮塔多炮管的问题
 > - `( 3 )` 新增 `BarrelOverTurret` 选项，用于让炮管图层永远在炮塔之上

### 2025.4.25  `Phobos-build-46+3_20` -> `Phobos-build-46+3_21`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.4.25 - `commit 21a3367a987640480e73eb87c56a4545dfca9ed6`*）
 >   - *`commit 26ae049b...`* 修复原版 `EIP:7FB178` 的错误和子机由非死亡行为被移除时导致的崩溃
 >   - *`commit 3a0bbef9...`* 修正说明书的内容和格式
 >   - *`commit e5b66f00...`* 新增 `SelectBox` 等相关自定义选项，用于自定义单位选择框
 >   - *`commit f61f3455...`* 新增 `AirstrikeTargets` 选项，用于扩展空袭可选目标
 >   - *`commit 6688c42e...`* 修复部分内容未正确存档读档的问题
 >   - *`commit 3d667938...`* 新增 `AttackMove.Aggressive` 等相关自定义选项，用于让玩家单位的移动攻击命令具有侵略性
 >   - *`commit 72c9b03e...`* 修复步兵可能卡在播放移动动画的问题
 >   - *`commit 674f78de...`* 新增 `AmphibiousEnter` 和 `AmphibiousUnload` 选项，用于让载具能够在水面上装载卸载乘客
 >   - *`commit 9aa30f57...`* 新增三种触发类型，用于修改所属方对所属方的仇恨值
 >   - *`commit 4149958b...`* 优化辐射类的性能
 >   - *`commit 7ec5e772...`* 修复 `ExtendedAircraftMissions` 会使得战役中无机场召唤的飞机在投弹结束后直接消失的问题
 >   - *`commit c1c4ef46...`* 修复 `CanBeBuiltOn` 不能让围墙在其上建造的问题
 >   - *`commit 34ae4357...`* 修复 `CanBeBuiltOn` 不能独立于 `IsPassable` 单独正常工作的问题
 >   - *`commit cfd36d49...`* 修复无炮塔的建筑的开火坐标未计算朝向的问题
 >   - *`commit 21a3367a...`* 修复非扫射武器飞机在开火后开火间隔会意外计入返航时间的问题
 > - `( 3 )` 修复 `NoQueueUpToEnter` 能无视 `AmphibiousEnter` 在水上装载乘客的问题
 > - `( 3 )` 修复 `Phobos-build-46+3_20` 的更新后 `ExtendedBuildingPlacing` 不会在所有建造场都无法工作时中断放置任务的问题
 > - `( 3 )` 修复 `Locomotor=AdvancedDrive` 且 `Speed=0` 的单位在脚下的斜坡类型发生变化时会崩溃的问题
 > - `( 3 )` 修复 `WreckageSwapLocomotor` 会使得残骸单位继续行驶到原目的地的问题
 > - `( 3 )` 修复 `Strafing=yes` 的航母子机在返回时可能会一直盘旋的问题
 > - `( 3 )` 现在 `Airstrike.TargetCell` 和 `Airstrike.SecondaryFirst` 功能，由公版 `AirstrikeTargets` 实现
 > - `( 3 )` 现在 `SelectBox` 等相关自定义选项在公版合并后部分键名有所改动
 > - `( 3 )` 新增 `ExtraBarrelCount` 等相关自定义选项，用于绘制炮塔上的额外炮管，并支持独立的制退效果
 > - `( 3 )` 增加 `ShowCurrentInfo` 的显示内容

### 2025.4.21  `Phobos-build-46+3_19` -> `Phobos-build-46+3_20`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.4.20 - `commit 77d7b792597b7371a9894d8c1027b1d4de9d4ad5`*）
 >   - *`commit ed85cea9...`* 代码优化
 >   - *`commit 771e4448...`* 新增 `Overload.Count` 等相关自定义选项，用于微观定义心控超载能力
 >   - *`commit d27a43ad...`* 新增 `RadarInvisibleToHouse` 选项，用于扩展雷达不可见功能
 >   - *`commit 913523aa...`* 新增 `SelfHealing.EnabledBy` 等相关自定义选项，用于扩展护盾逻辑
 >   - *`commit 77d7b792...`* 新增 `HarvesterDumpAmount` 选项，用于限制矿车每次倒矿的量
 > - `( 3 )` 现在 `RadarInvisible.ToSelf` 和 `RadarInvisible.ToAlly` 功能，由公版 `RadarInvisibleToHouse` 实现
 > - `( 3 )` 优化 `ExtendedBuildingPlacing` 、`ExtendedScatterAction` 、`ShowCurrentInfo` 和 `SelectedInfo` 中部分代码
 > - `( 3 )` 修复 `ExtendedBuildingPlacing` 无法让达到建造上限的建筑进入放置等待的问题
 > - `( 3 )` 修复原版 `EIP:7FB178` 的错误（步兵尝试进入一个失效指针指向的原驻军建筑）

### 2025.4.19  `Phobos-build-46+3_18` -> `Phobos-build-46+3_19`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.4.19 - `commit 084fcb6f202f5a59696c1c9414339fc899f4924f`*）
 >   - *`commit 7d4a3d0d...`* -> *`commit 084fcb6f...`* 新增 `Duration.ApplyFirepowerMult` 和 `Duration.ApplyArmorMultOnTarget` 选项，用于让AE的附着时间受到来源火力增幅和目标护甲增幅影响
 > - `( 3 )` 修复 `UnifiedTechnoColor` 相关选项读取错误的问题
 > - `( 3 )` 现在 `OverTechno=no` 将不再额外计算单位的 `PixelSelectionBracketDelta`
 > - `( 3 )` 新增 `InfoIndex` 等相关自定义选项，用于指定数显具体选择类型中的哪个数据来显示

### 2025.4.18  `Phobos-build-46+3_17` -> `Phobos-build-46+3_18`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.4.18 - `commit 7bc8752ea8c88694ba0a9acb75863abeae4fe73e`*）
 >   - *`commit 94d81f5f...`* 面向开发人员的新超武类模板
 >   - *`commit 9d28d33a...`* 修复 `ForceWeapon` 未正确使用对象进行判断的问题
 >   - *`commit 5bfbc65e...`* 修正说明书的内容
 >   - *`commit 7008369b...`* 修复 `KeepTargetOnMove` 会在单位移动后卡在不追击的状态
 >   - *`commit 1341f20c...`* 新增 `CanKill` 等相关自定义选项，用于让单位不能被击杀
 >   - *`commit 7bc8752e...`* 新增 `Parasite.CullingTarget` 选项，用于自定义寄生斩杀类型
 > - `( 3 )` 修复 `HasSecondaryRallyPoint` 的建筑如果将第二集结点设置在单位上时，若单位死亡则会在下次选中该建筑时崩溃的问题
 > - `( 3 )` 修复指令上限优化导致的当缓存目标死亡时游戏崩溃的问题
 > - `( 3 )` 修复 `Locomotor=AdvancedDrive` 会让AI的单位出厂时旋转的问题
 > - `( 3 )` 修复弹道抛体的目标会像原版抛射体一样在原目标死亡后被设置为地面格子的问题
 > - `( 3 )` 新增 `SelectBox` 等相关自定义选项，用于自定义单位选择框，详细说明请见《额外功能说明》

### 2025.4.16  `Phobos-build-46+3_16` -> `Phobos-build-46+3_17`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.4.16 - `commit 35b7bd48fa13545139c5fcaf8424fe2a1bc9a920`*）
 >   - *`commit 23331ae3...`* -> *`commit c9e85c99...`* 代码优化，并修正说明书的内容和格式
 >   - *`commit 040f15e1...`* 新增 `ReflectDamage.Chance` 等相关自定义选项，用于定义伤害反射的效果
 >   - *`commit 35b7bd48...`* 修复 `IsLocomotor=yes` 的弹头在砸伤地面单位后，这个地面单位能够与超时空单位重叠的问题
 > - `( 3 )` 修复 `Trajectory.Missile.UniqueCurve` 无法使用的问题
 > - `( 3 )` 新增 `Trajectory.Tracing.StableRotation` 选项，用于自动计算旋转间隔角度
 > - `( 3 )` 优化 `Locomotor=Skilled` 的自动判断逻辑，且现在矿车倒矿时也会倒车
 > - `( 3 )` 更改 `Locomotor=Skilled` 名为 `Locomotor=AdvancedDrive` ，与其相关的标签同样进行了改动

### 2025.4.13  `Phobos-build-46+3_15` -> `Phobos-build-46+3_16`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.4.13 - `commit 3031541743e11411aa40f6e424187c16cf35d30c`*）
 >   - *`commit d60a9c24...`* 新增 `JumpjetTilt` 等相关自定义选项，用于 `Locomotor=Jumpjet` 的单位绘制倾斜效果
 >   - *`commit bd9b3731...`* 新增 `KillWeapon.Affects` 等相关自定义选项，用于添加目标类型过滤器
 >   - *`commit 6fcdb3a8...`* -> *`commit 3ba2ff6e...`* 代码优化，并修复 `Burst.FireWithinSequence` 可能导致的崩溃问题
 >   - *`commit 9ea36be2...`* 新增 `Spawner.UseTurretFacing` ，用于让子机发射时的初始方向同步发射者炮塔方向
 >   - *`commit 9dfc127a...`* -> *`commit 30315417...`* 据GPLv3重新授权项目，移除 `blowfish.dll` 依赖，更新说明书
 > - `( 3 )` 现在潜地矿车和超时空矿车在满载且在矿场边上被卡停下时会继续开始工作
 > - `( 3 )` 现在 `Locomotor=Skilled` 能够正确按照相反顺序画出VXL单位的步进帧
 > - `( 3 )` 现在 `WreckageType` 禁止单位已为残骸时嵌套生成自己类型的残骸
 > - `( 3 )` 现在 `JumpjetTilt` 的最大限制角度提高到90度，且能够影响阴影的绘制
 > - `( 3 )` 新增 `Trajectory.RetargetHouses` 选项，用于控制弹道抛体重索敌的所属方
 > - `( 3 )` 恢复 `TurretRecoil` 选项和逻辑，用于启用炮塔炮管制退

### 2025.4.12  `Phobos-build-46+3_14` -> `Phobos-build-46+3_15`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.4.11 - `commit 350cc2fbb0e00c6ef30eac9c3f983cda32f80431`*）
 >   - *`commit a9915479...`* 现在 `SpySat` 和 `BigGap` 选项，被 `Reveal` 和 `CreateGap` 替换，能够自定义揭示或创造黑幕的范围
 >   - *`commit c86b162c...`* 修复多个与搭乘交通工具相关的问题
 >   - *`commit 67c83bd7...`* 新增 `ForceWeapon.InRange` 等相关自定义选项，用于强制在特定射程使用特定武器
 >   - *`commit 4f45f262...`* 修复 `DeploysInto` 导致的联机不同步问题
 >   - *`commit bac23646...`* 修复 `BuildingAnim` 在 `Ares3.0p1` 环境下的问题
 >   - *`commit 350cc2fb...`* 修复 `IsLocomotor=yes` 弹头抬起的车辆阴影不正确的问题
 > - `( 3 )` 修复带乘客位置的载具无法离开坦克碉堡的问题
 > - `( 3 )` 现在 `UpdateInLimbo` 禁止除了在 `OpenTopped` 载具内和在驻军建筑内的乘客开火
 > - `( 3 )` 优化 `UniqueTechno` 的显示逻辑
 > - `( 3 )` 新增 `WreckageSwapLocomotor` 选项，用于让残骸保持在正确的位置
 > - `( 3 )` 新增 `DirectionalArmor` 等相关自定义选项，用于定义不同方向上受到伤害时的伤害倍率，详细说明请见《额外功能说明》

### 2025.4.10  `Phobos-build-46+3_13` -> `Phobos-build-46+3_14`

 > - `( 3 )` 修复 `IsLocomotor=yes` 弹头抬起的车辆阴影不正确的问题
 > - `( 3 )` 优化 `Locomotor=Skilled`
 > - `( 3 )` 新增 `Skilled.ConfrontEnemies` 选项，可以控制单位在攻击目标时是否强制朝向目标而非由当前朝向自动控制朝向
 > - `( 3 )` 新增 `WreckageType` 等相关自定义选项，用于制造单位残骸，详细说明请见《额外功能说明》

### 2025.4.9  `Phobos-build-46+3_12` -> `Phobos-build-46+3_13`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.4.9 - `commit 235389b2f0d02aae1b5bd62f2ef1230f34cf83b0`*）
 >   - *`commit 7308937e...`* 新增 `Airburst.TargetAsSource` 等相关自定义选项，用于拓展空爆逻辑
 >   - *`commit a73a3a81...`* -> *`commit 81c9e20b...`* 代码优化和修正说明书的内容
 >   - *`commit daaffa33...`* 新增 `KillWeapon` 等相关自定义选项，用于在杀死单位时发射武器
 >   - *`commit 5506f6bf...`* 新增 `ProneSpeed` 等相关自定义选项，用于自定义匍匐速度比例
 >   - *`commit 5a37e23c...`* 修正说明书的格式
 >   - *`commit cd0746ae...`* 新增 `DamagedSpeed` 选项，用于自定义受损车辆速度比例
 >   - *`commit 8fbf6c65...`* 修正说明书的格式
 >   - *`commit 2548b55d...`* 新增 `ElectricAssaultLevel` 等相关自定义选项，用于拓展建筑充能逻辑
 >   - *`commit 9f09c20e...`* 修正说明书的内容和格式
 >   - *`commit eb534709...`* 新增 `KillWeapon.AffectsHouses` 等相关自定义选项
 >   - *`commit 7c4de991...`* 修复VXL抛射体图像色盘可能错误的问题
 >   - *`commit 66cfeee3...`* 现在 `Promote.VeteranAnimation` 等相关自定义选项支持微观定义
 >   - *`commit 50a3608a...`* -> *`commit bbba5e12...`* 修复 `IsLocomotor=yes` 抬起正在卸载的矿车后矿车不再能够正常使用的问题，修复原版微观定义的 `EnterBioReactorSound` 、`LeaveBioReactorSound` 和 `EnterGrinderSound` 未被使用的问题
 >   - *`commit ea638d66...`* 修正说明书的内容
 >   - *`commit 235389b2...`* 修复原版单位在斜坡上倾斜程度不正确的问题
 > - `( 3 )` 修复原版步兵连续进入载具的行为可能导致空气墙的问题
 > - `( 3 )` 修复原版子机导弹只会读取主武器 `Burst` 的问题
 > - `( 3 )` 修复 `Trajectory=Missile` 会提前一帧开始转向的问题
 > - `( 3 )` 修复 `Trajectory.TolerantTime` 失效的问题
 > - `( 3 )` 修复 `NoQueueUpToEnter` 会导致单位在桥上搭乘载具时留下空气墙并在之后会导致游戏卡死的问题
 > - `( 3 )` 修复 `NoQueueUpToEnter` 能让单位在桥下搭乘桥上载具的问题
 > - `( 3 )` 现在可以让车辆伪装成其它车辆
 > - `( 3 )` 现在 `MaximumBuildingPlacementFailures` 能够对战役生效
 > - `( 3 )` 现在 `AutoBuilding` 不再会让普通建筑放置在与集结点位置高差大于2的位置上
 > - `( 3 )` 现在 `ProneSpeed` 的全局值被拆分成了 `ProneSpeed.Crawls` 和 `ProneSpeed.NoCrawls`
 > - `( 3 )` 现在 `VehicleDamagedSpeedMultiplier.Yellow` 和 `VehicleDamagedSpeedMultiplier.Red` 选项，由公版 `DamagedSpeed` 实现
 > - `( 3 )` 新增 `Locomotor=Skilled` (4A582751-9839-11d1-B709-00A024DDAFD1) ，可以作为 `Locomotor=Drive` (4A582741-9839-11d1-B709-00A024DDAFD1) 的上位替代，详细说明请见《额外功能说明》
 >   - 新增 `Skilled.ReverseSpeed` 、`Skilled.FaceTargetRange` 和 `Skilled.RetreatDuration` 选项

### 2025.4.4  `Phobos-build-46+3_11` -> `Phobos-build-46+3_12`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.4.3 - `commit 3dabd5245d42847242e4fc496dbdbc429bf3d028`*）
 >   - *`commit 0b470318...`* 修复 `BuildingSell` 在建筑有集结点的情况下可能不能卖掉的问题
 >   - *`commit caa3f93d...`* 修复维修武器无法自动获取空中目标的问题
 >   - *`commit bcfdaf88...`* 修正说明书的内容和代码优化
 >   - *`commit 4915e7b7...`* 现在VXL抛射体图像能够使用 `AnimPalette` 和 `FirersPalette`
 >   - *`commit 4b640208...`* 修复除了火风暴墙外所有障碍物都不会影响到激光电弧辐射波绘制的问题
 >   - *`commit bc29b09a...`* -> *`commit f0509c84...`* 修复电弧导致的存档崩溃问题
 >   - *`commit de669f8e...`* 修正说明书的内容和代码优化
 >   - *`commit 3dabd524...`* 修复找驻军建筑、找战斗碉堡、找坦克碉堡、找生化反应堆、找回收站这5个脚本会找到位于单位不可抵达位置的建筑的问题
 > - `( 3 )` 修复首位乘客在等待搭乘载具时会放弃进入载具的问题
 > - `( 3 )` 修复非悬浮型载具不会移动到合适位置卸载乘客的问题
 > - `( 3 )` 修复单位变形的各种问题，且现在能够对建筑变形
 > - `( 3 )` 修复 `AmphibiousUnload` 不能为海军寻找合适的位置卸载的问题
 > - `( 3 )` 修复 `SubjectToBuildings` 不能对超高速弹道抛体拦截生效的问题
 > - `( 3 )` 重新取消维修武器索敌的距离硬编

### 2025.4.1  `Phobos-build-46+3_10` -> `Phobos-build-46+3_11`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.4.1 - `commit fc0251e7a656c12702d57e1639744727b6f1b91d`*）
 >   - *`commit 0be45d18...`* 修正说明书的内容或格式错误
 >   - *`commit 2c7209e0...`* 修复 `Tint.VisibleToHouses`
 >   - *`commit f5015599...`* 新增 `NoQueueUpToEnter` 和 `NoQueueUpToUnload` 选项
 >   - *`commit 1011ae53...`* 现在单位在高架桥上空时能停不下来，桥下单位也能停止除移动以外的任务
 >   - *`commit 0fdca8b7...`* 现在 `ExtendedAircraftMissions` 能让飞机在返回机场降落时提前降低高度
 >   - *`commit 9885f73a...`* 修复部分两栖移动方式的单位无法进入水上建筑的问题
 >   - *`commit 646bc1b5...`* 修复空中母舰在高架桥附近时不能发射子机的问题和非空中母舰在高架桥附近不会重新找合适的攻击位置的问题以及子机不能返回处在高架上的母舰并始终保持一定高度的问题
 >   - *`commit 96c6550b...`* 修复悬浮空军在高架桥上时影子位置仍然在地面上的问题
 >   - *`commit 838c2e7b...`* 现在激光电弧辐射波可以散射且能够拥有额外视觉偏移
 >   - *`commit fc0251e7...`* 优化 `AlphaImage` 的性能表现，对建筑极有帮助
 > - `( 3 )` 修复两栖单位不能在水上上车的问题
 > - `( 3 )` 修复 `Phobos-build-46+3_10` 的更新后飞机不能正常坠毁的问题
 > - `( 3 )` 修复 `Trajectory.ProximityImpact` 的最后一次有效计数在 `Trajectory.ProximityWarhead` 未设置时不能生效的问题
 > - `( 3 )` 修复 `NoQueueUpToEnter` 在载具主动向主要乘客移动后不会呼叫其它所有乘客搭乘的问题
 > - `( 3 )` 新增 `AmphibiousEnter` 和 `AmphibiousUnload` 选项，用于让载具能够在水面上装载卸载乘客
 > - `( 3 )` 新增 `RenderIfOutOfScreen` 选项，用于让动画在超出绘制范围时仍然绘制

### 2025.3.31  `Phobos-build-46+3_9` -> `Phobos-build-46+3_10`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.3.31 - `commit 3bf8ddde9a6a707b74f053a7a7bd805de283ac6f`*）
 >   - *`commit f16db814...`* 退回 *`commit 97705572`* 中在建筑变更所属方时更新激活动画下一帧的修改
 >   - *`commit 4d8938ec...`* 修复 `KeepRange` 的武器在载具内不能连续发射的问题
 >   - *`commit d4280454...`* -> *`commit 746711f6...`* 代码优化
 >   - *`commit b6c545a9...`* 新增 `Strafing.EndDelay` 选项并修复 `Strafing.Shots` 的冲突问题
 >   - *`commit 5c73877d...`* 新增 `Bolt.FollowFLH` 选项并修复电弧绘制的错误
 >   - *`commit 5c6d1079...`* -> *`commit a2c565a1...`* 代码优化
 >   - *`commit 09a8c17f...`* 改进攻击和移动脚本对隐形的检查
 >   - *`commit bcc954b1...`* 为攻击脚本目标评估添加缺失检查
 >   - *`commit 3e76acbc...`* -> *`commit 67c39d99...`* 代码优化和说明书修正
 >   - *`commit 60b1ff10...`* 新增 `Sinkable` 相关自定义选项，用于拓展沉船逻辑
 >   - *`commit 3bf8ddde...`* 修复传送出场的首个单位将导致所有相同单位在同方向上不举的问题
 > - `( 3 )` 修复空中母舰在高架桥附近时不能发射子机的问题和非空中母舰在高架桥附近不会重新找合适的攻击位置的问题
 > - `( 3 )` 修复子机不能返回处在高架上的母舰并始终保持一定高度的问题
 > - `( 3 )` 修复悬浮空军在高架桥上时影子位置仍然在地面上的问题（暂未修复图层问题）
 > - `( 3 )` 修复部分两栖移动方式的单位无法进入水上建筑的问题
 > - `( 3 )` 修复单位在高架桥上空时停不下来的问题
 > - `( 3 )` 修复 `FlightClimb` 导致飞机在高架桥上出现错误行为
 > - `( 3 )` 修复 `NoQueueUpToEnter` 对紧贴的单位无效的问题、有时会导致单位重叠的问题
 > - `( 3 )` 现在 `ExtendedAircraftMissions` 能让飞机在返回机场降落时提前降低高度
 > - `( 3 )` 新增 `NoCellSpread` 和 `NoCellSpread.SnapDistance` 选项，用于让弹头仅伤害目标本身
 > - `( 3 )` 新增 `Airstrike.TargetCell` 和 `Airstrike.SecondaryFirst` 选项，用于解决空袭武器的问题

### 2025.3.27  `Phobos-build-46+3_8` -> `Phobos-build-46+3_9`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.3.26 - `commit a37c074159f4228fe26dc026b23f936b66711660`*）
 >   - *`commit a37c0741...`* 新增 `BombParachute` 选项，用于自定义炸弹降落伞
 > - `( 3 )` 新增 `CameoPriority.Houses` 选项，用于图标排序
 > - `( 3 )` 修复 `NoQueueUpToEnter` 无视 `Passengers.BySize` 的问题
 > - `( 3 )` 新增 `UnifiedTechnoColor` 相关自定义选项，用于统一单位颜色

### 2025.3.25  `Phobos-build-46+3_7` -> `Phobos-build-46+3_8`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.3.24 - `commit 278e0a07fe29c532c1fae0cfaea25caf6acf3ee7`*）
 >   - *`commit 278e0a07...`* 修正说明书的内容或格式错误
 > - `( 3 )` 修复多功能步兵车总使用主副武器射程进行索敌的问题
 > - `( 3 )` 修复 `Trajectory=Missile` 在 `Trajectory.DetonationDistance` 与 `Trajectory.OffsetCoord` 或 `Inaccurate` 同时使用时不能正确引爆的问题
 > - `( 3 )` 修复 `Trajectory=Bombard` 在 `Trajectory.Bombard.NoLaunch` 与 `Trajectory.Bombard.FreeFallOnTarget` 同时使用时不能正确工作的问题，以及在下落阶段会提早一帧引爆的问题
 > - `( 3 )` 修复 `Trajectory.Synchronize` 启用后无法同步到有效目标时导致游戏崩溃的问题
 > - `( 3 )` 修复 `ExtendedBuildingPlacing` 在部分情况下对建筑外圈判断不正确的问题
 > - `( 3 )` 现在 `DistributionCommand` 会优先选择距离更接近中心的目标
 > - `( 3 )` 新增 `BombParachute` 选项，用于自定义炸弹降落伞
 > - `( 3 )` 新增 `UnifiedRadarColor` 相关自定义选项，用于统一雷达颜色
 > - `( 3 )` 暂时恢复维修武器索敌的距离硬编

### 2025.3.23  `Phobos-build-46+3_6` -> `Phobos-build-46+3_7`

 > - `( 3 )` 修复 `Turret.Restriction` 和 `Turret.BodyOrientation` 同时使用时可能导致炮塔卡住或越过限制方向等问题
 > - `( 3 )` 修复 `Trajectory=Tracing` 的抛体在 `Trajectory.Synchronize=no` 的情况下不会跟踪目标的问题
 > - `( 3 )` 现在 `MessageDisplayInCenter` 仅会对触发添加的字幕有效，并使其与其它信息相互独立

### 2025.3.22  `Phobos-build-46+3_5` -> `Phobos-build-46+3_6`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.3.22 - `commit b75c8f06ec7914f2701c113b014e518ab846d601`*）
 >   - *`commit 6206efd2...`* 现在 `NoManualMove` 能够对展开的建筑生效，使其不能被鼠标命令收起并移动
 >   - *`commit b75c8f06...`* 新增 `MindControl.ThreatDelay` 选项，可以延迟自动攻击被心控单位
 > - `( 3 )` 更改 `AttackMindControlledDelay` 键名为 `MindControl.ThreatDelay` ，使全局与微观定义键名保持一致
 > - `( 3 )` 修复 `Trajectory.Parabola.BounceTimes` 无法工作的问题
 > - `( 3 )` 修复 `Turret.Restriction` 在旋转炮塔时会落后身体一帧的问题，以及在移动中存在炮塔抽搐的问题
 > - `( 3 )` 现在 `PlayerReturnFire.Smarter` 只有开火者位于射程内且具有比当前目标更高优先级时才会进行还击，而且仅将开火者设为目标而不会跑过去打
 > - `( 3 )` 新增 `Turret.BodyFoundation` 选项，用于让单位在旋转身体时同时跟随旋转炮塔
 > - `( 3 )` 新增 `CanAttackMeThreatBonus` 选项，用于计算可以攻击索敌者的单位的额外仇恨
 > - `( 3 )` 新增 `ExtraTargeting.OnNoTargetAssigned` 选项，用于让单位在攻击的目标不是玩家手动指定的时仍执行周期性索敌
 > - `( 3 )` 新增 `DefaultVisualCharacter` 和 `IgnoreByMouse` 选项，用于作为子选项默认值

### 2025.3.20  `Phobos-build-46+3_4` -> `Phobos-build-46+3_5`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.3.20 - `commit 1d24ff8cf143f55241cc8e7957518667989cd212`*）
 >   - *`commit 1d24ff8c...`* 新增 `SubjectToGround` 选项，用于开火前检查地形
 > - `( 3 )` 修复找驻军建筑、找战斗碉堡、找坦克碉堡、找生化反应堆、找回收站这5个脚本会找到位于单位不可抵达位置的建筑的问题
 > - `( 3 )` 修复维修武器索敌距离强制为512，而且无法获取空中的目标的问题
 > - `( 3 )` 现在 `NoManualMove` 能够对展开的建筑生效，使其不能被鼠标命令收起并移动
 > - `( 3 )` 新增 `SinkableBySquid` 选项，用于控制被寄生摧毁的船只是否会沉没
 > - `( 3 )` 新增 `ExtraTargeting.OnLoseTarget` 和 `ExtraTargeting.OnStopCommand` 选项，用于额外索敌
 > - `( 3 )` 新增 `ProneSpeed` 选项，用于自定义匍匐速度比例
 > - `( 3 )` 新增 `VehicleDamagedSpeedMultiplier.Yellow` 和 `VehicleDamagedSpeedMultiplier.Red` 选项，用于自定义受损单位减速比例

### 2025.3.19  `Phobos-build-46+3_3` -> `Phobos-build-46+3_4`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.3.19 - `commit 6b245dfb9f09e2fe0be5c2a659c04dccc579395e`*）
 >   - *`commit 172251b5...`* 修复机场与飞机链接在部分情况下不能正确断开的问题
 >   - *`commit 97705572...`* 在建筑变更所属方时更新激活动画下一帧
 >   - *`commit 6b245dfb...`* 修复存读档后一些数据被错误重置的问题
 > - `( 3 )` 优化 `SubjectToGround` 的判断逻辑
 > - `( 3 )` 优化高速弹道抛体的性能，跳过了原本的重复判断
 > - `( 3 )` 现在 `Trajectory.DisperseFaceCheck` 能够正确被 `Trajectory.BulletFacing=Velocity` 覆盖
 > - `( 3 )` 现在 `Trajectory=Bombard` 的抛体也能使用负数的 `Trajectory.DetonationDistance`
 > - `( 3 )` 现在 `EMPulseCannon` 也会影响侵略模式开关的默认值
 > - `( 3 )` 现在 `AttackMove.UpdateTarget` 拥有自己独立的全局设置
 > - `( 3 )` 现在 `SubjectToWalls` 被高速弹道抛体使用时不再一定会被友军围墙阻挡，且增加了高度判断
 > - `( 3 )` 修复 `Trajectory.Straight.ConfineAtHeight` 在悬崖处有时会减速的问题，和其与 `Trajectory.PassDetonateLocal` 同时使用时抛体经过悬崖存在不正确行为的问题
 > - `( 3 )` 修复 `Trajectory=Engrave` 在悬崖处工作不正常的问题
 > - `( 3 )` 修复 `Trajectory=Engrave` 使用自定义 `Trajectory.Duration` 时不会计时的问题
 > - `( 3 )` 修复 `Trajectory=Bombard` 不能正常工作的问题
 > - `( 3 )` 修复 `CloningFacility` 和 `WeaponsFactory=no` 时不读取 `ClonedAs` 的问题
 > - `( 3 )` 修复鼠标悬浮在底部栏主图标上时显示的内容不会换行的问题
 > - `( 3 )` 新增 `UIDescription.HoveredInfo` 选项，用于定义悬浮在底部栏主图标上时显示的内容
 > - `( 3 )` 新增 `Trajectory.Engrave.AttachToTarget` 选项，用于让切割线的位置随目标位置变化
 > - `( 3 )` 新增 `Trajectory.Engrave.UpdateDirection` 选项，用于让切割线会随发射者和目标位置更新角度
 > - `( 3 )` 新增 `Trajectory.RetargetInterval` 选项，用于控制抛射体重新索敌的间隔
 > - `( 3 )` 新增 `AllyShareControl` 选项，用于共享控制
 > - `( 3 )` 移除 `JumpjetTilt` 全局选项，将只使用微观定义
 > - `( 3 )` 更换了超武侧栏的保存位置

### 2025.3.17  `Phobos-build-46+3_2` -> `Phobos-build-46+3_3`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.3.17 - `commit e28d9a00209e3428f76ca02ad63e567700c21b88`*）
 >   - *`commit 845c6c28...`* 修复部分导致野指针的错误
 >   - *`commit c38dab5b...`* 修复 `IsSonic` 在绘制时可能导致的崩溃问题
 >   - *`commit 023d8dac...`* 修正说明书的内容或格式错误
 >   - *`commit f1c16e04...`* 该死的数据结构改动
 >   - *`commit ce437fb1...`* 修正说明书的内容或格式错误
 >   - *`commit 755ce7e2...`* 新增 `Spawner.RecycleRange` 等相关自定义选项，用于拓展子机返回逻辑
 >   - *`commit 37ae64e8...`* 移除重复的代码内容
 >   - *`commit 6391e7de...`* 修正说明书的内容或格式错误，并修复医院在读档后不能正确踢出成员的问题，修复 `DestroyAnim` 的错误使用以及电弧失效时野指针的问题
 >   - *`commit fe61c638...`* 修正说明书的内容或格式错误
 >   - *`commit b4a2c614...`* 更改 `ExtendedAircraftMissions` 启用时飞机区域警戒的行为
 >   - *`commit 3cea153b...`* 新增 `KeepRange` 等相关自定义选项，用于单位自动保持距离
 >   - *`commit e28d9a00...`* 修复Phobos功能在计算建筑FLH时不正确的问题
 > - `( 3 )` 修正《额外功能说明》的部分内容错误
 > - `( 3 )` 修复底部栏在未选择单位时仍会挡住战术地图选择功能的问题
 > - `( 3 )` 修复维修子机返航工作不正确的问题
 > - `( 3 )` 新增 `IgnoreByMouse` 等相关自定义选项，用于设置鼠标会忽略的物件
 > - `( 3 )` 新增 `DefaultVisualCharacter` 等相关自定义选项，用于让单位在默认情况下具有透明度
 > - `( 3 )` 新增 `RadarInvisible.ToSelf` 和 `RadarInvisible.ToAlly` 选项，用于扩展雷达不可见功能
 > - `( 3 )` 弹道抛体修复了一些问题并新增了部分功能，详细说明请见《额外功能说明》，已更新《抛体案例参考》
 >   - 修复 `Trajectory.DisperseWeapons` 在 `Trajectory.Synchronize=yes` 时会无视 `Trajectory.DisperseForceFire=no` 攻击射程外目标的问题
 >   - 修复 `Trajectory=Straight` 、`Trajectory=Bombard` 、`Trajectory=Parabola` 和 `Trajectory=Missile` 的弹道抛体存在命中延迟的问题
 >   - 修复 `Trajectory.Missile.LeadTimeCalculate` 默认值错误的问题
 >   - 修复 `Trajectory.Missile.LaunchSpeed` 未正确设置的问题
 >   - 更名选项：正则表达式例 `(Trajectory)(\.).+\.(YYY=) -> $1$2$3`
 >     - 更改 `Trajectory.XXX.RotateCoord` 键名为 `Trajectory.RotateCoord`
 >     - 更改 `Trajectory.XXX.OffsetCoord` 键名为 `Trajectory.OffsetCoord`
 >     - 更改 `Trajectory.XXX.AxisOfRotation` 键名为 `Trajectory.AxisOfRotation`
 >     - 更改 `Trajectory.XXX.LeadTimeCalculate` 键名为 `Trajectory.LeadTimeCalculate`
 >     - 更改 `Trajectory.XXX.DetonationDistance` 键名为 `Trajectory.DetonationDistance`
 >     - 更改 `Trajectory.XXX.TargetSnapDistance` 键名为 `Trajectory.TargetSnapDistance`
 >     - 更改 `Trajectory.XXX.EarlyDetonation` 键名为 `Trajectory.EarlyDetonation`
 >     - 更改 `Trajectory.XXX.DetonationHeight` 键名为 `Trajectory.DetonationHeight`
 >     - 更改 `Trajectory.XXX.AllowFirerTurning` 键名为 `Trajectory.AllowFirerTurning`
 >   - 现在 `Trajectory.Duration` 新增了 `Trajectory=Tracing` 的负数用法，表示不限时间
 >   - 现在 `Trajectory=Straight` 、`Trajectory=Bombard` 和 `Trajectory=Parabola` 的弹道抛体使用 `Inaccurate=yes` 时采用原版算法（增加了武器射程的影响）
 >   - 现在 `Trajectory=Missile` 支持使用 `Inaccurate=yes` 和 `Trajectory.OffsetCoord`
 >   - 现在 `Trajectory.Tracing.SuicideAboveRange` 被功能更全面的 `Trajectory.Tracing.ChasableDistance` 替换
 >   - 现在 `Trajectory.BulletSpin` 、`Trajectory.BulletStable` 、`Trajectory.BulletOnPlane` 被功能更全面的 `Trajectory.BulletFacing` 替换
 >   - 新增 `Trajectory.CreateCapacity` 选项，用于控制发射者能够发射的弹道抛体的最大数量

### 2025.3.13  `Phobos-build-46+3_1` -> `Phobos-build-46+3_2`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.3.11 - `commit 38738a8f8e110f1e2e291fefc67ed0c867c2fa6e`*）
 >   - *`commit d5164c01...`* 修复 `DefaultDisguise` 为不同玩家显示错误的房屋颜色的问题
 >   - *`commit a55c5988...`* 新增 `Image.ConditionYellow` 等相关自定义选项，用于自定义受损单位图像
 >   - *`commit 0ad6e878...`* 修正说明书的内容或格式错误
 >   - *`commit ca89ad1c...`* 新增 `Spawns.Queue` 选项，用于子机生成逻辑增强
 >   - *`commit 8c84bd4a...`* 修正说明书的内容或格式错误
 >   - *`commit d6a8ef0b...`* -> *`commit df1c512d...`* 替换一个消耗较高的补丁实现
 >   - *`commit 3787b9b7...`* 修复计算威胁时未正确使用 `AAOnly` 的问题
 >   - *`commit 8351a58d...`* 修复隐形、上车等会导致子机、抛体等无法正确使单位获得经验的问题
 >   - *`commit 321877bf...`* 新增 `ForceWeapon.UnderEMP` 选项，用于定义攻击处在EMP状态下的目标时的武器
 >   - *`commit 31aac67b...`* 修正说明书的内容或格式错误，并修复分裂空爆抛体的阵营色盘丢失
 >   - *`commit e1e5a730...`* -> *`commit 38738a8f...`* 修正说明书的内容或格式错误
 > - `( 3 )` 弹道抛体进行了通用化修改，注意使用时要保证其武器正确被注册，详细说明请见《额外功能说明》，已更新《抛体案例参考》
 >   - 类型更名
 >     - 变更 `Trajectory=Disperse` 为 `Trajectory=Missile` ，且其下标签更名为 `Trajectory.Missile.YYY`
 >   - 新增选项
 >     - 新增 `Trajectory.BulletStable` 选项，用于禁止抛体图像转动
 >     - 新增 `Trajectory.BulletOnPlane` 选项，用于限制抛体只能在水平面上转动
 >     - 新增 `Trajectory.DisperseForceFire` 选项，用于控制抛体没有目标时能否扩散武器
 >   - 移除选项
 >     - 移除 `Trajectory.Disperse.WeaponToAllies` 选项，现在使用扩散武器的 `CanTargetHouses`
 >     - 移除 `Trajectory.Disperse.RetargetAllies` 选项，现在使用抛体武器的 `CanTargetHouses`
 >     - 移除 `Trajectory.XXX.UseDisperseBurst` 选项，现在必定使用
 >     - 移除 `Trajectory.XXX.SubjectToGround` 选项，现在根据类型和 `SubjectToGround` 共同决定
 >     - 移除 `Trajectory.Engrave.ConfineOnGround` 选项，现在根据原目标是否在空中自动决定
 >   - 更名选项
 >     - 更改 `Trajectory.Engrave.Duration` 和 `Trajectory.Tracing.TheDuration` 键名为 `Trajectory.Duration`
 >     - 更改 `Trajectory.Tracing.ROT` 键名为 `Trajectory.BulletROT`
 >     - 更改 `Trajectory.Straight.EdgeAttenuation` 键名为 `Trajectory.DamageEdgeAttenuation`
 >     - 更改 `Trajectory.Straight.CountAttenuation` 键名为 `Trajectory.DamageCountAttenuation`
 >     - 更改 `Trajectory.Disperse.ROT` 键名为 `Trajectory.Missile.TurningSpeed`
 >   - 通用化修改1：正则表达式例 `(Trajectory)(\.).+\.YYY= -> $1$2DisperseZZZ=`
 >     - 更改 `Trajectory.XXX.SuicideIfNoWeapon` 键名为 `Trajectory.DisperseSuicide`
 >     - 更改 `Trajectory.XXX.WeaponCheck` 键名为 `Trajectory.DisperseFaceCheck`
 >     - 更改 `Trajectory.XXX.Weapons` 键名为 `Trajectory.DisperseWeapons`
 >     - 更改 `Trajectory.XXX.WeaponBurst` 键名为 `Trajectory.DisperseBursts`
 >     - 更改 `Trajectory.XXX.WeaponCount` 键名为 `Trajectory.DisperseCounts`
 >     - 更改 `Trajectory.XXX.WeaponDelay` 键名为 `Trajectory.DisperseDelays`
 >   - 通用化修改2：正则表达式例 `(Trajectory)(\.).+\.(YYY=) -> $1$2$3`
 >     - 更改 `Trajectory.XXX.TolerantTime` 键名为 `Trajectory.TolerantTime`
 >     - 更改 `Trajectory.XXX.BulletSpin` 键名为 `Trajectory.BulletSpin`
 >     - 更改 `Trajectory.XXX.MirrorCoord` 键名为 `Trajectory.MirrorCoord`
 >     - 更改 `Trajectory.XXX.RetargetRadius` 键名为 `Trajectory.RetargetRadius`
 >     - 更改 `Trajectory.XXX.Synchronize` 键名为 `Trajectory.Synchronize`
 >     - 更改 `Trajectory.XXX.PeacefulVanish` 键名为 `Trajectory.PeacefulVanish`
 >     - 更改 `Trajectory.XXX.ApplyRangeModifiers` 键名为 `Trajectory.ApplyRangeModifiers`
 >     - 更改 `Trajectory.XXX.UseDisperseCoord` 键名为 `Trajectory.UseDisperseCoord`
 >     - 更改 `Trajectory.XXX.RecordSourceCoord` 键名为 `Trajectory.RecordSourceCoord`
 >     - 更改 `Trajectory.XXX.PassDetonate` 键名为 `Trajectory.PassDetonate`
 >     - 更改 `Trajectory.XXX.PassDetonateWarhead` 键名为 `Trajectory.PassDetonateWarhead`
 >     - 更改 `Trajectory.XXX.PassDetonateDamage` 键名为 `Trajectory.PassDetonateDamage`
 >     - 更改 `Trajectory.XXX.PassDetonateDelay` 键名为 `Trajectory.PassDetonateDelay`
 >     - 更改 `Trajectory.XXX.PassDetonateInitialDelay` 键名为 `Trajectory.PassDetonateInitialDelay`
 >     - 更改 `Trajectory.XXX.PassDetonateLocal` 键名为 `Trajectory.PassDetonateLocal`
 >     - 更改 `Trajectory.XXX.ProximityImpact` 键名为 `Trajectory.ProximityImpact`
 >     - 更改 `Trajectory.XXX.ProximityWarhead` 键名为 `Trajectory.ProximityWarhead`
 >     - 更改 `Trajectory.XXX.ProximityDamage` 键名为 `Trajectory.ProximityDamage`
 >     - 更改 `Trajectory.XXX.ProximityRadius` 键名为 `Trajectory.ProximityRadius`
 >     - 更改 `Trajectory.XXX.ProximityDirect` 键名为 `Trajectory.ProximityDirect`
 >     - 更改 `Trajectory.XXX.ProximityMedial` 键名为 `Trajectory.ProximityMedial`
 >     - 更改 `Trajectory.XXX.ProximityAllies` 键名为 `Trajectory.ProximityAllies`
 >     - 更改 `Trajectory.XXX.ProximityFlight` 键名为 `Trajectory.ProximityFlight`
 >     - 更改 `Trajectory.XXX.ThroughVehicles` 键名为 `Trajectory.ThroughVehicles`
 >     - 更改 `Trajectory.XXX.ThroughBuilding` 键名为 `Trajectory.ThroughBuilding`
 >   - 通用化修改3：正则表达式例 `(Trajectory)(\.).+\.Weapon(YYY=) -> $1$2Disperse$3`
 >     - 更改 `Trajectory.XXX.WeaponCycle` 键名为 `Trajectory.DisperseCycle`
 >     - 更改 `Trajectory.XXX.WeaponInitialDelay` 键名为 `Trajectory.DisperseInitialDelay`
 >     - 更改 `Trajectory.XXX.WeaponEffectiveRange` 键名为 `Trajectory.DisperseEffectiveRange`
 >     - 更改 `Trajectory.XXX.WeaponSeparate` 键名为 `Trajectory.DisperseSeparate`
 >     - 更改 `Trajectory.XXX.WeaponRetarget` 键名为 `Trajectory.DisperseRetarget`
 >     - 更改 `Trajectory.XXX.WeaponLocation` 键名为 `Trajectory.DisperseLocation`
 >     - 更改 `Trajectory.XXX.WeaponTendency` 键名为 `Trajectory.DisperseTendency`
 >     - 更改 `Trajectory.XXX.WeaponHolistic` 键名为 `Trajectory.DisperseHolistic`
 >     - 更改 `Trajectory.XXX.WeaponMarginal` 键名为 `Trajectory.DisperseMarginal`
 >     - 更改 `Trajectory.XXX.WeaponDoRepeat` 键名为 `Trajectory.DisperseDoRepeat`
 >     - 更改 `Trajectory.XXX.WeaponFromFirer` 键名为 `Trajectory.DisperseFromFirer`
 >     - 更改 `Trajectory.XXX.WeaponCoord` 键名为 `Trajectory.DisperseCoord`
 >   - 变动提示
 >     - 现在 `Trajectory.DisperseWeapons` 需要设置 `Trajectory.DisperseCycle` 才能发射武器
 >     - 现在 `Trajectory=Tracing` 在更改后需要设置 `Trajectory.DisperseBursts` 才会像原本一样发射武器
 >     - 现在 `Trajectory=Engrave` 在更改后需要设置 `Trajectory.PassDetonate=yes` 才会像原本一样工作
 >     - 现在 `Trajectory.Engrave.DamageDelay` 已被移除，现在使用 `Trajectory.PassDetonateDelay` 能达到相同效果
 >     - 现在 `Trajectory.PeacefulVanish` 的默认值是随 `Trajectory.ProximityImpact` 和 `Trajectory.DisperseCycle` 变化的
 > - `( 3 )` 修复单位初始需要武器冷却问题未能完全解决的问题
 > - `( 3 )` 更改 `ExtendedAircraftMissions` 启用时飞机区域警戒的行为
 > - `( 3 )` 新增 `RemoveMindControl.OnVictim` 和 `RemoveMindControl.OnController` 选项，用于控制作用者
 > - `( 3 )` 修复 `CloningFacility` 和 `WeaponsFactory=no` 的不正确交互
 > - `( 3 )` 现在使用快捷键切换侵略模式时，也会影响所有选中对象的载员和驻军

### 2025.3.9  `Phobos-build-46+3_0` -> `Phobos-build-46+3_1`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.3.9 - `commit d58cfe4f1d3835069b868f85548020a066bec07e`*）
 >   - *`commit d62bf34b...`* 修正说明书的内容或格式错误
 >   - *`commit 47347e08...`* 修复变形转换与超时空武器间的错误交互
 >   - *`commit c83c87a9...`* 修复两栖矿车无法自动回到水面上矿场的问题
 >   - *`commit c78c2aad...`* 新增 `Crit.ExtraDamage.ApplyFirepowerMult` 选项
 >   - *`commit 8fc7b682...`* 修正说明书的内容或格式错误
 >   - *`commit 008b83ba...`* 修复重工内单位可能卡住的问题
 >   - *`commit b8d2f954...`* 修正说明书的内容错误
 >   - *`commit ef2d9835...`* 退回在对伪装的检查中判断遥控坦克是否失灵的问题
 >   - *`commit 91e3641b...`* -> *`commit 4e4a1325...`* 代码优化
 >   - *`commit 2c4948dd...`* 修复钻地移动方式自定义速度的错误实现方式
 >   - *`commit a2da2a81...`* 更新 YRpp
 >   - *`commit 93d77571...`* 修复部分情况下 `AltNextScenario` 未生效的问题
 >   - *`commit c25b0cd2...`* 修复部分情况下动画所有者可能不正确的问题
 >   - *`commit f0802473...`* 修正说明书的内容或格式错误
 >   - *`commit b0b01c70...`* 修复电弧频繁生成后销毁时可能导致的崩溃和悬浮飞行器坠毁时绘制阴影可能导致的崩溃
 >   - *`commit 5736568d...`* -> *`commit c5970a7a...`* 修正说明书的内容或格式错误
 >   - *`commit 6ed007ba...`* 修复 `DisguiseBlinkingVisibility` 中的疏忽
 >   - *`commit d58cfe4f...`* 继续修复 `DisguiseBlinkingVisibility` 中的疏忽
 > - `( 3 )` 修复 `Phobos-build-45+3_20` 的更新中导致单位初始需要武器冷却的问题
 > - `( 3 )` 修复 `JustHasRallyPoint` 会导致建造场无法在不按住 `Alt` 的情况下移动的问题
 > - `( 3 )` 新增 `NoManualEject` 选项，可以禁止手动弹出载员，但允许使用其它部署行为
 > - `( 3 )` 新增 `Refinery.UseNormalActiveAnim` 选项，可以让矿场使用常规的工作动画
 > - `( 3 )` 现在设置集结点的快捷键能够本地化了
 > - `( 3 )` 现在 `Trajectory.Disperse.SuicideAboveRange` 新增了负数用法，且默认值变更为 `-3`
 > - `( 3 )` 现在 `Trajectory.Disperse.SuicideShortOfROT` 的默认值变更为 `false`

### 2025.3.6  `Phobos-build-45+3_23` -> `Phobos-build-46+3_0`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.3.6 - `commit fea4b155d55b26537d89df9cef3491a82f406be7`*）
 >   - *`commit 5994d6eb...`* 修复 `FreeUnit` 、`StartUnit` 、和建筑取消部署时的几个寻路问题
 >   - *`commit 6e44b4a3...`* 修正说明书的内容或格式错误
 >   - *`commit 200ac0f9...`* 修复 `IsAnimDelayedBurst` 和 `ZShapePointMove.OnBuildup` 的读取问题，现在如果单位的图像不同名也可以正确使用了
 >   - *`commit f107c2ea...`* 版本号更新（[build-45] -> [build-46]）
 >   - *`commit fea4b155...`* 修正说明书的内容位置错误
 > - `( 3 )` 修复 `AutoBuilding` 在判断建造时错误判断放置建筑的 `BaseNormal` 的问题
 > - `( 3 )` 修复 `ExtraTurretCount` 等选项的读取问题，现在如果单位的图像不同名也可以正确使用了
 > - `( 3 )` 新增 `InTransportInfantryAmmoFix` 功能，使用不同技术路线修复载具中步兵装弹问题
 > - `( 3 )` 新增 `UpdateInLimbo` 等相关自定义选项，可以让单位保持更新，详细说明请见《额外功能说明》
 > - `( 3 )` 恢复 `SelectedDisplay.Enable` 读取 `GroupAs` 判断是否为同一类型的机制，且若现在读取不到素材则无法启用

### 2025.3.5  `Phobos-build-45+3_22` -> `Phobos-build-45+3_23`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.3.4 - `commit d66d818e7081e2226f2f784bbbf3daff9ddf09ce`*）
 >   - *`commit 8010aea3...`* 更新并修复说明书的内容
 >   - *`commit d66d818e...`* 修复处理附着粒子系统指针失效时的疏忽
 > - `( 3 )` 恢复 `AutoBuilding.Gap` 选项，并重写了自动建筑的算法
 > - `( 3 )` 新增 `ExtraTurretCount` 等选项，用于额外绘制多个炮塔

### 2025.3.3  `Phobos-build-45+3_21` -> `Phobos-build-45+3_22`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.3.3 - `commit ed339c0727ae31e28dd2b586c443124eba1af698`*）
 >   - *`commit 6e1ed17b...` -> `commit 799ea22f...`* 修正说明书的内容或格式错误
 >   - *`commit 47837877...`* 代码优化并增加一些注释
 >   - *`commit 3be57b76...`* 新增 `BuildingSell` 等相关自定义选项，用于弹头出售和收起建筑
 >   - *`commit 2a623a6d...`* 新增 `Interceptor.ApplyFirepowerMult` 选项，用于控制反抛体武器是否计算单位火力加成
 >   - *`commit 051dbcff...`* 现在分裂空爆武器支持视觉效果绘制
 >   - *`commit 482a74be...`* 新增 `NoRearm.UnderEMP` 等相关自定义选项，受到控制停止武器冷却
 >   - *`commit 661a4f68...`* 新增 `RecountBurst` 选项，用于重置Burst计数
 >   - *`commit ed339c07...`* 新增 `InheritArmor.Allowed` 和 `InheritArmor.Disallowed` 选项，用于自定义护盾护甲能否继承某些类型
 > - `(1/3)` 修复 `Trajectory.Disperse.LockDirection` 在 `Phobos-build-45+3_18` 的改动后失效的问题
 > - `(1/3)` 修复激光、电弧、辐射波绘制时不支持抛射体散射（包括 `FlakScatter=yes` 和 `Inaccurate=yes`）的问题
 > - `(1/3)` 现在 `Trajectory.Disperse.CruiseEnable` 能够在使用 `Trajectory.Disperse.RetargetRadius` 时会先再次尝试回到巡航阶段
 > - `(1/3)` 拓展底边栏选择信息的可本地化内容
 > - `(1/3)` 优化超武侧栏添加图标的行为
 > - `(1/3)` 优化 `Trajectory.Disperse.WeaponRetarget` 的索敌选择行为
 > - `(1/3)` 新增 `VisualScatter` 等选项，用于激光、电弧、辐射波绘制的额外散射，详细说明请见《额外功能说明》
 > - `( 3 )` 新增建筑集结点和第二集结点等功能，用于自动建造建筑，同时暂时停用 `AutoBuilding.Gap` ，详细说明请见《额外功能说明》

### 2025.2.28  `Phobos-build-45+3_20` -> `Phobos-build-45+3_21` (beta: `Phobos-build-45+1_8`)

 > - `(1/3)` 退回超武侧栏在 `Phobos-build-45+3_20` 的刷新机制修改，避免出现问题

### 2025.2.28  `Phobos-build-45+3_19` -> `Phobos-build-45+3_20` (beta: `Phobos-build-45+1_7`)

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.2.28 - `commit da2f6f1de1c18ba75bb7a3dcbc147329ddaa5e6d`*）
 >   - *`commit bdc39bcf...`* 添加项目维护结构和贡献类型
 >   - *`commit 3d0c7642...`* 现在 `Deployer=yes` 的步兵在转换时会更新序列
 >   - *`commit 10d94133...`* 现在可以通过 `-ExceptionHandler=` 命令行参数控制异常处理程序
 >   - *`commit dbe06234...`* 修复新脚本会选择到隐形建筑的问题
 >   - *`commit 47d909d0...` -> `commit 9768b1c7...`* 修正说明书的内容或格式错误
 >   - *`commit 72dfb77f...`* 修复 `Insignia.Weapon` 的读取问题
 >   - *`commit c93235bd...`* 新增 `NoTurret.TrackTarget` 选项，用于无炮塔车辆武器冷却时转向
 >   - *`commit 00752e41...`* 新增 `DamageOwnerMultiplier` 等相关自定义选项，用于弹头伤害的所属方额外补正
 >   - *`commit 13cdc0cc...`* 代码优化
 >   - *`commit 7667db72...`* 优化粒子系统附加动画失效回收时的性能
 >   - *`commit 67477b74...`* 优化单位附加动画失效回收时的性能
 >   - *`commit 8edd4d3d...`* 修正说明书的内容或格式错误
 >   - *`commit 89250580...`* 修复地面载具被磁电坦克逻辑抬起后会转向抬起者的问题
 >   - *`commit 99f9805f...`* 修正说明书的内容或格式错误
 >   - *`commit 962a8a4e...`* 优化矿车计数器的性能
 >   - *`commit 2ff45a0a...`* 新增 `DisableRadDamageOnBuildings` 选项，用于取消辐射对建筑伤害
 >   - *`commit 622f690a...`* 修复之前实施的优化中的问题
 >   - *`commit 20efc644...`* 新增 `Bolt.Duration` 选项，用于自定义电弧效果的持续时间
 >   - *`commit af5f6b69...`* 代码优化
 >   - *`commit 36aadec4...`* 修正说明书的内容或格式错误
 >   - *`commit 8093e866...`* 修复单位死亡后可能留下一些反隐范围的问题
 >   - *`commit 78fea5be...`* 修正说明书的内容
 >   - *`commit 79db0d72...`* 适应最新改装指南的条款
 >   - *`commit da2f6f1d...`* 添加说明书的内容
 > - `(1/3)` 更换了独特单位独立显示的底层，且现在不影响友军的弹头不再会使图标闪红
 > - `(1/3)` 更换了底边栏选择信息的底层，并增加了一些新功能，详细说明请见《额外功能说明》，模板素材请见《底部选择栏模板》
 >   - 新增 `SelectedInfo.Main` 等几个自定义选项，用于定义背景素材
 >   - 新增 `SelectedInfo.UpperDivisor` 和 `SelectedInfo.BelowDivisor` 选项，用于降低实际显示的值
 >   - 新增 `SelectedInfo.CameoType` 选项，用于定义需要额外显示的值
 >   - 新增 `SelectedDisplay.Expand` 选项，用于控制默认情况下是否显示所有单位的图标
 >   - 新增 `SelectedDisplay.MaxCameo` 选项，用于控制底边栏最大图标数
 >   - 现在 `SelectedDisplay.Enable` 的快捷键移动到了 [界面] 面板
 >   - 移除 `FPSCounter.Enable` 功能
 > - `(1/3)` 修复超武侧栏在超过显示上限后排序混乱的问题，且现在能够正确播放选择目标的声音，同时修复了部分与左右键点击相关的问题
 > - `(1/3)` 修复 `ExtendedBuildingPlacing` 会导致存在部署动画的单位被卡住的问题
 > - `(1/3)` 修复 `ExtendedScatterAction` 会导致 `DefaultToGuardArea=yes` 的单位反复横跳的问题
 > - `( 3 )` 修复 `CanManualReload` 事件会同样响应切换侵略模式的问题
 > - `( 3 )` 新增 `Spawner.ReturnOnRepairDone` 选项，用于让子机在维修的单位修理完毕后能自动重置目标
 > - `( 3 )` 新增 `Spawner.RecycleFLH` 和 `Spawner.RecycleOnTurret` 选项，用于控制子机的回收位置
 > - `(1/3)` 新增 `AutomaticPlacingBuilding` 选项，用于控制自动建造快捷键的默认值
 > - `(1/3)` 新增 `ValueAsTimer` 选项，用于控制数显是否以计时器形式输出
 > - `(1/3)` 新增 `Trajectory.Tracing.WeaponFromFirer` 选项，用于 `Trajectory.Tracing.TraceTheTarget` 解耦并控制武器发射位置
 > - `(1/3)` 更改 `Trajectory.Tracing.PeacefullyVanish` 键名为 `Trajectory.Tracing.PeacefulVanish`
 > - `(1/3)` 新增 `Trajectory.Engrave.ProximityFlight` 选项，用于控制抛体是否能够获取空军目标
 > - `(1/3)` 新增 `Trajectory.Engrave.ConfineOnGround` 选项，用于控制抛体是否始终吸附地面

### 2025.2.20  `Phobos-build-45+3_18` -> `Phobos-build-45+3_19`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.2.20 - `commit 039cb7627c081681304fb7d30cf3034dd912767a`*）
 >   - *`commit d4bf67c6...`* 修复超时空传送单位到其不可碾压的单位身上并自爆时，会在原位置留下空气墙的问题
 >   - *`commit e72dc3c2...`* 修复 `Ammo.DeployUnlockMinimumAmount` 、`Ammo.DeployUnlockMaximumAmount` 和 `Ammo.AddOnDeploy` 对坦克碉堡内单位部分不正确行为
 >   - *`commit 039cb762...`* 现在 `BuildingWaypoints` 暂时仅在建筑使用路径点攻击时才会生效
 > - `(1/3)` 修复 `KeepTargetOnMove` 在单位进行一次移动后，若目标远离则既不会追击也不会重置目标的问题
 > - `(1/3)` 修复 `Trajectory.Disperse.WeaponRetarget` 在主目标为空军且武器能够攻击地面格子时会选择到地面单位的问题，并修复其有时能够选取到过长距离外的单位的问题
 > - `( 3 )` 修复原版用子机对目标进行维修的单位在修理完毕后不会自动重设维修目标而一直盘旋在目标周围的问题
 > - `(1/3)` 现在目标平均分配模式可以在按住开关快捷键时通过鼠标滚轮来调整范围，并拥有了几个新标签和功能，详细说明请见《额外功能说明》
 > - `( 3 )` 新增 `BuildingSell.IgnoreUnsellable` 选项，用于控制卖建筑时是否无视其它所有可能导致无法售卖的条件

### 2025.2.18  `Phobos-build-45+3_17` -> `Phobos-build-45+3_18`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.2.18 - `commit e14ffa831948c6ba8ca3ef21725fe0ad91b6994f`*）
 >   - *`commit e4a0f5d1...` -> `commit e14ffa83...`* 修正说明书的内容或格式错误
 > - `(1/3)` 优化 `Trajectory=Disperse` 功能，其对应的 `Trajectory.Speed` 不再限制上限，同时简化并重写其转向计算方式，现在在大半部分情况下 `Trajectory.Disperse.ROT` 的单位约相当于原本的两倍至三倍，且现在能够进行瞬间回转，现在的单位为每帧最大的旋转角度，默认值从 `30` 降低到 `10`
 > - `(1/3)` 现在 `Trajectory=Engrave` 和 `Trajectory=Tracing` 的发射者在载具中时会跟随载具的状态，且不再允许在更换最外载具时持续开火
 > - `(1/3)` 现在 `Trajectory=Engrave` 在使用 `Trajectory.Engrave.UseDisperseCoord` 时能够继承当时的 `Burst` 计数
 > - `(1/3)` 现在 `Trajectory=Tracing` 不再会跟随不符合自身 `AA` 或 `AG` 要求的目标，会视为无目标
 > - `(1/3)` 现在 `ScrollSidebarStripWhenHoldKey` 被拆分成了三个独立选项，详细说明请见《额外功能说明》
 > - `(1/3)` 修复 `Trajectory.Tracing.AllowFirerTurning` 错误读取 `Trajectory.Engrave.AllowFirerTurning` 的问题
 > - `(1/3)` 修复 `MergeBuildingDamage` 的一个潜在风险
 > - `( 3 )` 修复 `HarvesterScanAfterUnload` 微观定义默认值错误的问题

### 2025.2.16  `Phobos-build-45+3_16` -> `Phobos-build-45+3_17`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.2.15 - `commit dbf7f74eecc375765ca21ba3120dda24080ca3f9`*）
 >   - *`commit 02da3f9b...`* 更改翻译官方中文文档
 >   - *`commit dbf7f74e...`* 修复仅使用 `BarracksExitCell` 时飞行步兵会打断步兵工厂的问题
 > - `(1/3)` 优化 `ExtendedBuildingPlacing` 功能，且现在能够分别让科技建筑和防御建筑等待放置，并在图标变灰时取消等待
 > - `(1/3)` 更改 `UnitWithoutTurretAlwaysTurnToTarget` 键名为 `NoTurret.EarlyTurnToTarget` ，并增加了微观定义
 > - `( 3 )` 合并 [Improve subterranean harvester pathfinding #1535] by Starkku，用于修复钻地移动方式矿车的移动问题
 > - `(1/3)` 修复 `HarvesterQuickUnloader` 启用时钻地移动方式矿车的移动问题
 > - `(1/3)` 修复原版两栖矿车不会返回水上矿场的问题
 > - `( 3 )` 现在 `HarvesterScanAfterUnload` 增加了微观定义

### 2025.2.14  `Phobos-build-45+3_15` -> `Phobos-build-45+3_16`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.2.13 - `commit 33bb7c4fadbb2e7388691b2017d2c2ffcd2a4e59`*）
 >   - *`commit 29d25604...`* 新增 `PassengerDeletion.UnderEMP` 选项，能让单位在处在EMP下时仍能删除乘客，并将遥控坦克失灵视为处在EMP下
 >   - *`commit 5b2b5e04...`* 现在 `AuxBuilding` 、`SW.AuxBuildings` 和 `SW.NegBuildings` 能够计入插件建筑
 >   - *`commit 5d80ad7c...`* 新增 `Trajectory=Parabola` 的弹道抛体
 >   - *`commit 245b08aa...`* 新增 `BuildingTypeSelectable` 选项，可以让建筑像其他单位一样使用T键全选
 >   - *`commit fa7fc057...`* 新增 `CombatAlert` 等相关选项
 >   - *`commit 521605ce...`* 更新 `Trajectory=Bombard` 的弹道抛体，拥有了大量新功能
 >   - *`commit f5f3c9ad...`* 新增 `BuildingWaypoint` 选项，可以让建筑使用路径点
 >   - *`commit e0a19ab9...`* 合并寄生虫返回优化，如果返回位置被其它单位占据，它会出现在该位置周围
 >   - *`commit ee7462df...`* 新增 `CombatAlert.Default`  选项，作为单位受击事件的全局默认值
 >   - *`commit f0fb4de8...`* 替换轰击抛体中显式声明的变量
 >   - *`commit 0ee80905...`* 修复寄生逻辑的单位返回时有可能飞到地图坐标0,0处的问题
 >   - *`commit bd177ab8...`* 更改 `BuildingWaypoint` 键名为 `BuildingWaypoints` 并同步修改说明书
 >   - *`commit f701344f...`* 优化直线抛体的一个功能，修复轰击抛体的一个功能
 >   - *`commit bd73c2e3...`* 新增 `BunkerableAnyWay` 功能，可拓展进入坦克碉堡的单位，同时禁止会导致游戏崩溃的单位进入
 >   - *`commit a1e26ba6...`* 修正说明书的内容错误
 >   - *`commit c317e0cd...`* 修复轰击抛体的两个问题，并简化代码
 >   - *`commit 33bb7c4f...`* 添加社区中文版说明书链接
 > - `(1/3)` 更换了建筑建造范围拓展的底层，现在支持所有单位类型拓展建筑范围
 >   - 更改 `CheckUnitBaseNormal` 键名为 `CheckExtraBaseNormal`
 >   - 更改 `UnitBaseNormal` 键名为 `ExtraBaseNormal`
 >   - 更改 `UnitBaseForAllyBuilding` 键名为 `ExtraBaseForAllyBuilding`
 >   - 更改 `Adjacent.AllowedUnit` 键名为 `Adjacent.AllowedExtra`
 >   - 更改 `Adjacent.DisallowedUnit` 键名为 `Adjacent.DisallowedExtra`
 > - `(1/3)` 修复公版情况下地面载具被磁电坦克逻辑抬起后会神秘转向抬起者的问题
 > - `(1/3)` 修复 `ExtendedBuildingPlacing` 启用时，会永久卡住疏散地点上正在部署的单位的问题，以及单位有小概率不会移动的问题
 > - `(1/3)` 修复 `DeploysInto` 的单位在使用107号触发传送出场或使用超时空移动方式进行移动等极端情况下时导致的RE
 > - `(1/3)` 修复超时空传送单位到其不可碾压的单位身上并自爆时，会在原位置留下空气墙的问题
 > - `(1/3)` 修复一部分在非常极端的情况下时，某些物体可能出现在地图 0,0 位置的问题
 > - `(1/3)` 更改 `Trajectory.Engrave.TheDuration` 键名为 `Trajectory.Engrave.Duration`
 > - `(1/3)` 更改 `Trajectory.Engrave.IsSupported` 键名为 `Trajectory.Engrave.IsIntense`
 > - `(1/3)` 更改 `RateDown.Cover` 键名为 `RateDown.Cover.Value`
 > - `(1/3)` 更改 `RateDown.Ammo` 键名为 `RateDown.Cover.AmmoBelow`
 > - `(1/3)` 新增 `JumpjetTilt` 等几个自定义选项，可以让悬浮空军移动时前倾，转向时侧倾
 > - `(1/3)` 新增 `ScrollSidebarStripInTactical` 和 `ScrollSidebarStripWhenHoldKey` 选项，详细说明请见《额外功能说明》
 > - `(1/3)` 新增 `Trajectory.Engrave.AllowFirerTurning` 和 `Trajectory.Tracing.AllowFirerTurning` 选项，允许发射者朝向变动
 > - `(1/3)` 新增 `AllowDistributionCommand` 选项，用于启用整个功能并添加其快捷键
 > - `( 3 )` 新增 `InvisoLatencyFix` 选项，用于修复 `Inviso=yes` 的抛体的延迟问题
 > - `( 3 )` 现在 `BuildingWaypoints` 暂时仅在建筑使用路径点攻击时才会生效
 > - `( 3 )` 现在 `AttackMove.StopWhenTargetAcquired` 不会再使单位有概率在射程外停下

### 2025.2.9  `Phobos-build-45+3_14` -> `Phobos-build-45+3_15`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.2.9 - `commit 54a4b357eaf50bc2d1b7f6980b1f77b8a6777c1e`*）
 >   - *`commit a7ca25f2...`* 简化部分直线抛体的代码并更新说明书内容
 >   - *`commit 54a4b357...`* 简化直线抛体的向量计算并修正说明书的内容错误
 > - `(1/3)` 新增 `Trajectory.Engrave.ProximityImpact` 等几个自定义选项，详细说明请见《额外功能说明》
 > - `(1/3)` 修复 `Cameo.OverlayFrames` 未填写时使用了 `ShowBuildingStatistics` 导致的弹窗

### 2025.2.8  `Phobos-build-45+3_13` -> `Phobos-build-45+3_14`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.2.7 - `commit 033dbf096bb89d427f6aeb1adbd2f85d212e8747`*）
 >   - *`commit d44622f2...`* 暂时降低文档旧式搜索的依赖关系
 >   - *`commit 805da819...`* 新增 `IsAnimDelayedBurst` 选项，可用于让建筑的多段攻击无视蓄力动画
 >   - *`commit b9569c49...`* 修正说明书的格式错误
 >   - *`commit 57a1c585...`* 现在 `BuildingProductionQueue` 会在等待建筑放置时将其余建筑变灰，同时简化移除图标时事件的处理
 >   - *`commit 34fb1d6e...`* 修复 `Shield.SelfHealing.RestartInCombatDelay` 未被正确读取的问题
 >   - *`commit 6355937f...`* 新增 `AllowWeaponSelectAgainstWalls` 选项，能让 `NoSecondaryWeaponFallback` 取消选择对墙壁的武器
 >   - *`commit 3a2a4441...`* 修正说明书的内容错误
 >   - *`commit e5831b89...`* 修正说明书的格式错误
 >   - *`commit 2f5a3e40...`* 修正说明书的格式错误
 >   - *`commit 66595181...`* 更新中文文档以对应当前版本
 >   - *`commit ebb624c0...`* 部分还原 *`commit 57a1c585...`* 的对移除图标时事件的修改，并保留原代码
 >   - *`commit 88ea0fbb...`* 修正中文翻译，并更改QQ群ID为纯文本
 >   - *`commit 033dbf09...`* 为直线抛体增加代码注释，并简化部分代码
 > - `( 3 )` 更改 `AnimDontDelayBurst` 键名为 `IsAnimDelayedBurst` ，且其默认值由 `no` 更改为 `yes` ，该功能现在已被公版合并
 > - `(1/3)` 现在 `Cameo.OverlayShapes` 增加了一个参数，用于定义建筑可以被自动建造时覆盖的图形，详细说明请见《额外功能说明》
 > - `(1/3)` 修复 `MergeBuildingDamage` 在部分情况下会对超出范围的建筑造成伤害的问题，并修复在合并后不会保留最低伤害的问题
 > - `(1/3)` 修复 `NoQueueUpToEnter` 可能导致 `Locomotor=Hover` 的单位会在离开载具时无视一切障碍和命令回到原位置的问题
 > - `(1/3)` 修复 `Trajectory.Engrave.SourceCoord=0,0` 时说明与实际不相符的问题
 > - `(1/3)` 修复目标平均分配模式会选择到隐形、伪装、地图外或黑幕中单位的问题，即使无法真正选择其中一些单位为目标
 > - `( 3 )` 更新同帧指令上限优化的功能
 > - `(1/3)` 新增目标平均分配模式的一个快捷键，现在只有按住该快捷键时才会启用功能
 > - `(1/3)` 新增 `Trajectory.Engrave.UseDisperseCoord` 、`Trajectory.Tracing.UseDisperseCoord` 和 `Trajectory.Disperse.RecordSourceCoord` 选项，可以让扩散的切割抛体和追踪抛体能够继承扩散抛体的发射位置
 > - `(1/3)` 新增 `Adjacent.AllowedUnit` 、`Adjacent.DisallowedUnit` 选项，可以用于限定哪些单位能够拓展建筑的建造范围
 > - `(1/3)` 更换了建筑图标预览和叠加显示的底层，现在能够读取 `FactoryOwners` 、`StolenTech` 、`SecretLab` 和 `RequiredTheaters` 等 Ares 标签，详细说明请见《额外功能说明》
 >   - 移除 `Cameo.AuxTechnos` 选项
 >   - 移除 `Cameo.NegTechnos` 选项
 >   - 新增 `Cameo.RequiredHouses` 选项
 >   - 新增 `Cameo.OverrideTechnos` 选项
 > - `(1/3)` 移除《MO3实例参考》和《超武侧栏背景》

### 2025.2.2  `Phobos-build-45+3_12` -> `Phobos-build-45+3_13`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.1.29 - `commit f5580682eca55070c3e0d67047f24ac51316d75b`*）
 >   - *`commit 319fd276...`* 修正一个说明书的链接错误和图片引用错误
 >   - *`commit 45536155...`* 修正四个说明书的链接错误
 >   - *`commit f5580682...`* 现在 `Power` 能被所有科技类型的单位使用
 > - `(1/3)` 退回 `SuperWeaponSidebar` 在 `Phobos-build-45+3_12` 的修复，采用下一帧刷新的机制
 > - `(1/3)` 现在 `AutoBuilding` 会先判断 `PowersUpBuilding` ，再判断 `PlaceAnywhere`
 > - `(1/3)` 现在 `ExtendedScatterAction` 启用后，不再记录非移动状态下单位的目的地（将由单位当前的任务决定新目的地）
 > - `(1/3)` 修复 `ExtendedBuildingPlacing` 启用后，未知情况下会因为没有建造建筑所属方而弹窗的问题
 > - `(1/3)` 修复 `MergeBuildingDamage` 在部分情况下可能导致伤害丢失的问题（如产生的碎片在判定过程中造成了伤害）

### 2025.1.27  `Phobos-build-45+3_11` -> `Phobos-build-45+3_12`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.1.27 - `commit 444504194a0549eebdc0a110031502f944d10170`*）
 >   - *`commit 6b001087...`* 新增 `ShowFlashOnSelecting` 选项，用于客户端设置选中时是否闪光
 >   - *`commit 44450419...`* 新增 `KeepTargetOnMove` 相关选项，用于让单位接受移动命令时不重置目标
 > - `(1/3)` 修复 `ExtendedBuildingPlacing` 启用后，刚拍下的建筑在判断无法疏散时，不会返还给鼠标建筑的问题
 > - `(1/3)` 修复 `ExtendedScatterAction` 启用后，在步兵重叠时（如伞兵）可能导致游戏出现死循环的问题
 > - `(1/3)` 修复 `SuperWeaponSidebar` 在更新时有时存在延迟的问题
 > - `(1/3)` 新增 `AttackMove.Aggressive` 和 `AttackMove.UpdateTarget` 选项，用于微观定义攻击移动时是否会选择建筑或重选目标

### 2025.1.26  `Phobos-build-45+3_10` -> `Phobos-build-45+3_11` (beta: `Phobos-build-45+1_6`)

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.1.26 - `commit e8876554b7fbb0a997120cba52a4ddeffa5d16d2`*）
 >   - *`commit 041eb9f4...`* 修复 `BuildingProductionQueue` 启用后防御栏建筑能够超过建造限制的问题
 >   - *`commit e064905d...`* 更新文档项目依赖关系和版权年份
 >   - *`commit 17c008ae...`* 修复 `BuildingProductionQueue` 启用后防御栏建筑在失去建造前提后不会被移出队列的问题
 >   - *`commit b8c829f4...`* 更新贡献文档和改动说明（区分v0.4稳定版）
 >   - *`commit ceb4013f...`* 移除变形逻辑重复无用的健全性检查
 >   - *`commit e8876554...`* 修复文档的各种问题
 > - `(1/3)` 修复 `Cameo.AuxTechnos` 和 `Cameo.NegTechnos` 检查非升级建筑的数量时的错误
 > - `(1/3)` 修复 `MergeBuildingDamage` 会使范围为零的弹头无法对建筑造成伤害的问题
 > - `(1/3)` 现在更换目标平均分配模式时不再输出提示信息，而是直接在鼠标位置绘制一个范围指示圈
 > - `(1/3)` 现在 `PlaceBuilding.OnLand` 和 `PlaceBuilding.OnWater` 从检查 `Naval` 更换为检查 `WaterBound` 以保证功能准确
 > - `(1/3)` 新增 `Cameo.ShouldCount` 选项，用于自定义建筑是否需要统计

### 2025.1.24  `Phobos-build-45+3_9` -> `Phobos-build-45+3_10`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.1.24 - `commit e91486f20e4ad4b6886aa64418d5e233a88d00ff`*）
 >   - *`commit 60547ce4...`* 修正说明书的拼写错误
 >   - *`commit 53e06847...`* 修正说明书的链接错误
 >   - *`commit 5a9a0ef7...`* 修复 `Trajectory.Straight.ProximityMedial` 在被大面积建筑触发时仍可能在建筑中心引爆的问题，修复 `Trajectory.Straight.ProximityRadius` 较大且 `Trajectory.Speed` 较小时抛体在决定引爆后会突然加速冲出射程的问题
 >   - *`commit 750afebb...`* 修复 `ExtraWarheads.DetonationChances` 和 `ExtraWarheads.FullDetonation` 总是读取最后一项有效设置的问题
 >   - *`commit a2a96b90...`* 补充遗漏的贡献文档
 >   - *`commit e91486f2...`* 新增 `BuildingProductionQueue` 选项，用于允许建筑像其它单位一样可以排队建造
 > - `(1/3)` 修复 `RateDown.Reset` 在重置目标的第一帧可能仍然使用重置前阶段的武器进行攻击的问题
 > - `(1/3)` 修复 `AIBiasSpawnCell` 在AI没有基地节点时可能出现的弹窗
 > - `(1/3)` 修复 `Cameo.AuxTechnos` 和 `Cameo.NegTechnos` 未计入建筑升级的问题
 > - `( 3 )` 修复同帧指令上限优化在多于128个单位同时使用移动攻击指令时会弹窗的问题
 > - `(1/3)` 现在 `ExpandBuildingPlace` 启用后，若某个建筑建造完成，不再会清空鼠标上的不同建筑
 > - `(1/3)` 现在 `LimboBuild` 和 `AutoUpgrade` 不再需要 `ExpandBuildingPlace` 就能启动
 > - `(1/3)` 更改 `ExpandBuildingPlace` 键名为 `ExtendedBuildingPlacing`
 > - `(1/3)` 更改 `NoRearm.Inactive` 键名为 `NoRearm.UnderEMP`
 > - `(1/3)` 更改 `NoReload.Inactive` 键名为 `NoReload.UnderEMP`
 > - `(1/3)` 更改 `AutoUpgrade` 键名为 `AutoBuilding` ，且现在支持全自动放置建筑，并增加一个 `AutoBuilding.Gap` 选项
 > - `(1/3)` 新增 `PlaceBuilding.OnLand` 和 `PlaceBuilding.OnWater` 选项，用于在放置建筑时随地面类型自动切换
 > - `(1/3)` 新增 `AutoBuilding` 全局选项，作为微观定义的默认值，并增加一个快捷键用于开关，详细说明请见《额外功能说明》
 > - `( 3 )` 新增 `EnableAggressiveStance` 选项，用于全局启用该逻辑
 > - `(1/3)` 新增 `MergeBuildingDamage` 选项，用于合并范围伤害对建筑的结算
 > - `(1/3)` 新增 `CanManualReload.ResetROF` 、`CanManualReload.DetonateWarhead` 和 `CanManualReload.DetonateConsume` 选项，详细说明请见《额外功能说明》

### 2025.1.17  `Phobos-build-45+3_8` -> `Phobos-build-45+3_9`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.1.17 - `commit 040637e17d870b306ff2fe7ddb0e13a70fc9d672`*）
 >   - *`commit 32f4cd36...`* 现在所有飞机可以正常朝水面上的格子移动了，且如果拥有路径点，则不会在执行路径点前先朝着机场飞行
 >   - *`commit 4699bc43...`* 现在使用平面距离而不是立体距离来判断空中的小队成员是否到达任务目标点
 >   - *`commit 006795b0...`* 更新中文用户QQ群的说明
 >   - *`commit 060d2da9...`* 更新贡献文档
 >   - *`commit ab579b9f...`* 更换 `Units.RepairRate` 等功能在说明文档中的位置
 >   - *`commit d55e49b7...`* 修缮命名问题
 >   - *`commit d39e3495...`* 新增 `BarracksExitCell` 选项，用于自定义的兵营出兵点
 >   - *`commit ff1dbb74...`* 补充贡献文档
 >   - *`commit 68e222a3...`* 更新 `Trajectory=Sample` 弹道抛体模板，并修缮了弹道抛体的部分逻辑
 >   - *`commit 47c7751c...`* 更新 `Trajectory=Straight` 的弹道抛体，拥有了大量新功能
 >   - *`commit ef7c119e...`* 修正说明书的拼写错误
 >   - *`commit 921d4392...`* 修复 `SuspendedEMPulseSWs` 可能导致的存档读档问题
 >   - *`commit 040637e1...`* 修复 `PowerPlantEnhancers` 可能导致的存档读档问题
 > - `(1/3)` 更换了超武侧栏的底层，现在使用 [Exclusive SuperWeapon Sidebar #1384] by NetsuNegi & CrimRecya 的配置
 >   - 移除 `SWSidebarBackground` 、`SW.QuickFireAtMouse` 、`SW.QuickFireInScreen` 选项
 >   - 更改 `SW.InScreen.Show` 键名为 `SuperWeaponSidebar.Allow` ，且其默认值由 `no` 更改为 `yes`
 >   - 更改 `SW.InScreen.PriorityHouses` 键名为 `SuperWeaponSidebar.PriorityHouses`
 >   - 更改 `SW.InScreen.RequiredHouses` 键名为 `SuperWeaponSidebar.RequiredHouses`
 >   - 更改 `SWSidebarBackground.OnPCX` 键名为 `SuperWeaponSidebar.OnPCX`
 >   - 更改 `SWSidebarBackground.OffPCX` 键名为 `SuperWeaponSidebar.OffPCX`
 >   - 更改 `SWSidebarBackground.TopPCX` 键名为 `SuperWeaponSidebar.TopPCX`
 >   - 更改 `SWSidebarBackground.CenterPCX` 键名为 `SuperWeaponSidebar.CenterPCX`
 >   - 更改 `SWSidebarBackground.BottomPCX` 键名为 `SuperWeaponSidebar.BottomPCX`
 >   - 新增 `SuperWeaponSidebar` 等选项，详细说明请见《额外功能说明》
 > - `(1/3)` 现在 `NoRearmInEMPState` 被拆分成了 `NoRearm.Inactive` 和 `NoReload.Inactive`
 > - `(1/3)` 现在 `NoRearmInTemporal` 被拆分成了 `NoRearm.Temporal` 和 `NoReload.Temporal`
 > - `(1/3)` 现在 `ExtendedScatterAction` 能够用于对向移动时让路，并修复某些情况下单位被堵住时会中断行为的问题，但需要注意在面对大量步兵停在狭窄路口的情况时仍然不够完善
 > - `( 1 )` 修复 `RateDown.Ammo` 默认值错误的问题
 > - `( 3 )` 新增 `SpyEffect.RadarJamDuration` 选项，用于间谍渗透时关闭小地图
 > - `(1/3)` 新增 `BuildingSell` 选项，用于在弹头引爆时出售建筑
 > - `(1/3)` 新增 `UnitWithoutTurretAlwaysTurnToTarget` 选项，用于原本的无炮塔车辆武器冷却时转向功能
 > - `(1/3)` 新增 `CanManualReload` 选项和手动回复弹药的快捷键，详细说明请见《额外功能说明》

### 2025.1.9  `Phobos-build-45+3_7` -> `Phobos-build-45+3_8` (beta: `Phobos-build-45+1_5`)

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2025.1.7 - `commit 40711340ee2aa4db0d0b9a65d2b4807a7c737c56`*）
 >   - *`commit 9d6cf389...`* 修复单位升级动画在已设置但未全部设置的情况下会弹窗的问题
 >   - *`commit 995cfba5...`* 修复单位在接收停止命令时不会清除区域防守任务的问题
 >   - *`commit 40711340...`* 修复 `DeployToFire` 在 [build-37] 后出现的联机不同步的问题
 > - `( 3 )` 合并 [[New Feature] Aggressive Stance (auto target buildings) #1464] by Aephiex，用于启用单位的侵略模式
 > - `(1/3)` 优化 `NoRearmInEMPState` 、`NoRearmInTemporal` 、 `AutoUpgrade` 和 `FactoryProgressDisplay` 的部分代码
 > - `(1/3)` 现在使用平面距离而不是立体距离来判断空中的小队成员是否到达任务目标点
 > - `(1/3)` 现在 `Turret.Restriction` 在极低范围下采用独立逻辑
 > - `( 3 )` 修复游戏内部分强制散开的功能被意外跳过的问题
 > - `( 3 )` 修复 `JumpjetSpeedType` 意外导致 `BalloonHover=no` 的单位无法移动到水面上的问题
 > - `(1/3)` 修复 `SubjectToGround` 失效并引起的寻路问题
 > - `(1/3)` 新增 `MindControl.ThreatDelay` 选项，用于心控弹头单独定义被心控单位被自动攻击或反击的延迟
 > - `(1/3)` 新增 `ExtendedScatterAction` 选项，用于启用优化的让路逻辑
 > - `(1/3)` 新增 `Trajectory.Disperse.CruiseAltitude` 和 `Trajectory.Disperse.CruiseAlongLevel` 选项，用于自定义巡航高度
 > - `(1/3)` 增加调试显示的输出内容，并修复图层问题

### 2025.1.1  `Phobos-build-45+3_6` -> `Phobos-build-45+3_7` (beta: `Phobos-build-45+1_4`)

 > - `(1/3)` 优化 `AirburstWeapon` 、`Trajectory=Disperse` 、`Trajectory=Tracing` 和超武侧边栏的部分代码
 > - `(1/3)` 现在 `Trajectory.Tracing.Weapons` 开火时的动画可以正确附着在单位或抛体上
 > - `(1/3)` 修复 `Trajectory.Disperse.Weapons` 和 `Trajectory.Tracing.Weapons` 的开火动画在建筑上的图层不太正确的问题，并修复其在 `Phobos-build-45+3_6` 的更新后可能丢失开火动画来源单位的问题
 > - `(1/3)` 新增调试显示及其开关快捷键

### 2024.12.31  `Phobos-build-45+3_5` -> `Phobos-build-45+3_6`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2024.12.29 - `commit 7090604b3865bec90a838c92e6e3ab62a057fab1`*）
 >   - *`commit c5b894e7...`* 修复 Ares 的 `InitialPayload` 在被触发生成小队时的问题
 >   - *`commit 7090604b...`* 新的飞机拓展行为机制，并改进了对所有单位停止行为的处理
 > - `(1/3)` 现在 `ExtendedAircraftMissions` 功能已被公版合并，从说明书中移除（[#1366] -> [commit 7090604b...]）
 > - `(1/3)` 现在 `NoQueueUpToEnter` 和 `NoQueueUpToUnload` 可以在载具上进行微观定义
 > - `(1/3)` 现在所有飞机可以正常朝水面上的格子移动了，且如果拥有路径点，则不会在执行路径点前先朝着机场飞行
 > - `( 3 )` 现在鼠标点击可以操作任意数量的单位，且可以在多人游戏生效，使用新的技术路线，可能会导致一些问题，遇到请反馈
 > - `( 3 )` 修复玩家自动攻击围墙时，有时墙被拆掉后仍然不会停火的问题
 > - `( 3 )` 修复搜索移动中获取目标后可能丢失搜索移动任务的问题
 > - `(1/3)` 更改 `Trajectory.Straight.PassDetonateTimer` 键名为 `Trajectory.Straight.PassDetonateInitialDelay`
 > - `(1/3)` 更改 `Trajectory.Disperse.WeaponTimer` 键名为 `Trajectory.Disperse.WeaponInitialDelay`
 > - `(1/3)` 更改 `Trajectory.Disperse.WeaponScope` 键名为 `Trajectory.Disperse.WeaponEffectiveRange`
 > - `(1/3)` 更改 `Trajectory.Tracing.WeaponTimer` 键名为 `Trajectory.Tracing.WeaponInitialDelay`
 > - `(1/3)` 移除 `NoRepeatFire` 功能，功能可以由更通用的武器 `AttachEffect` 完成
 > - `( 3 )` 新增 `RallySpeedType` 和 `RallyMovementZone` 选项，详细说明请见《额外功能说明》
 > - `(1/3)` 新增武器上的 `AttachEffect` 选项，详细说明请见《额外功能说明》
 > - `(1/3)` 增加 `ObjectInfoDump` 的输出信息
 > - `(1/3)` 更新《MO3实例参考》

### 2024.12.27  `Phobos-build-45+3_4` -> `Phobos-build-45+3_5`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2024.12.26 - `commit 3a54e85d2195c5915bcee29cceecdb2451e9a748`*）
 >   - *`commit 31afb490...`* 测试人员鸣谢名单
 >   - *`commit 3a54e85d...`* 添加有关如何向YRpp提交更改的信息
 > - `(1/3)` 更改 `AINonAutoDeploy` 键名为 `AIAutoDeployMCV`，且其默认值由 `no` 更改为 `yes` ，同时 `AISetBaseCenter` 的默认值由 `no` 更改为 `yes` ，使这两项的键名更符合其实际作用，当使用默认值时，仍然都是使用原版的行为逻辑
 > - `(1/3)` 更改 `ExpandAircraftMission` 键名为 `ExtendedAircraftMissions`
 > - `(1/3)` 移除 `LeaveTransportKill` 功能
 > - `( 3 )` 修复 `ResetGattlingValue` 未重置盖特阶段的问题
 > - `(1/3)` 修复 `ExpandBuildingPlace` 在单向结盟时显示建造预览判断错误的问题
 > - `(1/3)` 修复 `AttackMindControlledDelay` 在单向结盟时对可攻击对象判断错误的问题
 > - `(1/3)` 修复 `SelectedDisplay.Enable` 在单向结盟时显示敌对单位判断错误的问题
 > - `(1/3)` 修复 `Trajectory.Tracing.SuicideAboveRange` 判断条件错误的问题
 > - `(1/3)` 新增 `Trajectory=Tracing` 的几个自定义选项，详细说明请见《额外功能说明》

### 2024.12.23  `Phobos-build-45+3_3` -> `Phobos-build-45+3_4`

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2024.12.21 - `commit fa48f6128791023cb64dff6d75d29bd86cb1e86c`*）
 >   - *`commit a650e6b8...`* 说明文档修正
 >   - *`commit f0791edb...`* 说明文档修正
 >   - *`commit 3b63de7a...`* 修复 Ares 的 `InitialPayload` 在部分情况下的问题
 >   - *`commit fa48f612...`* 更新贡献文档（[Pre-Release-v0.4]）
 > - `( 3 )` 移除触发小队初始载员修复（ #1375 ），公版已对其进行修复
 > - `(1/3)` 现在 `ExpandAircraftMission` 的平滑移动路线不会影响正在执行脚本任务的飞机，而且现在飞机在返回机场时如果接收到停止命令不再会刷新返回状态，同时修复原版停在机场的飞机能够接收停止命令并会停止回复弹药生命且可以重叠放置的问题，并修复当 Ctrl+Shift 的目标是一个实体对象（不是地面）时会导致飞机一直重复失去目标、获取目标并原地盘旋的问题
 > - `(1/3)` 现在 `NoQueueUpToUnload` 释放载员时不再会有1帧的间隔
 > - `(1/3)` 现在 `AttackMove.Aggressive` 启用时，在单位有攻击目标的情况下仍会自动重新选择威胁更高的目标进行攻击
 > - `( 3 )` 现在 `AttackMove.Follow` 启用时，会尽量避免单位被太多单位跟随
 > - `(1/3)` 修复 `ExpandBuildingPlace` 启用时，正在等待部署的载具被转移所属方后未从原所属方等待队列中移除而导致可能在继续等待部署的过程中被摧毁后弹窗的问题
 > - `(1/3)` 修复 `NoQueueUpToEnter` 在 `Phobos-build-45+3_2` 的更新后如果进入载具的乘客较多，会因和载具重叠而产生空气墙的问题
 > - `(1/3)` 修复 `Trajectory=Disperse` 的抛体在同时使用 `Trajectory.Disperse.CruiseEnable` 和 `Trajectory.Disperse.LeadTimeCalculate` 时如果目标为单位则会不断朝上飞的问题
 > - `(1/3)` 新增 `BuildingUndeploy.Leave` 选项，可以让单位在被收起后前往威胁较低的空地
 > - `(1/3)` 新增 `RateDown.Reset` 选项，可以让单位在没有目标或更换目标时直接重置盖特计数
 > - `( 3 )` 新增 `AttackMove.PursuitTarget` 选项，可以让近战单位在移动攻击时在获取目标后转而向目标移动
 > - `(1/3)` 新增目标平均分配模式，需要两个快捷键，详细说明请见《额外功能说明》
 > - `(1/3)` 增加 `ObjectInfoDump` 的输出信息

### 2024.12.11  `Phobos-build-45+3_2` -> `Phobos-build-45+3_3` (beta: `Phobos-build-45+1_3`)

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2024.12.9 - `commit 796b2b4d12bfb9a452eb9532c8fd117b79f2ce88`*）
 >   - *`commit 1fc263b2...`* 修复暴击弹头触发时未检查暴击动画数量的问题
 >   - *`commit 796b2b4d...`* [重要]部分还原 *`commit 6c856f03...`* 的多文件解析行为，防止读取遗漏或错误导致出现严重问题
 > - `(1/3)` 修复 `ExpandAircraftMission` 意外影响 `AirportBound=no` 的飞行器移动行为的问题
 > - `(1/3)` 更正 `ObjectInfoDump` 的部分格式

### 2024.12.10  `Phobos-build-45+3_1` -> `Phobos-build-45+3_2` (beta: `Phobos-build-45+1_2`)

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2024.12.9 - `commit d0aca00ee3cc6f50bce1cf545ec5370b8f79b89b`*）
 >   - *`commit f749352b...`* 修复所有Ares和Phobos新效果范围变大的问题
 >   - *`commit 06e0285b...`* 临时修复 `CreateUnit` 创造空中单位时没有将单位添加到 AirTracker 的问题
 >   - *`commit 43a75880...`* 修复发射超武触发行为的问题
 >   - *`commit fd935c3f...`* 部分代码修正
 >   - *`commit 5889d51a...`* 新增 `ApplyLunarFixes` 选项，以决定是否启用月球地形修复
 >   - *`commit d0aca00e...`* 从客户端加载游戏时初始化 Enumerable 数组
 > - `( 3 )` 移除 `AircraftWaypoint` 选项，现在 `ExpandAircraftMission` 启用后即会启用飞机的路径点功能
 > - `(1/3)` 修复 `ExpandAircraftMission` 启用后，飞机的警戒行为搜索距离为固定的超长距离的问题，且现在平滑了飞机的移动路线
 > - `(1/3)` 修复 `Trajectory=Straight` 的抛体在 `Phobos-build-44+3_30` 的更新后只能在特定角度下攻击到障碍物的问题
 > - `(1/3)` 现在 `NoQueueUpToEnter` 不再禁止使用路径点进行连续进入的行为（若之后路径点进入的载具已满，则可能出现空气墙）
 > - `(1/3)` 新增 `NoQueueUpToUnload` 选项，可以让载员快速离开载具
 > - `(1/3)` 新增 `ExpandTurretRotation` 选项，用于启用所有炮塔旋转相关的功能
 >   - 更改 `UnitIdleRotateTurret` 键名为 `Turret.IdleRotate`
 >   - 更改 `UnitIdlePointToMouse` 键名为 `Turret.PointToMouse`
 >   - 更改 `UnitIdleActionRestartMin` 键名为 `Turret.IdleRestartMin`
 >   - 更改 `UnitIdleActionRestartMax` 键名为 `Turret.IdleRestartMax`
 >   - 更改 `UnitIdleActionIntervalMin` 键名为 `Turret.IdleIntervalMin`
 >   - 更改 `UnitIdleActionIntervalMax` 键名为 `Turret.IdleIntervalMax`
 >   - 更改 `Turret.SelfRotation.Restriction` 键名为 `Turret.Restriction`
 >   - 更改 `Turret.SelfRotation.Angle` 键名为 `Turret.ExtraAngle`
 >   - 更改 `Turret.BodyRotation.Enable` 键名为 `Turret.BodyOrientation`
 >   - 更改 `Turret.BodyRotation.Angle` 键名为 `Turret.BodyOrientationAngle`
 >   - 更改 `Turret.BodyRotation.Symmetric` 键名为 `Turret.BodyOrientationSymmetric`
 >   - 现在 `Turret.IdleRotate` 支持 `Locomotor=Jumpjet` 的单位在闲置时旋转炮塔了

### 2024.12.8  `Phobos-build-45+3_0` -> `Phobos-build-45+3_1` (beta: `Phobos-build-45+1_1`)

 > - `(1/3)` 修复所有 Ares 和 Phobos 新效果范围变大的问题（由公版 *`commit 69e88125...`* 导致的问题）
 > - `(1/3)` 修复 `SelectedDisplay.Enable` 在非己方侦测伪装范围内选中一个伪装成石头、树木等物体的单位时会弹窗的问题
 > - `(1/3)` 增加 `ObjectInfoDump` 的输出信息

### 2024.12.6  `Phobos-build-44+3_31` -> `Phobos-build-45+3_0` (beta: `Phobos-build-45+1_0`)

 > - `( 0 )` 合并 Phobos-develop 近期所有改动（*2024.12.6 - `commit e7a13104c67c24744364f9d5e02c6198df88332b`*）
 >   - *`commit 69e88125...`* 优化获取范围内单位的算法（~~该更新不明所以地把格子上单位的距离检查给删了，感觉会~~**有问题**）
 >   - *`commit 1528f0d4...`* 将 Ares 原本不准确的获取范围内单位的算法修正为 Phobos 使用的算法
 >   - *`commit 6f9b1c47...`* 部分代码简化
 >   - *`commit e7a13104...`* 版本号和版权信息更新（[build-44] -> [build-45]）
 > - `( 3 )` 修复 `UnlimboDetonate` 的武器在部分情况下可能会弹窗的问题
 > - `(1/3)` 修复 `Locomotor=Jumpjet` 的单位在执行停止命令时判断朝向不够准确的问题，以及在执行移动攻击命令过之后在执行停止命令时仍会陷入袖手旁观状态的问题
 > - `(1/3)` 优化 `JumpjetClimbPredictHeight` 对前方格子的判断，使用更精确的算法并移除了原本多余的重复判断
 > - `(1/3)` 增加 `ObjectInfoDump` 的输出信息
