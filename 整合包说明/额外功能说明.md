# 额外功能说明

> - Modified by **[绯红热茶/CrimRecya](https://github.com/CrimRecya)**
> - 平台仓库：**[Mix-ECpack](https://github.com/CrimRecya/Phobos-Mix/tree/Mix-ECpack)**
> - 公版地址：**[Phobos](https://phobos.readthedocs.io/en/latest/#)**
> - 公版仓库：**[Phobos-develop](https://github.com/Phobos-developers/Phobos)**

```{hint}
- 在正式使用前，请认真阅读以下使用须知
```

---

## 使用须知

- 本平台仅在**凛冬将至**群内发布最新版本，并授权EC整合包开发组合使用
- 除制作任务包、模组演示外，**不允许**以任何形式在未获得作者许可的情况下，对本平台一个或多个独有功能进行独立展示
- 本平台仅作内部测试使用，不对外宣传，除待公版合并的内容外，**不允许**任何人以任何形式直接对外宣传平台开发内容
  > 为什么不对外宣传？-本质是测试，其功能并不如公版稳定，bug发出去了没人修
- 本平台可以在上述条件下自行使用，但请注意公开场合使用时（直播、视频等），必须在合适位置注明使用的本平台**并非公版**
- 如果需要反馈问题，请在确认为**最新版本**后，在群内联系作者以寻求解决方案
- 如只需在模组使用部分功能，请自行合并需要的内容，并**标明来源**
  > 严禁冒名顶替作者的行为，请尊重他人的劳动成果
- 平台试用期自发布起计算，共30天，自用时请自行更新最新版本，若需要发布任务包、模组使用，请联系作者

```{note}
- **`注意`** 平台并非所谓新版、开发版Phobos
  - 就目的而言，平台属于测试版，其中部分功能*可能*会被公版合并
  - 就结果而言，平台属于私改版，与公版并无直接关系
```

```{note}
- **`注意`** 凛冬将至（WIC/486835964）群并非本平台作者的群，仅作为传统在其中进行发布
  - 凛冬将至群为WIC平台/模组的官方群，请保证基本的礼貌和尊重
  - 本平台不会与WIC平台发生直接冲突，但如在游戏过程中发现此类问题，请及时反馈
```

---

## 温馨提醒

- 平台仍在持续更新中，新增内容较多，如果找不到更新改动说明中的内容，请善用查找搜索
- 当非大型功能被公版合并，此说明书将不再保留其功能介绍，而为方便使用，大型功能的说明仍会保留
- 使用某些功能出现小问题时，请不要觉得没什么关系或者问题不大就算了，只要是说明中没有明确提到的，都可以提出
- 提出建议、反馈问题时，请不要在群里直接说，善用 **@** 成员、私聊（或 `凛冬将至` 群在线文档），否则容易被遗忘或者根本没注意到
  - 如果出现弹窗需要反馈，提供根目录下 `debug` 文件夹中当局的 `snapshot` 文件夹内的 `debug.log` 、`except.txt` 和 `syringe.log` ，如果你在弹窗时点了 `确认` ，那么该文件夹中还会生成一个 `.dmp` 文件，除特殊情况外，该文件**不**需要提供
  - 你也可以通过查看 `debug.log` 底部的 `EIP` 或 `except.txt` 顶部的 `EIP` 自行检查错误类型，当 `EIP` 为六位数时（ `00` 开头的八位数），通常为原版问题，你可以自行通过 `modenc` 等网站自行查阅常见 `EIP` 类型；如果为正常的八位数，你可以通过 `debug.log` 顶部的数个 `Module XXX.dll  base address` 看到所有拓展的基地址，打开计算器的程序员模式，使用十六进制减法将 `EIP` 减去各个基地址，当结果为正数（不是一长串 `F` 开头的数）且最小时，通常能够表明是这个拓展出现的问题，如果仍不能确定，请使用上一种方法

```{hint}
- 如果在刚更换完dll时发生问题，请先尝试重启电脑，可能是dll未成功完全完成替换导致的，重启后若问题仍然存在，请反馈
```

---

## 特别感谢

- 感谢 **`Phobos`** 的所有开发者和贡献者
- 感谢 **`Sidoupiar和水菌`** 给予的额外功能开发方面的帮助
- 感谢 **`钢铁之锤`** 制作了适配的EC开发组件
- 感谢 **`航味麻酱`** 制作了更多的拓展功能并帮助维护平台
- 感谢 **`凛冬将至`** 的**群友们**给予的思路并帮忙进行各种测试
- 感谢 **`pbs测试群`** 的**群友们**给予的思路并帮忙进行各种测试

---

## 内容简介

- 公版内容不做任何解释，请自行前往 **[Phobos文档](https://phobos.readthedocs.io/en/latest/)** 查阅公版最新的英文说明书
  > **[中文版本](https://phobos.readthedocs.io/zh-cn/latest/)** 为每周更新，会略慢于英文版本
- 注意使用的是develop分支而不是build（名称中的build只是简单告诉你现在是在哪个build之后的版本），即公版部分内容也是随版本更新的，具体更新的内容可以根据《更新改动说明》中的commit编号进行搜索
  > 公版问题可以提出，作者会尝试为公版问题提供修复
- 其它 **Phobos Contributor** 制作的未合并功能，内容来自于 **[Phobos拉取](https://github.com/Phobos-developers/Phobos/pulls)**
  > 出现问题时不保修，出现严重问题时请联系我，我会将其剔除；当该这些内容被公版合并时会从本说明书中移除

- **[绯红热茶/CrimRecya](https://github.com/CrimRecya)** ，平台作者
- **[航味麻酱/TaranDahl](https://github.com/TaranDahl)** ，主要功能作者

---

## 额外功能目录

- 单位

> - [方向装甲](#方向装甲)
> - [残骸系统](#残骸系统)
> - [载具伪装](#载具伪装)
> - [小队管理](#小队管理)
> - [步兵移动攻击](#步兵移动攻击)
> - [同帧发射武器](#同帧发射武器)
> - [波能蓄力逻辑](#波能蓄力逻辑)
> - [渗透关闭小地图](#渗透关闭小地图)
> - [工程师自动开火](#工程师自动开火)
> - [多炮管和多炮塔](#多炮管和多炮塔)
> - [炮塔旋转行为拓展](#炮塔旋转行为拓展)
> - [矿车倒矿流程优化](#矿车倒矿流程优化)
> - [禁止手动弹出载员](#禁止手动弹出载员)
> - [移动攻击任务修复](#移动攻击任务修复)
> - [不在地图上保持更新](#不在地图上保持更新)
> - [自定义解除隐身行为](#自定义解除隐身行为)
> - [进入命令不停止路径点](#进入命令不停止路径点)
> - [移动不打破超时空锁定](#移动不打破超时空锁定)
> - [修复原超时空保持距离](#修复原超时空保持距离)
> - [导弹子机发射位置拓展](#导弹子机发射位置拓展)
> - [区域警戒跟随目标本身](#区域警戒跟随目标本身)
> - [机场生产悬浮空军单位](#机场生产悬浮空军单位)
> - [载具中步兵装弹问题修复](#载具中步兵装弹问题修复)

- 建筑

> - [集结点增强](#集结点增强)
> - [重工顺产优化](#重工顺产优化)
> - [碾压死亡增强](#碾压死亡增强)
> - [集结点寻路优化](#集结点寻路优化)
> - [修复载具克隆工厂](#修复载具克隆工厂)
> - [电磁脉冲暂停电脑生产](#电磁脉冲暂停电脑生产)
> - [重工和通行建筑自定义](#重工和通行建筑自定义)
> - [建筑建造范围边界拓展](#建筑建造范围边界拓展)
> - [建筑建造和部署行为拓展](#建筑建造和部署行为拓展)

- 运动

> - [优化让路逻辑](#优化让路逻辑)
> - [高级驾驶移动方式](#高级驾驶移动方式)
> - [钻地移动矿车修复](#钻地移动矿车修复)
> - [自定义钻地决定距离](#自定义钻地决定距离)
> - [钻地移动方式自定义](#钻地移动方式自定义)
> - [飞机移动方式自定义](#飞机移动方式自定义)
> - [悬浮空军单位智力恢复](#悬浮空军单位智力恢复)
> - [悬浮空军单位速度类型](#悬浮空军单位速度类型)
> - [悬浮空军单位寻路修正](#悬浮空军单位寻路修正)

- 索敌

> - [区域警戒优化](#区域警戒优化)
> - [自动攻击优化](#自动攻击优化)
> - [攻击潜地单位](#攻击潜地单位)
> - [自动攻击围墙](#自动攻击围墙)
> - [额外索敌时机](#额外索敌时机)
> - [血量检测增强](#血量检测增强)
> - [维修子机修复](#维修子机修复)
> - [方向威胁系数](#方向威胁系数)
> - [被索敌额外仇恨](#被索敌额外仇恨)

- 武器

> - [位移武器](#位移武器)
> - [追踪激光](#追踪激光)
> - [扫射增强](#扫射增强)
> - [圆柱形射程](#圆柱形射程)
> - [武器附加效果](#武器附加效果)

- 弹头

> - [合并范围伤害](#合并范围伤害)
> - [杀伤矿石弹头](#杀伤矿石弹头)
> - [仅伤害目标弹头](#仅伤害目标弹头)
> - [改变地图光照弹头](#改变地图光照弹头)
> - [弹头爆炸范围增强](#弹头爆炸范围增强)
> - [解除心控弹头扩展](#解除心控弹头扩展)
> - [禁用围墙递归受伤](#禁用围墙递归受伤)

- 抛射体

> - [抛体额外弹头](#抛体额外弹头)
> - [抛体扩散武器](#抛体扩散武器)
> - [抛体生命周期](#抛体生命周期)
> - [抛体重新索敌](#抛体重新索敌)
> - [弹道抛体系统](#弹道抛体系统)
> - [抛射体散布增强](#抛射体散布增强)
> - [无形抛体无延迟引爆](#无形抛体无延迟引爆)
> - [无形抛体阻挡问题修复](#无形抛体阻挡问题修复)

- 动画

> - [屏幕外依旧绘制](#屏幕外依旧绘制)

- 电脑

> - [玩家队友电脑智力恢复](#玩家队友电脑智力恢复)
> - [脚本巡逻至路径点增强](#脚本巡逻至路径点增强)
> - [新的火卫脚本目标选项](#新的火卫脚本目标选项)
> - [基地节点尝试次数恢复](#基地节点尝试次数恢复)
> - [作战方被击败行为扩展](#作战方被击败行为扩展)
> - [电脑建筑节点距离限制](#电脑建筑节点距离限制)

- 拓展

> - [附加单位](#附加单位)

- 界面

> - [拓展居中字幕](#拓展居中字幕)
> - [底边栏选择信息](#底边栏选择信息)
> - [独特单位独立显示](#独特单位独立显示)
> - [图标预览和叠加显示](#图标预览和叠加显示)
> - [统一雷达和单位颜色](#统一雷达和单位颜色)
> - [观战者计分板显示电脑](#观战者计分板显示电脑)

- 操作

> - [切换侵略模式](#切换侵略模式)
> - [切换停火模式](#切换停火模式)
> - [平均分配模式](#平均分配模式)
> - [手动回复弹药](#手动回复弹药)
> - [共享盟友控制](#共享盟友控制)
> - [自动化建筑放置](#自动化建筑放置)
> - [鼠标忽略的物件](#鼠标忽略的物件)
> - [选中心控的单位](#选中心控的单位)
> - [同帧指令上限优化](#同帧指令上限优化)

- 杂项

> - [渐隐弹坑](#渐隐弹坑)
> - [减少绘制帧率](#减少绘制帧率)
> - [额外建筑进度条](#额外建筑进度条)
> - [单位默认透明度](#单位默认透明度)
> - [修改侧栏滚动方式](#修改侧栏滚动方式)
> - [玩家建筑自动维修](#玩家建筑自动维修)
> - [占据位空气墙修复](#占据位空气墙修复)
> - [占领建筑副官语音](#占领建筑副官语音)

- 开发

> - [调试模式](#调试模式)

---

## 具体说明

### 单位

---

#### 方向装甲

  > - *pr #1623 by NetsuNegi*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[CombatDamage]                               ; 全局伤害设置
DirectionalArmor=no                          ; 布尔值，是否启用方向护甲逻辑
DirectionalArmor.FrontMultiplier=1.0         ; 浮点型，正面受击时，伤害倍率
DirectionalArmor.SideMultiplier=1.0          ; 浮点型，侧面受击时，伤害倍率
DirectionalArmor.BackMultiplier=1.0          ; 浮点型，背面受击时，伤害倍率
DirectionalArmor.FrontField=0.5              ; 浮点型，正面判定范围
DirectionalArmor.BackField=0.5               ; 浮点型，背面判定范围

[SOMEVEHICLE]                                ; 任意车辆
DirectionalArmor=                            ; 布尔值，是否启用方向护甲逻辑，默认值为 `CombatDamage` 中 `DirectionalArmor` 的值
DirectionalArmor.FrontMultiplier=            ; 浮点型，正面受击时，伤害倍率，默认值为 `CombatDamage` 中 `DirectionalArmor.FrontMultiplier` 的值
DirectionalArmor.SideMultiplier=             ; 浮点型，侧面受击时，伤害倍率，默认值为 `CombatDamage` 中 `DirectionalArmor.SideMultiplier` 的值
DirectionalArmor.BackMultiplier=             ; 浮点型，背面受击时，伤害倍率，默认值为 `CombatDamage` 中 `DirectionalArmor.BackMultiplier` 的值
DirectionalArmor.FrontField=                 ; 浮点型，正面判定范围，默认值为 `CombatDamage` 中 `DirectionalArmor.FrontField` 的值
DirectionalArmor.BackField=                  ; 浮点型，背面判定范围，默认值为 `CombatDamage` 中 `DirectionalArmor.BackField` 的值

[SOMEWARHEAD]                                ; 任意弹头
Directional=no                               ; 布尔值，弹头是否具有方向性，具有方向性的抛射体击中具有方向性的载具或载具的护盾时将会触发方向护甲判定
Directional.Multiplier=1.0                   ; 浮点型，方向护甲影响倍率
```

```{note}
- 判定范围始终都是对称的，正面 0.5 表示以车头方向为轴，左右各 45 度角范围内均属于正面判断范围，1.0 则表示左右各 90 度均为正面判定范围
- 侧面范围是除开正面和背面后剩余的角度范围
```

---

#### 残骸系统

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
WreckageInitialHealthPercent=0.1             ; 浮点型，残骸初始生命比例

[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
WreckageType=                                ; Techno列表，残骸的类型，填多个会随机生成其中一个
WreckageInitialHealthPercent=                ; 浮点型，残骸初始生命比例，默认值为 `General` 中 `WreckageInitialHealthPercent` 的值
WreckageDeactive=yes                         ; 布尔值，残骸是否会被断电，就像遥控坦克失去控制中心了一样
WreckageMarkUp=yes                           ; 布尔值，残骸是否不会产生碰撞体积，不会被各种范围查找效果找到，只有精控、超时空等点名该单位的效果才能对其造成影响
WreckageOwner=Victim                         ; 归属类型，Invoker/Killer/Victim/Civilian/Special/Neutral（Invoker和Killer都是造成伤害的作战方，Victim是死亡单位的作战方）
WreckageLeaveOnWater=no                      ; 布尔值，单位位于水面时是否生成残骸
WreckageLeaveInAir=no                        ; 布尔值，单位位于空中时是否生成残骸
WreckageSwapLocomotor=no                     ; 布尔值，单位是否会跟残骸交换 Locomotor ，这有助于残骸保持在正确的位置上，但请确保原有的单位在受伤后会立即死亡

[SOMEWARHEAD]                                ; 任意弹头
SuppressWreckage=no                          ; 布尔值，被该弹头击杀的单位是否不会留下残骸
ActivateWreckage=no                          ; 布尔值，被该弹头伤害的残骸是否会恢复行动能力
```

```{note}
- 残骸的类别（步兵、车辆、飞机或建筑）不必和原本单位一致，新的单位会被强制放置在单位原本的位置，你可以使用 `WreckageMarkUp` 避免残骸与大多数效果产生交互，但如果你让残骸移动，那么 `WreckageMarkUp` 的效果会失效，`WreckageMarkUp` 建议和禁止选择、鼠标忽视物件等功能配合使用
- 如果残骸的类型是自己，那么当单位已经是残骸时，将不再生成残骸
- 单位必须有一个作战方，如果定义的归属作战方无效，那么就会使用死亡单位的作战方
- 建筑残骸目前没有额外支持（Ares已有废墟）
```

---

#### 小队管理

  > - *pr #1663 by Laspta971*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEINFANTRY]                               ; 任意步兵
Squad.Members=                               ; Techno类型，目前只能是步兵单位，当步兵被训练出来的时候，同时还会有哪些步兵出来

[SOMETECHNO]                                 ; 任意步兵、车辆
Squad.IsInitAsTeam=                          ; 布尔值，`Squad.Members` 和载具车辆的初始载员是否会同时被选中
```

---

#### 载具伪装

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

- 仅优化，无ini
- 现在可以让车辆伪装成其它车辆了， `DefaultDisguise` 功能现在也支持对车辆生效

```{note}
- 目前仅能正确伪装成vxl车辆，伪装成shp车辆时会发生图像异常
```

---

#### 步兵移动攻击

  > - *pr #1341 by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEINFANTRY]                               ; 任意步兵
FiringByPassMovingCheck=no                   ; 布尔值，步兵开火前是否不检查移动状态，如果为是，JJ步兵会转身朝向目标，而Walk步兵由于移动时必须播放移动序列而只能在每次进入格子时尝试开火，这能解决JJ步兵脚底下有建筑时无法开火的问题，同时可以通过 Ctrl+Shift 移动攻击，即使没有 [OpportunityFire=yes]
```

---

#### 同帧发射武器

  > - *pr #1467 by TaranDahl & CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEWEAPON]                                 ; 任意武器
Burst.NoDelay=no                             ; 布尔值，是否在同一帧发射所有 `Burst` 并无视任何 `BurstDelay`
```

```{note}
- 仅对步兵和车辆有效，对飞机和建筑是无效的
```

---

#### 波能蓄力逻辑

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEWEAPON]                                 ; 任意武器
ResetGattlingValue=no                        ; 布尔值，开火后是否清空加特林计数
AddtionalDamage.GattlingValue=no             ; 布尔值，武器是否额外附加正比于加特林计数的伤害
AddtionalDamage.GattlingValue.Mult=1.0       ; 浮点型，额外伤害是加特林计数的多少倍
```

---

#### 渗透关闭小地图

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEBUILDING]                               ; 任意 `SpyEffect.Custom=yes` 的建筑
SpyEffect.RadarJamDuration=0                 ; 整数型，单位为帧，建筑被渗透时关闭小地图多久
```

---

#### 工程师自动开火

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEINFANTRY]                               ; 任意步兵
Engineer.CanAutoFire=no                      ; 布尔值，工程师是否会自动开火，目前只能自动治疗友军
```

---

#### 多炮管和多炮塔

  > - 暂无pr *by TaranDahl & CrimRecya*
  > - [返回目录](#额外功能目录)

在 `artmd.ini` 之中：

```ini
[SOMEVEHICLE]                                ; 任意 VXL 车辆
BarrelOverTurret=                            ; 布尔值，炮管图层是否永远在炮塔之上，不填为原本的按照方向自动计算
BarrelOffset=0                               ; 整数型，主炮管相对炮塔中心点在Y轴（左右）方向上的偏移
ExtraBarrelCount=0                           ; 整数型，每个炮塔额外绘制的炮管数量
ExtraBarrelOffsetX=0                         ; 整数型，额外绘制的炮管相对炮塔中心点在Y轴（左右）方向上的偏移，X 是额外炮管索引，从 0 开始
ExtraTurretCount=0                           ; 整数型，额外绘制的炮塔数量
ExtraTurretOffsetX=0,0,0                     ; FLH坐标，请写完整，额外绘制的炮塔相对单位中心点的偏移，X 是额外炮塔索引，从 0 开始
BurstPerTurret=0                             ; 整数型，每个炮塔 Burst 多少次后会切换到下一个炮塔
```

```{note}
- 现在你可以让载具将自己的炮塔在多个位置进行绘制并开火，仅支持常规vxl载具
P.S.1: 单位开火时会首先使用原本的炮塔开火 `BurstPerTurret` 次，然后切换到0号额外炮塔开火 `BurstPerTurret` ，然后切换到 1 号，依此类推。开火时的 FLH 也是相对开火时炮塔的位置计算的，若单位的炮塔或炮管设置了制退效果，则会根据 `Burst` 自动计算炮塔炮管编号
P.S.2: 开火位置本质上是由当前的 `Burst` 决定的，如果你要让后面的炮塔开火，就得有 `Burst` 很高的武器才行。例如一个具有 2 个额外炮塔（共 3 个炮塔）的战列舰，`BurstPerTurret=3` ，那么需要一个至少 `Burst=9` 才能让所有炮塔完整开火一遍，炮管在每个炮塔上同理
P.S.3: 这本质上是为了复刻红警1的多炮塔，而不是帮助你在红警2里制作赞托斯，所以不同炮塔使用不同武器/分别索敌/分别计算冷却/分别具有生命值等都不在这个功能的考虑范围内，相关需求请使用替身/附加单位
```

---

#### 炮塔旋转行为拓展

  > - *pr #1480 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
ExpandTurretRotation=no                      ; 布尔值，是否开启以下的炮塔旋转相关功能

[AudioVisual]                                ; 视听通用设置
Turret.IdleRotate=no                         ; 布尔值，所有有炮塔的单位是否会在闲置时随机旋转炮塔
Turret.PointToMouse=no                       ; 布尔值，所有有炮塔的单位是否会在闲置或移动并且再被选中时旋转炮塔朝向鼠标位置（多人模式会自动禁用）
Turret.BodyFoundation=no                     ; 布尔值，所有有炮塔的单位是否会在旋转身体时同时跟随旋转炮塔
Turret.IdleRestartMin=150                    ; 整数型，单位在停止指令后至少多少帧会认为是处于闲置状态并开始旋转炮塔
Turret.IdleRestartMax=300                    ; 整数型，单位在停止指令后至多多少帧会认为是处于闲置状态并开始旋转炮塔
Turret.IdleIntervalMin=150                   ; 整数型，单位处在闲置状态下旋转炮塔后至少间隔多少帧会再次旋转炮塔
Turret.IdleIntervalMax=450                   ; 整数型，单位处在闲置状态下旋转炮塔后至多间隔多少帧会再次旋转炮塔

[SOMEVEHICLE]                                ; 任意 `Turret=yes` 的车辆
Turret.IdleRotate=                           ; 布尔值，当前单位是否会在闲置时随机旋转炮塔，默认值为 `AudioVisual` 中 `Turret.IdleRotate` 的值
Turret.PointToMouse=                         ; 布尔值，当前单位是否会在（闲置或仅移动中）且被选中时旋转炮塔朝向鼠标位置，默认值为 `AudioVisual` 中 `Turret.PointToMouse` 的值
Turret.Restriction=180.0                     ; 浮点型，炮塔可以向两侧转动的角度，若该值低于180.0则会在角度不合适时自动转动身体而不需要设置 `Turret.BodyOrientation` ，如果确实有需要设置 `Turret.BodyOrientation` ，则不合适的值可能导致问题
Turret.ExtraAngle=0                          ; 浮点型，炮塔额外的转动角度，同时 `Turret.Restriction` 的限制范围也会随该值进行转动，正数为顺时针方向，负数为逆时针方向，即炮塔应该用何种角度回正或是对着目标，0.0~180.0为左侧面对，-180.0~0.0为右侧面对
Turret.BodyFoundation=                       ; 布尔值，当前单位是否会在旋转身体时同时跟随旋转炮塔，默认值为 `AudioVisual` 中 `Turret.BodyFoundation` 的值
Turret.BodyOrientation=no                    ; 布尔值，身体是否需要在开火的时候对准目标，启用后以下两项才生效
Turret.BodyOrientationAngle=0                ; 浮点型，身体对准目标时额外的转动角度，正数为顺时针方向，负数为逆时针方向，即身体应该用何种角度对着目标，0.0~180.0为左侧面对，-180.0~0.0为右侧面对
Turret.BodyOrientationSymmetric=yes          ; 布尔值，身体对准目标时额外的转动角度是否左右均可，不必判断具体往哪一边，即身体既可以用左侧也可以用右侧对着目标
```

---

#### 矿车倒矿流程优化

  > - *pr #1476 by TaranDahl & CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEVEHICLE]                                ; 采矿车辆
HarvesterQuickUnloader=no                    ; 布尔值，矿车是否可以在矿场旁直接倒矿（就像星际和魔兽农民那样），手动倒矿仍然会使用原有的倒矿流程，暂时不考虑支持采矿步兵；超时空矿车在细节上可能跟原版有所不同，`Weeder` 逻辑理论上可以正常工作，这两个遇到问题请反馈
```

---

#### 禁止手动弹出载员

  > - 暂无pr *by Otamaa*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEVEHICLE]                                ; 任意车辆
NoManualEject=no                             ; 布尔值，是否禁止手动弹出载员，但允许使用其它部署行为
```

---

#### 移动攻击任务修复

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

- 仅修复，无ini
- 现在执行 `AttackMove` 的单位如果因为某些原因（比如 EMP ）进入了不正确的任务，那么会尝试恢复或退出 `AttackMove`

---

#### 不在地图上保持更新

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
UpdateInLimbo.Occupier=no                    ; 布尔值，驻军是否保持更新
UpdateInLimbo.NormalPassenger=no             ; 布尔值，非对外开火的载员是否保持更新
UpdateInLimbo.Parasite=no                    ; 布尔值，寄生虫是否保持更新
```

```{note}
- 在原版游戏中，当单位Limbo时会从更新名单中移除，诸如回血、AE等效果都会暂停
- 现在你可以让驻军、非对外开火的载员、寄生虫在Limbo后保持更新
P.S.1: 这个功能会带来额外的更新，消耗额外的资源，除非你知道自己在做什么，否则不建议开启这个功能
P.S.2: 我目前只能想到以上三个Limbo的调用会需要保持更新，如果有别的场景可以告诉我
```

---

#### 自定义解除隐身行为

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
DecloakDamagedTargets=yes                    ; 布尔值，受到伤害时是否解除隐身，之前已有弹头上的同名标签，此标签会被用作其默认值
Decloak.OnBlockingMovement=yes               ; 布尔值，阻挡敌方移动时是否解除隐身
Decloak.OnCloakingWithLowHealth=yes          ; 布尔值，血量较低时进入隐身，且比较倒霉时是否解除隐身
Decloak.OnCrushing=yes                       ; 布尔值，碾压成功时是否解除隐身
```

```{note}
- 解除了原本这些的硬编码，现在可以关闭这些行为了
```

---

#### 进入命令不停止路径点

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
StopPlanningOnEnter=yes                      ; 布尔值，下令进入载具/建筑等操作后是否终止路径点
```

---

#### 移动不打破超时空锁定

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMETECHNO]                                 ; 任意步兵、车辆或飞机
KeepWarping=no                               ; 布尔值，是否只有切换目标或者本体消失才会打破超时空锁定
KeepWarping.Distance=                        ; 整数型，与目标距离超过该距离的时候会打断超时空锁定，单位为格，默认使用 `CombatDamage` 中 `OpenToppedWarpDistance` 的值
```

---

#### 修复原超时空保持距离

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

- 仅修复，无ini
- 修复 `OpenToppedWarpDistance` 和 `KeepWarping.Distance` 与射程的计算方式不一致的问题，现在计算对大型建筑的保持距离时也有与射程相同的面积补正

---

#### 导弹子机发射位置拓展

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
MissileSpawnUseOtherFLHs=no                  ; 布尔值，是否能够使用除了Burst为0和1以外的其它FLH，进而通过 `XXXFLH.BurstN` 让它们使用其它FLH，导弹将会出现在那些位置上
```

```{note}
- 开启这个选项后就不要使用 `SecondSpawnOffset` 了
```

---

#### 区域警戒跟随目标本身

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
FollowTargetSelf=no                          ; 布尔值，是否只跟随单位本体而不是自动前往被跟随单位的目的地
```

---

#### 机场生产悬浮空军单位

  > - 暂无pr *by TaranDahl & CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEAIRCRAFT]                               ; 只能是飞机
ThisIsAJumpjet=                              ; VehicleType，这个飞机出厂时变成什么单位，需要有 `Locomotor=Jumpjet`
```

```{note}
- 你需要做的是：
  - 注册一架飞机 A ，不要将其添加到 `PadAircraft` 中，并写上 `AirportBound=no` ，在 A 上写 `ThisIsAJumpjet=B` ，然后注册一艘 `Locomotor=Jumpjet` 的载具 B ，最后在机场上写 `WeaponsFactory=no` ，在计算 A 的 `BuildLimit` （大于0）时，会计入 B 的数量
- 最终效果：
  - 当你建造飞机 A 时，机场会刷出一个 B 来；建造时间、造价等都是按 A 的来，而刷出位置、被克隆等都是按 B 的来
```

---

#### 载具中步兵装弹问题修复

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
InTransportInfantryAmmoFix=no                ; 布尔值，是否启用修复
```

```{note}
- 在原版游戏中，只有载具可以自动装弹，Ares 把这个效果扩展到了步兵和建筑，但是步兵在载具中无法装弹
P.S.1: Ares 的装弹挂在更新函数上，这意味着单位必须能更新才能装弹，各种会导致单位从地图上消失的效果中，只有进入能对外开火的载具会保持更新
P.S.2: 这个功能和 `ReloadInTransport` 采用不同的技术路线，不要共同使用
```

---

### 建筑

---

#### 集结点增强

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
RallyPointOnTechno=no                        ; 布尔值，是否能在步兵、车辆、飞机或建筑上设置集结点，默认情况下只有克隆缸可以
RallyPointForceMove=no                       ; 布尔值，往单位上设置集结点时是否不需要按住Alt就能设置
RallyPointAreaGuard=no                       ; 布尔值，产出的单位是否默认设置AreaGuard任务，如果有集结点则会跟随目标或保卫目标区域，矿车会去采矿

[SOMEBUILDING]                               ; 任意建筑
JustHasRallyPoint=no                         ; 布尔值，该建筑是否可以设置集结点
HasSecondaryRallyPoint=no                    ; 布尔值，该建筑是否可以设置第二个集结点

[SOMEAIRCRAFT]                               ; 任意飞机
IgnoreRallyPoint=no                          ; 布尔值，该飞机是否无视机场集结点
```

- 拥有快捷键功能，位于 [控制] 面板
- 可以直接将集结点设置在鼠标指针位置的单位/格子上

```{note}
- 关于2个功能快捷键：
  - 功能快捷键默认名为 `Assign the rally point` 与 `Assign 2nd rally point`，可以在csf中添加名为 `TXT_ASSIGN_RALLY_POINT` 与 `TXT_ASSIGN_2ND_RALLY_POINT` 和 `TXT_ASSIGN_RALLY_POINT_DESC` 与 `TXT_ASSIGN_2ND_RALLY_POINT_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
- 船厂暂不支持在步兵、车辆、飞机或建筑上设置集结点
- 建筑即使能够设置集结点，也不一定有实际作用
- 第二个集结点会显示为白色以作区分，用于配合 [自动化建筑放置](#自动化建筑放置) 使用的
```

---

#### 重工顺产优化

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
ExitCoord=                                   ; XYZ坐标，请写完整，（原有功能-增强）设置在建筑上时，产出的单位将会被默认放置在该位置，设置在产出单位上时，则会覆盖建筑的

[SOMEBUILDING]                               ; 任意建筑
JumpjetExitCoord=                            ; XYZ坐标，请写完整，产出的具有 `Locomotor=Jumpjet` 的单位将会被默认放置在该位置
```

```{note}
- 如果是战车工厂使用，则需要其有 `WeaponsFactory=no`
```

---

#### 碾压死亡增强

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[CombatDamage]                               ; 全局伤害设置
UseGlobalDeathWeaponDamage=no                ; 布尔值，无死亡武器且无武器单位是否使用全局死亡武器的Damage作为死亡武器伤害，默认使用的是血量/2
FireDeathWeaponOnCrushed=no                  ; 布尔值，被碾压死亡是否触发死亡武器

[General]                                    ; 全局通用设置
CrushBuildingOnAnyCell=no                    ; 布尔值，是否建筑的任意一格都可以被碾压，默认只有中心可以被碾压

[AudioVisual]                                ; 视听通用设置
NonVehExplodeOnDestroy=no                    ; 布尔值，非车辆单位通过Destroy方法死亡时是否触发Explosion和DeathAnim，哪些死亡方式调用了Destroy方法有待检查，但碾压是其中之一

[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
ExplodeOnDestroy=                            ; 布尔值，通过Destroy方法死亡时是否触发Explosion和DeathAnim，车辆单位默认为是，其余类型默认为 `AudioVisual` 中 `NonVehExplodeOnDestroy` 的值
FireDeathWeaponOnCrushed=                    ; 布尔值，被碾压死亡是否触发死亡武器，默认为 `CombatDamage` 中 `FireDeathWeaponOnCrushed` 的值
```

---

#### 集结点寻路优化

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEBUILDING]                               ; 只能是建筑
RallySpeedType=                              ; 整数型，Winged是4
RallyMovementZone=                           ; 整数型，Fly是9
```

```{note}
- `RallySpeedType` 和 `RallyMovementZone` 可以设置为其它值，请自行查阅yrpp
```

---

#### 修复载具克隆工厂

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

- 仅修复，无ini
- 修复 `CloningFacility` 和 `WeaponsFactory=no` 的不正确交互

---

#### 电磁脉冲暂停电脑生产

  > - *pr #1833 by ahasasjeb*
  > - [返回目录](#额外功能目录)

- 仅修复，无ini
- 修复 `EMP` 状态下的 AI 的工厂建筑仍会继续生产的问题

---

#### 重工和通行建筑自定义

  > - *pr #1812 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
ExtendedWeaponsFactory=no                    ; 布尔值，是否解除重工相关的所有硬编，并启用 `WeaponsFactory.Dir`

[SOMEBUILDING]                               ; 只能是建筑
Bib.Dir=2                                    ; 整数型，建筑的 `Bib` 在哪个方向
NumberImpassableRows.Dir=2                   ; 整数型，建筑的 `NumberImpassableRows` 在哪个方向
WeaponsFactory.Dir=2                         ; 整数型，重工生产的单位往哪个方向开出来
```

```{note}
- 三个微观定义方向的可选范围是：0-北（右上），2-东（右下），4-南（左下），6-西（左上）
```

---

#### 建筑建造范围边界拓展

  > - *pr #1478 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `ra2md.ini` 之中：

```ini
[Phobos]                                     ; Phobos设置
DrawAdjacentBoundary=no                      ; 布尔值，是否绘制建筑的建造判定边界
```

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
CheckExtraBaseNormal=no                      ; 布尔值，是否需要检测单位扩展建筑范围

[AudioVisual]                                ; 视听通用设置
PlacementGrid.Expand=no                      ; 布尔值，是否需要彩色化显示建造网格
PlacementGrid.LandFrames=1,0,0               ; 三维整数型，分别代表在非水上建造时，该格子处在 存在单位、超出范围或完全正常 状态时，显示的建筑格子在place.shp中的帧号（从0开始，由于不改变原有的红格设计，建议从尾部增加）
PlacementGrid.WaterFrames=1,0,0              ; 三维整数型，分别代表在水面上建造时，该格子处在 存在单位、超出范围或完全正常 状态时，显示的建筑格子在place.shp中的帧号（从0开始，由于不改变原有的红格设计，建议从尾部增加）

[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
ExtraBaseNormal=no                           ; 布尔值，该单位是否能扩展己方的建筑范围
ExtraBaseForAllyBuilding=no                  ; 布尔值，该单位是否能扩展其它友方的建筑范围

[SOMEBUILDING]                               ; 只能是建筑
Adjacent.AllowedExtra=                       ; Techno列表，该建筑能否被该单位拓展其建筑范围
Adjacent.DisallowedExtra=                    ; Techno列表，该建筑能否被该单位拓展其建筑范围
```

```{note}
- `ExtraBaseNormal` 和 `ExtraBaseForAllyBuilding` 支持所有类型单位，包括建筑
- 启用 `CheckExtraBaseNormal` 会增加少量性能消耗，启用 `PlacementGrid.Expand` 也会增加一定程度的性能消耗
```

---

#### 建筑建造和部署行为拓展

  > - *pr #1479 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `ra2md.ini` 之中：

```ini
[Phobos]                                     ; Phobos设置
DefaultPlacingDirection=0                    ; 整数型，摆放 `PlaceBuilding.Extra` 类建筑时默认的方向（在游戏开始和建筑摆放成功时设置为默认方向，中途取消时不会重置）
```

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
ExtendedBuildingPlacing=no                   ; 布尔值，是否允许建筑能在友方单位头顶建造，并尝试散开占据这些格子的友方单位

[SOMEBUILDING]                               ; 只能是建筑
LimboBuild=no                                ; 布尔值，该建筑从侧边栏被建造时，是否将成为虚拟建筑（会在完成建造后自动放置），如果需要给AI用，请不要给 [BuildPower] 里列出的建筑使用
LimboBuildID=-1                              ; 整数型，该建筑被建造成为虚拟建筑时的虚拟建筑ID
PlaceBuilding.Extra=no                       ; 布尔值，该建筑是否多态，开启后才能使用后面的四个选项，且开启后在放置此类建筑时，左键按下后不能移动位置，而是改为根据与中心格之间的方向，调整 `PlaceBuilding.OnLand`（中心格在陆地上）或 `PlaceBuilding.OnWater`（中心格在水面上）的具体类型（0方向位于地图右上角）
PlaceBuilding.OnLand=                        ; Building列表，定义当该建筑放置到陆地上会切换成哪些建筑
PlaceBuilding.OnWater=                       ; Building列表，定义当该建筑放置到水面上会切换成哪些建筑
PlaceBuilding.DirectionShape=                ; SHP文件名，需要后缀，当放置此类建筑时，以中心格为中心额外绘制的图形
PlaceBuilding.DirectionPalette=              ; PAL文件名，需要后缀，控制 `PlaceBuilding.DirectionShape` 使用的色盘
LaserFencePost.Fence=                        ; Building类型，只能是 `LaserFencePost=yes` 的建筑，若此处定义的建筑有 `LaserFence=yes` ，则会以此建筑替换默认使用的激光围墙类型（列表中第一个拥有 `LaserFence=yes` 的建筑类型），且使用不同激光围墙的激光围墙柱之间不会相互连接
CanBuildUnderUnits=no                        ; 布尔值，该建筑是否能在有步兵、车辆和飞机的格子上直接放置

[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
CanBeBuiltOn=no                              ; 布尔值，该类型是否不影响建筑建造且会在建筑在其头顶建造时直接消失
```

```{note}
- 启用 `ExtendedBuildingPlacing` 会在建筑拍下时略微增加性能消耗
- 如果所有 `LaserFencePost=yes` 的建筑都使用 `LaserFencePost.Fence` 时能够略微降低性能消耗
- `PlaceBuilding.OnLand` 和 `PlaceBuilding.OnWater` 仅对玩家生效
- 使用 `PlaceBuilding.OnLand` 和 `PlaceBuilding.OnWater` 时，不支持原建筑和这里列出的建筑中任意一方拥有 `PlaceAnywhere` 或使用 `LimboBuild` ，且双方必须拥有相同的 `BuildCat`
```

---

### 运动

---

#### 优化让路逻辑

  > - 暂无pr *by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
ExtendedScatterAction=no                     ; 布尔值，是否使用优化的让路逻辑
```

---

#### 高级驾驶移动方式

  > - *pr #1622 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEVEHICLE]                                ; 只能是车辆
Locomotor=AdvancedDrive                      ; 移动方式，以下功能仅在此移动方式下可用 {4A582751-9839-11d1-B709-00A024DDAFD1}
AdvancedDrive.Reverse=yes                    ; 布尔值，单位是否会倒车，启用后能够使用下面的五个选项
AdvancedDrive.Reverse.FaceTarget=yes         ; 布尔值，当单位指定了目标时，在上述距离内强制正面向敌进行移动，不再自动选择
AdvancedDrive.Reverse.FaceTargetRange=16.0   ; 浮点型，当单位指定了目标时，离目标距离多远内，会倒车来保持正面相对
AdvancedDrive.Reverse.MinimumDistance=2.5    ; 浮点型，当单位制定了目的地时，离目的地距离多远内，会自动选择倒车移动
AdvancedDrive.Reverse.RetreatDuration=150    ; 整数型，当单位制定了目的地时，距上次受到伤害这么长时间内，会倒车前往目的地
AdvancedDrive.Reverse.Speed=0.85             ; 浮点型，倒车时的速度倍率
AdvancedDrive.Hover=no                       ; 布尔值，单位是否会悬浮，启用后能够使用下面的六个选项
AdvancedDrive.Hover.Sink=yes                 ; 布尔值，单位是否会在水面上被动失去动力时沉没
AdvancedDrive.Hover.Spin=yes                 ; 布尔值，单位是否会在被动失去动力时旋转
AdvancedDrive.Hover.Tilt=yes                 ; 布尔值，单位是否会在斜坡上倾斜
AdvancedDrive.Hover.Height=                  ; 整数型，单位悬浮时的标准高度，默认值为 `General` 中 `HoverHeight` 的值
AdvancedDrive.Hover.Dampen=                  ; 浮点型，单位悬浮时的弹性程度，默认值为 `General` 中 `HoverDampen` 的值
AdvancedDrive.Hover.Bob=                     ; 浮点型，单位悬浮时的浮动间隔，默认值为 `General` 中 `HoverBob` 的值
```

```{note}
- 一般情况下，都会正面朝向移动方向
- 所有使用限制和 `Locomotor=Drive` 相同
- 同时，`Locomotor=AdvancedDrive` 相比 `Locomotor=Drive` 修复了在进行日字形移动时不会碾压到中间格的问题，且上下坡转动将更为顺滑
- 与 `DirectionalArmor` 和 `ExpandTurretRotation` 相性较佳，与 `KeepTargetOnMove` 和 `KeepRange` 同时使用则能提升操作手感
```

---

#### 钻地移动矿车修复

  > - *pr #1535 by Starkku*
  > - [返回目录](#额外功能目录)

- 仅优化，无ini
- 现在钻地移动方式的矿车能够找到不在同一片陆地上的矿石和矿场了

---

#### 自定义钻地决定距离

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
TunnelSimpleDistTooFar=12                    ; 整数型，目的地和钻地车位置的x方向或y方向上距离不低于此值，才会考虑钻地移动
TunnelPathingDistTooFar=15                   ; 整数型，钻地车若在地面上需要移动到目的地的距离不低于此值，才会考虑钻地移动
```

---

#### 钻地移动方式自定义

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEVEHICLE]                                ; 钻地车辆
DigStartROT=-1                               ; 整数型，开始钻地时低头的ROT，抬头总时间是64/DigStartROT帧，为非正数时使用默认算法
DigInSpeed=-1                                ; 整数型，往下钻时的速度，单位Lepton，为非正数时使用默认算法
DigOutSpeed=-1                               ; 整数型，往上钻时的速度，单位Lepton，为非正数时使用默认算法
DigEndROT=-1                                 ; 整数型，结束钻地时抬头的ROT，抬头总时间是64/DigEndROT帧，为非正数时使用默认算法
```

---

#### 飞机移动方式自定义

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEAIRCRAFT]                               ; 任意飞机
FlightClimb=-1                               ; 整数型，爬升时的速度，一旦设置所有爬升都用这个速度，-1使用默认算法
FlightCrash=-1                               ; 整数型，下降时的速度，一旦设置所有下降都用这个速度，-1使用默认算法
```

---

#### 悬浮空军单位智力恢复

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

- 仅优化，无ini
- 在原版游戏中，给移动中的Jumpjet步兵下令攻击时，它会首先停止移动；如果连续下令，那么它将在原地抽搐无法前进；这里通过跳过该停止移动函数来避免这个问题；除此之外，如果Jumpjet单位攻击目标在射程范围内，则会停止移动

```{note}
- 是否存在副作用需要测试
```

---

#### 悬浮空军单位速度类型

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMETECHNO]                                 ; 任意 `Locomotor=Jumpjet` 的步兵、车辆
JumpjetSpeedType=3                           ; 整数型，Winged是4，Hover是3
```

```{note}
- 原本游戏中JJ硬编码使用Hover速度类型判断格子是否可以前往。这主要影响JJ步兵，使得它们无法停在建筑上或悬崖上，现在你可以把它改成Winged以解决这一问题，需要注意的是：
  1. 似乎这个改动不会影响JJ载具，不过你可以直接给JJ载具设置Winged速度类型来规避这一问题，目前没有发现什么明显的副作用
  2. 如果你不使用这个功能，即使你直接给JJ步兵设置Winged速度类型也没用，而且还会导致它无法正确从未设置路径点的兵营里出来、无法被Ares投放超武投放之类的愚蠢问题
  3. 这个功能不会影响JJ的速度
- `JumpjetSpeedType` 可以设置为其它值，请自行查阅yrpp，不保证不会出现意料之外的问题
```

---

#### 悬浮空军单位寻路修正

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
BalloonHoverPathingFix=yes                   ; 布尔值，是否修正 `BalloonHover=yes` 单位的寻路
```

```{note}
修复的 `BalloonHover=yes` 的单位的寻路问题：
- 建筑和悬崖等位置现在可以作为目的地
- 当点击鼠标时其下达的指令不再只会被分散到同一区域（例如桥上和桥边被认为是两个区域）
```

---

### 索敌

---

#### 区域警戒优化

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
PlayerGuardModePursuit=yes                   ; 布尔值，对玩家有效，是否追击敌人，为否则会在超过归巢距离（即离开警戒目标多远就该往回跑）后立即触发归巢，为是则必须停下且没在开火才会归巢
PlayerGuardModeGuardRangeMax=16              ; 浮点型，对玩家有效，最大警戒距离，单位：格，当警戒目标为【可移动单位】时，警戒距离（即多远处出现目标就会跑去攻击）等同于 `GuardRange` ，当警戒目标为【空地或建筑】时，警戒距离等于：1.1 × min(GuardRange × 乘数 + 加数, 最大值)
PlayerGuardModeGuardRangeMultiplier=2.0      ; 浮点型，对玩家有效，警戒距离乘数
PlayerGuardModeGuardRangeAddend=0            ; 浮点型，对玩家有效，警戒距离加数，单位：格
PlayerGuardStationaryStray=-1                ; 浮点型，对玩家有效，警戒目标为【空地或建筑】时的归巢距离，单位：格
AIGuardModePursuit=yes                       ; 布尔值，对电脑有效，是否追击敌人，为否则会在超过归巢距离（即离开警戒目标多远就该往回跑）后立即触发归巢，为是则必须停下且没在开火才会归巢
AIGuardModeGuardRangeMax=16                  ; 浮点型，对电脑有效，最大警戒距离，单位：格，当警戒目标为【可移动单位】时，警戒距离（即多远处出现目标就会跑去攻击）等同于 `GuardRange` ，当警戒目标为【空地或建筑】时，警戒距离等于：1.1 × min(GuardRange × 乘数 + 加数, 最大值)
AIGuardModeGuardRangeMultiplier=2.0          ; 浮点型，对电脑有效，警戒距离乘数
AIGuardModeGuardRangeAddend=0                ; 浮点型，对电脑有效，警戒距离加数，单位：格
AIGuardStationaryStray=-1                    ; 浮点型，对电脑有效，警戒目标为【空地或建筑】时的归巢距离，单位：格

[SOMETECHNO]                                 ; 任意步兵或单位
PlayerGuardModePursuit=                      ; 布尔值，对玩家单位有效，是否追击敌人
PlayerGuardModeStray=                        ; 浮点型，对玩家单位有效，警戒目标为【可移动单位】时的归巢距离，默认值为 `General` 中 `GuardModeStray` 的值
PlayerGuardModeGuardRangeMultiplier=         ; 浮点型，对玩家单位有效，警戒距离乘数，默认值为 `General` 中 `PlayerGuardModeGuardRangeMultiplier` 的值
PlayerGuardModeGuardRangeAddend=             ; 浮点型，对玩家单位有效，警戒距离加数，单位：格，默认值为 `General` 中 `PlayerGuardModeGuardRangeAddend` 的值
PlayerGuardStationaryStray=                  ; 浮点型，对玩家单位有效，警戒目标为【空地或建筑】时的归巢距离，单位：格，默认值为 `General` 中 `PlayerGuardStationaryStray` 的值，当且仅当为-1时使用响应距离作为归巢距离
AIGuardModePursuit=                          ; 布尔值，对电脑单位有效，是否追击敌人
AIGuardModeStray=                            ; 浮点型，对电脑单位有效，警戒目标为【可移动单位】时的归巢距离，默认值为 `General` 中 `GuardModeStray` 的值
AIGuardModeGuardRangeMultiplier=             ; 浮点型，对电脑单位有效，警戒距离乘数，默认值为 `General` 中 `AIGuardModeGuardRangeMultiplier` 的值
AIGuardModeGuardRangeAddend=                 ; 浮点型，对电脑单位有效，警戒距离加数，单位：格，默认值为 `General` 中 `AIGuardModeGuardRangeAddend` 的值
AIGuardStationaryStray=                      ; 浮点型，对电脑单位有效，警戒目标为【空地或建筑】时的归巢距离，单位：格，默认值为 `General` 中 `AIGuardStationaryStray` 的值，当且仅当为-1时使用响应距离作为归巢距离
```

```{note}
- `General` 中的 `GuardModeStray` 是原有标签
```

---

#### 自动攻击优化

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
AIAttackIronCurtain=no                       ; 布尔值，AI是否攻击铁幕
PlayerAttackIronCurtain=yes                  ; 布尔值，玩家是否攻击铁幕

[CombatDamage]                               ; 全局伤害设置
PlayerReturnFire.Smarter=no                  ; 布尔值，是否优化 `PlayerReturnFire` 的判定，原版游戏会让单位在挨打时无论是否有命令都直接去攻击开火者，现在只有开火者位于射程内且具有比当前目标更高优先级时才会进行还击，而且仅将开火者设为目标而不会跑过去打
```

```{note}
- `PlayerReturnFire.Smarter` 在移动状态时可能存在问题，如果有问题请反馈
```

---

#### 攻击潜地单位

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; 任意抛体
AU=no                                        ; 布尔值，是否可以攻击潜地单位

[SOMEWARHEAD]                                ; 任意弹头
PlayAnimUnderground=no                       ; 布尔值，抛体在地下引爆时，引爆动画是否显示
PlayAnimAboveSurface=no                      ; 布尔值，抛体在地下引爆时，引爆动画是否在地面位置显示
```

---

#### 自动攻击围墙

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
PlayerDestroyWalls=no                        ; 布尔值，玩家单位是否自动攻击围墙
AutoTargetWalls=1                            ; 整数型，弹头是否会自动攻击围墙
EndAutoTargetingIfFindWalls=yes              ; 布尔值，自动索敌中找到围墙时是否立刻停止索敌
DestroyOwnerlessWalls=no                     ; 布尔值，自动攻击围墙的单位是否将所有非友军的围墙都作为目标，包括无所属方的和三家中立的

[SOMEWARHEAD]                                ; 任意弹头
AutoTargetWalls=                             ; 布尔值，该弹头是否会自动攻击围墙，`Wall=no` 的弹头无视此选项，默认值由 `General` 中 `AutoTargetWalls` 的值决定，大于0时为 `yes`，等于0时为 `no`，小于0时为 `WallAbsoluteDestroyer` 的值
```

```{note}
- 同时修复了攻击围墙时，有时墙被拆掉后仍然不会停火的问题
- 围墙实际上具有最低优先级（如果找到了单位就不会继续查找围墙），原本游戏中自动索敌找到围墙时立刻停止索敌可能应该算是 bug
```

---

#### 额外索敌时机

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
ExtraTargeting=no                            ; 布尔值，单位在一些事件点是否额外进行索敌，让单位能以更聪明的方式工作
```

```{note}
- 事件点如下：
  - 单位接收到停止命令时
  - `OpportunityFire=yes` 的单位接收到移动或采矿指令时
  - 可移动单位在执行 `AttackMove` 中的攻击任务中失去目标时
  - 建筑在攻击任务中失去目标时
  - 单位在执行其它可开火任务中失去目标时
  - 玩家单位受到伤害且 `PlayerReturnFire=yes` 时。这会替换本来的覆盖任务行为
- 如果在某些情况下导致单位不听话之类的问题，请反馈
```

---

#### 血量检测增强

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[CombatDamage]                               ; 全局伤害设置
VHPScan.Enhanced=no                          ; 布尔值，是否让 `VHPScan` 以更聪明的方式工作
```

```{note}
- 原本单位会在索敌和开火时改变目标的预估生命值，现在仅会在开火时改变
- 如果单位的预估生命值小于等于 0 ，原本不会被 `VHPScan=Strong` 的单位索敌，现在会使单位的仇恨降至 `1/10` 而非完全不会被索敌
- 如果单位在开火前目标的预估生命值已经小于等于 0 ，那么 `VHPScan=Strong` 的单位会清除目标
  - 如果启用了 `ExtraTargeting` ，那么此时会自动索敌一次
- 如果在某些情况下导致单位不听话之类的问题，请反馈
```

---

#### 维修子机修复

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMETECHNO]                                 ; 任意 `Spawns` 有效的步兵、车辆、飞机或建筑
Spawner.ReturnOnRepairDone=no                ; 布尔值，子机在维修的单位修理完毕后是否能自动重置目标
```

---

#### 方向威胁系数

  > - *pr #1480 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
TargetExtraThreat=no                         ; 布尔值，是否启用方向威胁系数
TargetExtraThreat.Angles=                    ; 浮点型列表，角度从小到大填入，当朝向与目标方向之间的夹角开始小于其中某个值时，威胁系数会乘以 `TargetExtraThreat.Multipliers` 中对应的值
TargetExtraThreat.Multipliers=               ; 浮点型列表，与 `TargetExtraThreat.Angles` 配合使用，可以比它多一项，代表超过最大角度时的威胁值系数
TargetExtraThreat.Turret=yes                 ; 布尔值，朝向是否检查炮塔的朝向
```

```{note}
- 目标最终威胁为 0 时，单位不会主动攻击目标
- 如果想要和 `Turret.Restriction` 配合使用，那么设置 `TargetExtraThreat.Turret=no` 效果最好
```

---

#### 被索敌额外仇恨

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
CanAttackMeThreatBonus=0.0                   ; 浮点型，目标可以攻击索敌者时具有的额外仇恨
```

```{note}
- 现在你可以让单位在进行仇恨计算时考虑目标是否可能攻击到索敌者，如果可以的话则具有额外仇恨，仇恨具体算法参考modenc
- 对于建筑目标而言，索敌者处于其射程内时才被认为可以攻击到索敌者
```

---

### 武器

---

#### 位移武器

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEWEAPON]                                 ; 任意 `LimboLaunch=yes` 的武器
UnlimboDetonate=no                           ; 布尔值，是否在抛射体引爆时将被 `LimboLaunch=yes` 藏起来的单位拿出来，如果单位不是由 `LimboLaunch=yes` 藏起来的则不会生效，最终效果类似于单位随抛体移动过去
UnlimboDetonate.Force=no                     ; 布尔值，是否无视障碍而强制将单位在抛射体引爆处拿出来，为否且目标位置被阻挡则会在附近拿出来
```

```{note}
- `UnlimboDetonate` 不能和 `Parasite` 一起使用
```

---

#### 追踪激光

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEWEAPON]                                 ; 任意武器
Laser.IsTracking=no                          ; 布尔值，是否让激光一直持续并跟随开火者和目标单位，就像红警3中的冷冻或星际2里的虚空那样
```

```{note}
- 清除某些目标时可能存在问题，遇到请反馈
- 相比于原本游戏中1帧持续时间的激光和1ROF的武器，性能开销较低
```

---

#### 扫射增强

  > - *pr #1834 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEWEAPON]                                 ; 任意 `Strafing=yes` 的武器
Strafing.TargetCell=no                       ; 布尔值，飞机在扫射开火后是否将本轮扫射的目标更改为地面，可以防止因目标死亡扫射过程被打断的问题
```

---

#### 圆柱形射程

  > - *pr #1345 by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
CylinderRangefinding=no                      ; 布尔值，是否使用圆柱形射程计算，空军单位必定开启

[SOMEWEAPON]                                 ; 任意武器
CylinderRangefinding=                        ; 布尔值，是否使用圆柱形射程计算，默认值为 `General` 中 `CylinderRangefinding` 的值
```

---

#### 武器附加效果

  > - *pr #1482 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEWEAPON]                                 ; 任意武器
AttachEffect.AttachTypes=
AttachEffect.CumulativeRefreshAll=no
AttachEffect.CumulativeRefreshAll.OnAttach=no
AttachEffect.CumulativeRefreshSameSourceOnly=yes
AttachEffect.RemoveTypes=
AttachEffect.RemoveGroups=
AttachEffect.CumulativeRemoveMinCounts=
AttachEffect.CumulativeRemoveMaxCounts=
AttachEffect.DurationOverrides=
```

```{note}
- 懒得写了，和弹头的类似，公版说明书都有
- 在开火后抛体生成的瞬间附加或移除，即会先判断 `AttachEffect.RequiredTypes` 等等条件
```

---

### 弹头

---

#### 合并范围伤害

  > - *pr #1504 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[CombatDamage]                               ; 全局伤害设置
MergeBuildingDamage=no                       ; 布尔值，所有弹头是否合并对建筑的范围伤害，只结算一次

[SOMEWARHEAD]                                ; 任意弹头
MergeBuildingDamage=                         ; 布尔值，该弹头是否合并对建筑的范围伤害，只结算一次，默认值为 `CombatDamage` 中 `MergeBuildingDamage` 的值
```

```{note}
- 功能与 `CellSpread.MaxAffect` 不同
- 由于取整问题，可能导致伤害在一定程度上提升
```

---

#### 杀伤矿石弹头

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEWARHEAD]                                ; 任意弹头
ReduceTiberium=no                            ; 布尔值，是否伤害矿石（原版 `Tiberium` 用于控制弹头是否能杀伤矿石，但只有具有 `ChainReaction` 的矿石会被伤害，新标签可用于伤害任何矿石，受到伤害的格子里矿石的量将会减少抛射体伤害/10，伤害范围使用CellSpread，对矿石的伤害不会因为远离爆心而递减）
```

---

#### 仅伤害目标弹头

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEWARHEAD]                                ; 任意弹头
NoCellSpread=no                              ; 布尔值，是否仅伤害目标本身
NoCellSpread.SnapDistance=0.5                ; 浮点型，引爆时距离目标多远算作命中目标从而会对目标造成伤害，单位为格
```

```{note}
- 在原本游戏中，`CellSpread=0` 的弹头会伤害格子上的建筑和任何位置与引爆坐标相同的单位，现在你可以让弹头只伤害主目标
- 虽然是弹头上的标签，但也必须经过抛射体引爆阶段才能生效
```

---

#### 改变地图光照弹头

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEWARHEAD]                                ; 任意弹头
LightChanging=no                             ; 布尔值，是否启用光照弹头（改变的是默认光照，不会放个核弹就变回去了，且应该会比核弹性能好一点，但是仍然不应该高频调用）
SetAmbientLight=-1                           ; 整数型，光照强度，负数使用当前值（参考地图光照参数）
SetAmbientRed=-1                             ; 整数型，红光强度，负数使用当前值（参考地图光照参数）
SetAmbientGreen=-1                           ; 整数型，绿光强度，负数使用当前值（参考地图光照参数）
SetAmbientBlue=-1                            ; 整数型，蓝光强度，负数使用当前值（参考地图光照参数）
```

---

#### 弹头爆炸范围增强

  > - *pr #1334 by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEWARHEAD]                                ; 任意弹头
CellSpread.Cylinder=no                       ; 布尔值，计算弹头的影响范围是否使用圆柱形替代原本的球形，部分情况下无效
AffectsInAir=yes                             ; 布尔值，弹头是否影响位于空中的单位（高度高于208 lepton）
AffectsOnFloor=yes                           ; 布尔值，弹头是否影响位于地上的单位
AffectsUnderground=no                        ; 布尔值，（未提交pr功能）弹头是否影响位于地下的单位
```

```{note}
- Ares的 `DamageAirThreshold` 作用是“如果弹头爆炸高度高于该值，才会影响空军”，和本功能没有冲突且独立运作；举例来说，如果使用圆柱形AOE计算但弹头爆在地上并且没有设置Ares的flags，那么照样不会伤害空军
```

---

#### 解除心控弹头扩展

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEWARHEAD]                                ; 任意 `RemoveMindControl=yes` 弹头
RemoveMindControl.OnVictim=yes               ; 布尔值，命中被心控单位是否解除其被控连接
RemoveMindControl.OnController=no            ; 布尔值，命中心控单位是否解除其控制连接
```

---

#### 禁用围墙递归受伤

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[CombatDamage]                               ; 全局伤害设置
DamageWallRecursivly=yes                     ; 布尔值，围墙受伤时是否会递归地伤害周围围墙
```

```{note}
- 在原版游戏中，当围墙受伤时，它会对周围4格的围墙造成200点伤害，这会导致围墙仅剩单独一格时耐久度显著增加。在关闭这一行为后，所有围墙的耐久度都将和原本仅剩一格时一致，在这种情况下，建议重新调整围墙生命值
```

---

### 抛射体

---

#### 抛体额外弹头

  > - *pr #1582 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; 任意抛体
PassDetonate=no                              ; 布尔值，启用穿透伤害，即抛体每隔一段时间在抛体所在的位置引爆一个弹头
PassDetonateWarhead=                         ; Warhead类型，穿透伤害使用的弹头，不填则默认使用抛体本身的弹头
PassDetonateDamage=                          ; 整数型，穿透伤害撞击伤害的伤害量，不填则默认使用抛体本身的伤害
PassDetonateDelay=1                          ; 整数型，穿透伤害每隔这么多帧才能触发一次，最低为1
PassDetonateInitialDelay=0                   ; 整数型，穿透伤害在抛体发射后多少帧第一次触发，即穿透伤害触发的初始延迟
PassDetonateLocal=no                         ; 布尔值，穿透伤害的每帧引爆的弹头和最终引爆本体的弹头是否位于地面高度
ProximityImpact=0                            ; 整数型，撞击伤害次数，即抛体在检测到周围有单位时在单位所在的位置【额外】引爆一个弹头，为0代表不启用该逻辑，负数视为无限，最后一次触发是在其位置【额外】引爆一个弹头的同时引爆【本身】
ProximityWarhead=                            ; Warhead类型，撞击伤害使用的弹头，可以让武器的 `Warhead` 仅使用一个索敌用的低补正假弹头来播放动画或是防止对抛体本身的目标造成二次伤害（即撞击弹头和武器弹头），不填则默认使用抛体本身的弹头
ProximityDamage=                             ; 整数型，撞击伤害的伤害量，不填则默认使用抛体本身的伤害
ProximityRadius=0.7                          ; 浮点型，撞击伤害触发的范围，不支持负数（不建议设置太大）
ProximityDirect=no                           ; 布尔值，撞击伤害是否不引爆弹头而直接对目标造成伤害（不影响本体弹头引爆）
ProximityMedial=no                           ; 布尔值，撞击伤害是否不在目标位置引爆，而是直接在抛体位置引爆，若 `ProximityDirect` 为是，则该项只会影响 `Trajectory.EdgeAttenuation` 对伤害的计算
ProximityAllies=no                           ; 布尔值，撞击伤害是否对盟友生效
ProximityFlight=no                           ; 布尔值，撞击伤害对空中单位是否生效
PassThroughVehicles=yes                      ; 布尔值，抛体是否不会被车辆或飞机阻挡，为否即在检测到车辆或飞机时在其位置【额外】引爆弹头并同时引爆【本身】，且此时 `ProximityImpact` 不会对除障碍物以外任何车辆或飞机生效，不影响索敌
PassThroughBuilding=yes                      ; 布尔值，抛体是否不会被建筑阻挡，为否即在检测到建筑时会其位置【额外】引爆弹头并同时引爆【本身】，且此时 `ProximityImpact` 不会对除障碍物以外任何建筑生效，不影响索敌
DamageEdgeAttenuation=1.0                    ; 浮点型，射程边缘修正比，抛体飞行到最大射程造成的伤害与距离一格内造成伤害的【比例】，之间是线性的，不支持负数（包括自身伤害、`PassDetonateDamage` 和 `ProximityDamage`）
DamageCountAttenuation=1.0                   ; 浮点型，伤害次数衰减率，即每次造成伤害后下一次造成伤害会是本次伤害的多少倍，不支持负数（包括自身伤害、`PassDetonateDamage` 和 `ProximityDamage`）
```

```{note}
- 注意 `PassDetonateWarhead` 和 `ProximityWarhead` 中使用的弹头必须注册
- 除非必要，否则请给 `ProximityRadius` 设置一个比较小的值，过大的值会显著增加性能消耗
```

```{warning}
- 该功能为面向 [弹道抛体系统](#弹道抛体系统) 设计，如在原版抛体中使用时出现问题，请替换为新的弹道抛体
```

---

#### 抛体扩散武器

  > - *pr #1582 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; 任意抛体
UseDisperseCoord=no                          ; 布尔值，抛体的发射位置是否使用扩散它的上级扩散抛体的发射位置替换固定位置，发射位置可以嵌套继承
DisperseWeapons=                             ; Weapon列表，抛体的扩散武器
DisperseBursts=                              ; 整数型列表，抛体每次各扩散几个对应的武器，如果数量少于 `DisperseWeapons` ，将使用该列表的最后一项
DisperseCounts=                              ; 整数型列表，抛体需要各扩散几次对应武器，为零会直接计算冷却而不发射，负数视为无限，如果数量少于 `DisperseWeapons` ，将使用该列表的最后一项
DisperseDelays=                              ; 整数型列表，抛体在发射对应次数后需要等待多久才能进行下一次发射，至少为1，如果数量少于 `DisperseWeapons` ，将使用该列表的最后一项
DisperseCycle=0                              ; 整数型，抛体能够发射几轮武器，为零不会发射武器，负数视为无限
DisperseInitialDelay=0                       ; 整数型，抛体在发射后多少帧第一次扩散武器，即扩散武器的初始延迟
DisperseEffectiveRange=0                     ; 浮点型，抛体扩散武器计时器会在距离目标这么近的距离时才会开始计时，为0代表不启用该逻辑，为负数代表只有在即将引爆本体的瞬间扩散最多一次武器
DisperseSeparate=no                          ; 布尔值，抛体每次扩散武器时，是否不再一次性发射列表中的所有武器，改为按列表顺序发射列表中的一组武器，遵循其 `DisperseBurst` ，即：
;                                            ; 是则，按顺序发射一组 `DisperseWeapons` ，发射这组武器对应 `DisperseBursts` 中的数量，等待的冷却为这组武器对应 `DisperseDelays` 中的时间，
;                                            ; 在发射这组武器对应的 `DisperseCounts` 中的次数后，切换到下一组 `DisperseWeapons` 并继续；
;                                            ; 否则，发射所有 `DisperseWeapons` ，每种武器发射该武器对应 `DisperseBursts` 中的数量，等待的冷却为此时次数对应 `DisperseDelays` 中的时间，
;                                            ; 在发射此时次数对应的 `DisperseCounts` 中的次数后，切换到下一组 `DisperseCounts` 并继续，两者均会重复 `DisperseCycle` 中的轮次数
DisperseRetarget=no                          ; 布尔值，抛体每次扩散武器时，扩散的武器是否自行寻找新目标，寻找目标的范围使用武器的 `Range`, 会读取 `DisperseWeapons` 的 `CanTarget` 并判断攻击目标所需要的 `AttachedEffects` 是否满足
DisperseLocation=no                          ; 布尔值，抛体每次扩散武器时，扩散的武器在自行寻找新目标时，是否以扩散位置为中心的位置寻找，否则以原目标为中心位置寻找
DisperseTendency=no                          ; 布尔值，抛体每次扩散武器时，扩散的武器中每组的第一个武器是否必定会选择原目标
DisperseHolistic=no                          ; 布尔值，抛体每次扩散武器时，扩散的武器是否选择和原目标不同状态（在空中和在地上两种状态）的目标
DisperseMarginal=no                          ; 布尔值，抛体每次扩散武器时，扩散的武器是否会选择数木、路灯或可拦截抛体等物体作为次要目标
DisperseDoRepeat=no                          ; 布尔值，抛体每次扩散武器时，扩散的武器是否会因为搜索到的目标数量不足而尝试攻击相同目标，即：
;                                            ; 是则，当武器可以攻击单位和地面时，优先攻击单位，然后开始尝试重复攻击单位，只有当本轮没有任何可被攻击的单位存在于范围内时才会尝试攻击地面；
;                                            ; 否则，当武器可以攻击单位和地面时，优先攻击单位，然后尝试攻击所有可被攻击的地面格子，若本轮所有可选目标都已被锁定而仍能发射，则会停止发射剩余武器
DisperseSuicide=yes                          ; 布尔值，抛体本体是否在扩散武器次数耗尽后自爆，原本没有次数时无效
DisperseFromFirer=                           ; 布尔值，抛体发射的扩散武器是否由单位朝抛体发射，否则是由抛体朝目标发射，不同弹道类型使用不同默认值
;                                            ; Straight - 否
;                                            ; Bombard - 否
;                                            ; Missile - 否
;                                            ; Engrave - 是
;                                            ; Parabola - 否
;                                            ; Tracing - 是
DisperseFaceCheck=no                         ; 布尔值，抛体在发射武器前是否检查图像朝向，如果 `Trajectory.BulletFacing=Velocity` 或 `Trajectory.BulletFacing=Spin` 时无视该项强制不检查
DisperseForceFire=yes                        ; 布尔值，抛体本身在没有目标或目标超出射程时是否仍然发射扩散武器
DisperseCoord=0,0,0                          ; FLH坐标，请写完整，从抛体位置发射的武器的具体位置会叠加该位置，可以视作是抛体发射武器的FLH（即需要 `DisperseFromFirer=no`）
```

```{warning}
- 该功能为面向 [弹道抛体系统](#弹道抛体系统) 设计，如在原版抛体中使用时出现问题，请替换为新的弹道抛体
```

```{note}
- 注意 `DisperseWeapons` 中使用的武器必须注册，且尽管支持嵌套使用，但请合理运用
- 如果你设置了 `DisperseRetarget=yes` ，那么同样的，请给 `DisperseWeapons` 中所列出的所有武器的 `Range` 设置一个比较小的值，过大的值会显著增加性能消耗
- 尽管 `DisperseDoRepeat=no` 会禁用重复选择目标，但若武器能够攻击地面，则仍然可能因为锁定到目标所处的格子而攻击到重复目标
```

---

#### 抛体生命周期

  > - *pr #1582 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; 任意抛体
LifeDuration=0                               ; 整数型，抛体能够存在的时间，单位为帧，不同弹道类型存在不同含义
;                                            ; Straight - 抛体能够存在的时间，时间结束时抛体会引爆，如果为非正数，则不计时
;                                            ; Bombard - 抛体能够存在的时间，时间结束时抛体会引爆，如果为非正数，则不计时
;                                            ; Missile - 抛体能够存在的时间，时间结束时抛体会引爆，如果为非正数，则不计时
;                                            ; Engrave - 切割过程的持续时间，为非正数则会根据 `Trajectory.Engrave.SourceCoord` 和 `Trajectory.Engrave.TargetCoord` 自动计算时间，此时 `Trajectory.Engrave.TargetCoord` 可视作切割线段的终点坐标
;                                            ; Parabola - 抛体能够存在的时间，时间结束时抛体会引爆，如果为非正数，则不计时
;                                            ; Tracing - 抛体锁定的持续时间，时间结束时抛体会引爆，如果为零，则使用武器 `ROF` - 10 作为持续时间，持续时间最低为1，如果为负数，则不计时
NoTargetLifeTime=-1                          ; 整数型，抛体失去目标闲置时的容忍时间，抛体在失去目标超过这么长时间后会引爆，若为0，则会在切换目标时直接引爆
CreateCapacity=-1                            ; 整数型，设置为非负数时，只有当发射者发射的弹道抛体在地图上的数量小于该值时才能发射该弹道抛体
ApplyRangeModifiers=no                       ; 布尔值，抛体是否会受到射程AE的影响，抛体 `LifeDuration` 、`Trajectory.DetonationDistance` 和 `Trajectory.EdgeAttenuation` 的计算是否会受到射程AE的影响
PeacefulVanish=                              ; 布尔值，抛体在最后引爆时是否直接消失，没有动画和伤害，默认值为 `Trajectory=Engrave` 或 `ProximityImpact` 不等于0 或 `DisperseCycle` 不等于0
```

```{warning}
- 该功能为面向 [弹道抛体系统](#弹道抛体系统) 设计，如在原版抛体中使用时出现问题，请替换为新的弹道抛体
```

---

#### 抛体重新索敌

  > - *pr #1582 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; 任意抛体
RetargetRadius=0                             ; 浮点型，抛体本体在失去目标后重新搜索新目标的半径，不同弹道类型存在不同动作
;                                            ; Straight - 仅更换目标，以原目标位置为圆心搜索
;                                            ; Bombard - 仅更换目标，以原目标位置为圆心搜索
;                                            ; Missile - 更换目标，转向目标，若未到达过 `Trajectory.OffsetCoord` ，则以原目标最后的坐标为圆心，否则以自身前方这个距离的坐标为圆心，为0代表不启用重新搜索，会攻击原本的位置，为负数则会直接原地自爆，当原目标在空中时，新目标也只会在空中寻找，当原目标在地上时，新目标也只会在地上寻找
;                                            ; Engrave - 仅更换目标，以原目标位置为圆心搜索
;                                            ; Parabola - 仅更换目标，以原目标位置为圆心搜索
;                                            ; Tracing - 更换目标，转向目标，以抛体当前位置为圆心搜索
RetargetInterval=1                           ; 整数型，每次重新搜索新目标时，需要离上一次搜索多久时间才能搜索
RetargetHouses=enemies                       ; 所属枚举类，每次重新搜索新目标时，目标应该所属于哪一方，包括 none|owner/self|allies/ally|team|enemies/enemy|all
Synchronize=no                               ; 布尔值，抛体的目标是否同步发射者的目标，如果不同步，抛体不会直接更新目标，优先级高于 `RetargetRadius` ，不同弹道类型存在不同动作
;                                            ; Straight - 仅更换目标
;                                            ; Bombard - 仅更换目标
;                                            ; Missile - 更换目标，转向目标
;                                            ; Engrave - 仅更换目标
;                                            ; Parabola - 仅更换目标
;                                            ; Tracing - 更换目标，转向目标
```

```{note}
- 除非必要，否则请给 `RetargetRadius` 设置一个比较小的值，过大的值会显著增加性能消耗
```

```{warning}
- 该功能为面向 [弹道抛体系统](#弹道抛体系统) 设计，如在原版抛体中使用时出现问题，请替换为新的弹道抛体
```

---

#### 弹道抛体系统

  > - *pr #1582 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; 弹道抛体（不和原版四类抛体共存）
; ------------------------------------------ ; 弹道抛体的弹道类型
Trajectory=                                  ; Trajectory类型，包括 Straight/Bombard/Missile/Engrave/Parabola/Tracing
; ------------------------------------------ ; 弹道抛体的通用基本功能
Trajectory.Speed=100.0                       ; 浮点型，弹道抛体的速度，不同弹道类型存在不同含义
;                                            ; Straight - 抛体的速度
;                                            ; Bombard - 抛体在上升阶段的速度
;                                            ; Missile - 抛体在 `Trajectory.Missile.UniqueCurve=no` 的情况下时的最终速度
;                                            ; Engrave - 抛体在切割过程的水平移动速度（最高速度限制在128）
;                                            ; Parabola - 抛体在水平方向上的速度，且仅用于模式0、模式3和模式5
;                                            ; Tracing - 抛体在追击目标时的速度
Trajectory.BulletROT=0                       ; 整数型，抛体图像的转动速度，不影响移动方向，为0时会始终朝向 `Trajectory.BulletFacing` 中的方向，分别有不同的作用
Trajectory.BulletFacing=Velocity             ; 面向模式枚举类，抛体的面向模式，包括 Velocity/Spin/Stable/Target/Destination/FirerBody/FirerTurret
Trajectory.BulletFacingOnPlane=no            ; 布尔值，抛体的面向是否只会处在水平面上
Trajectory.OffsetCoord=0,0,0                 ; FLH坐标，请写完整，抛体目标位置的额外偏移，抛体会瞄准目标的这个相对坐标进行攻击，支持在此基础上启用随机散布
;                                            ; Straight - 有效
;                                            ; Bombard - 有效
;                                            ; Missile - 有效
;                                            ; Parabola - 有效
Trajectory.RotateCoord=0                     ; 浮点型，是否按照武器的 `Burst` 在以 `Trajectory.OffsetCoord` 为角平分线的角度内旋转抛体发射方向，为0代表不启用该逻辑（负值会反转旋转方向）
;                                            ; Straight - 有效
;                                            ; Bombard - 有效
;                                            ; Missile - 有效，但 `Trajectory.OffsetCoord` -> `Trajectory.Missile.PreAimCoord`
;                                            ; Parabola - 有效
Trajectory.MirrorCoord=yes                   ; 布尔值，是否镜像坐标，如果发射者开火时当前武器的 `Burst` 数为偶数，则 `Trajectory.OffsetCoord` 中的L会镜像翻转，同时 `Trajectory.RotateCoord` 计算的旋转方向也会反转，每侧相邻抛体间的旋转角度不变
;                                            ; Straight - 发射方向受此影响
;                                            ; Bombard - 发射方向受此影响
;                                            ; Missile - `Trajectory.OffsetCoord` 和 `Trajectory.Missile.PreAimCoord` 都会受此影响
;                                            ; Engrave - 与其它不同，抛体的 `Trajectory.Engrave.SourceCoord` 和 `Trajectory.Engrave.TargetCoord` 中的L方向是否会随当前的 `Burst` 计数镜像翻转
;                                            ; Parabola - 发射方向受此影响
;                                            ; Tracing - 与其它不同，抛体的 `Trajectory.Tracing.CreateCoord` 中的L方向是否会随当前的 `Burst` 计数镜像翻转，若 `Trajectory.Tracing.TraceMode=Connection` 则 `Trajectory.Tracing.AttachCoord` 也会
Trajectory.AxisOfRotation=0,0,1              ; FLH坐标，请写完整，计算 `Trajectory.RotateCoord` 时的旋转轴，旋转轴会随单位朝向或目标位置到发射位置的向量旋转，长度不重要，只看方向（相反的向量也会反转旋转方向）
;                                            ; Straight - 有效
;                                            ; Bombard - 有效
;                                            ; Missile - 有效
;                                            ; Parabola - 有效
Trajectory.LeadTimeCalculate=no              ; 布尔值，抛体在瞄准时是否计算目标的提前量
;                                            ; Straight - 有效，计算目标的提前量会在开火时延迟1帧发射，同时若抛体的速度过低，则会无法生效
;                                            ; Bombard - 有效，若 `Trajectory.Bombard.NoLaunch=yes` 且 `Trajectory.Bombard.FreeFallOnTarget=yes` 则会同时影响到下降阶段的初始位置，若 `Trajectory.Bombard.NoLaunch=no` 则计算目标的提前量会在开火时延迟1帧发射，同时若抛体的速度过低，则会无法生效
;                                            ; Missile - 有效，抛体在运动过程中是否持续计算目标的提前量，且默认值为 `yes`
;                                            ; Parabola - 有效，计算目标的提前量会在开火时延迟1帧发射且增加性能消耗，同时若抛体的速度过低，则会无法生效
Trajectory.LeadTimeMaximum=0                 ; 整数型，抛射体最多能够预测目标还会继续运动多久（用于让抛射体不会飞得太远）
Trajectory.DetonationDistance=0.4            ; 浮点型，抛体距离目标多远距离就会引爆
;                                            ; Straight - 有效，当抛体 `Trajectory.Straight.PassThrough=yes` 时，则若此值为正则抛体在距离【发射者】这个值的距离的位置引爆，为负则抛体在穿过【目标点】这个值的绝对值的位置引爆，为零视为无穷远
;                                            ; Bombard - 有效，若为正数，代表抛体在距离目标多远距离内就会引爆，若为负数，代表若抛体目标可以移动则会将目标直接丢失转为攻击其所在位置的地面格子，若为零则关闭功能
;                                            ; Missile - 有效，抛体在距离目标多远距离就会引爆
;                                            ; Parabola - 有效，若为正数，代表抛体在距离目标多远距离内就会引爆，若为负数，代表若抛体目标可以移动则会将目标直接丢失转为攻击其所在位置的地面格子，若为零则关闭功能
Trajectory.TargetSnapDistance=0.5            ; 浮点型，抛体引爆时，当前目标在多近的距离内弹头会吸附过去
;                                            ; Straight - 有效
;                                            ; Bombard - 有效
;                                            ; Missile - 有效
;                                            ; Parabola - 有效
Trajectory.EarlyDetonation=no                ; 布尔值，抛体在 `Trajectory.DetonationHeight` 启用时，是否在上升段高于该高度时引爆，否则在下降段低于该高度时引爆
;                                            ; Bombard - 有效
;                                            ; Parabola - 有效
Trajectory.DetonationHeight=-1               ; 整数型，抛体位于什么高度时提前引爆，与 `Trajectory.EarlyDetonation` 组合使用，为负数时不生效
;                                            ; Bombard - 有效
;                                            ; Parabola - 有效
Trajectory.AllowFirerTurning=yes             ; 布尔值，抛体在发射后是否允许发射者的朝向大幅度变动
;                                            ; Engrave - 有效
;                                            ; Tracing - 有效
Inaccurate=no                                ; 布尔值，抛体是否启用随机散布，不同弹道类型存在不同含义
;                                            ; Straight - 抛体在目标位置的随机散布
;                                            ; Bombard - 抛体在目标位置的随机散布
;                                            ; Missile - 抛体在目标位置的随机散布
;                                            ; Engrave - 无作用
;                                            ; Parabola - 抛体在目标位置的随机散布
;                                            ; Tracing - 无作用
BallisticScatter.Min=0                       ; 浮点型，最小散布范围，每武器 `Range` 的攻击距离会使最小散布距离中心几格
BallisticScatter.Max=                        ; 浮点型，最大散布范围，每武器 `Range` 的攻击距离会使最大散布距离中心几格，默认值为 `CombatDamage` 中 `BallisticScatter` 的值
Gravity=6.0                                  ; 浮点型，抛体所受重力大小，不同弹道类型存在不同含义
;                                            ; Straight - 无作用
;                                            ; Bombard - 抛体在下落阶段所受的重力大小
;                                            ; Missile - 抛体所受的重力大小
;                                            ; Engrave - 无作用
;                                            ; Parabola - 抛体所受的重力大小
;                                            ; Tracing - 无作用
SubjectToGround=no                           ; 布尔值，开火前是否对前方直线的地形进行检查防止穿过地下，不同弹道类型存在不同含义
;                                            ; Straight - 除了是否检查外，还会控制抛体低于地面高度时是否立刻引爆
;                                            ; Bombard - 除了是否检查外，还会控制抛体在上升阶段低于地面高度时是否立刻引爆，而下降阶段必定引爆
;                                            ; Missile - 仅是否检查，抛体低于地面高度时必定引爆
;                                            ; Engrave - 仅是否检查，抛体低于地面高度时不受影响
;                                            ; Parabola - 仅是否检查，抛体低于地面高度时必定引爆
;                                            ; Tracing - 仅是否检查，抛体低于地面高度时不受影响
```

- `Trajectory.BulletFacing` 可选类型：

| 类型          | 模式               | 简述                                                                                                                         |
|:--------------|:-------------------|:-----------------------------------------------------------------------------------------------------------------------------|
| *Velocity*    | 跟随速度方向       | 朝向抛射体的运动方向，`Trajectory.BulletROT` 非正会瞬间转向，`Trajectory.BulletFacingOnPlane` 有效                           |
| *Spin*        | 持续自旋转         | 不停在水平面上旋转，`Trajectory.BulletROT` 的正负能够控制方向，`Trajectory.BulletFacingOnPlane` 无效                         |
| *Stable*      | 发射后静止         | 发射后就不再转动朝向，`Trajectory.BulletFacingOnPlane` 有效                                                                  |
| *Target*      | 跟随目标方向       | 朝向抛射体目标单位的方向，`Trajectory.BulletROT` 非正会瞬间转向，`Trajectory.BulletFacingOnPlane` 有效                       |
| *Destination* | 跟随目的地方向     | 朝向抛射体移动目的地方向，`Trajectory.BulletROT` 非正会瞬间转向，`Trajectory.BulletFacingOnPlane` 有效                       |
| *FirerBody*   | 跟随发射者身体朝向 | 在水平面上跟随发射者身体朝向，发射者阵亡后不动，`Trajectory.BulletROT` 非正会瞬间转向，`Trajectory.BulletFacingOnPlane` 无效 |
| *FirerTurret* | 跟随发射者炮塔朝向 | 在水平面上跟随发射者炮塔朝向，发射者阵亡后不动，`Trajectory.BulletROT` 非正会瞬间转向，`Trajectory.BulletFacingOnPlane` 无效 |

 > 下表以表格的形式简单体现功能的适配性

| Key | `Straight` | `Bombard` | `Missile` | `Engrave` | `Parabola` | `Tracing` |
|:---|:---:|:---:|:---:|:---:|:---:|:---:|
| `Trajectory.Speed` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `Trajectory.BulletROT` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `Trajectory.BulletFacing` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `Trajectory.BulletFacingOnPlane` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `Trajectory.OffsetCoord` | ✔️ | ✔️ | ✔️ | ❌ | ✔️ | ❌ |
| `Trajectory.RotateCoord` | ✔️ | ✔️ | ✔️ | ❌ | ✔️ | ❌ |
| `Trajectory.MirrorCoord` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `Trajectory.AxisOfRotation` | ✔️ | ✔️ | ✔️ | ❌ | ✔️ | ❌ |
| `Trajectory.LeadTimeCalculate` | ✔️ | ✔️ | ✔️ | ❌ | ✔️ | ❌ |
| `Trajectory.LeadTimeMaximum` | ✔️ | ✔️ | ❌ | ❌ | ✔️ | ❌ |
| `Trajectory.DetonationDistance` | ✔️ | ✔️ | ✔️ | ❌ | ✔️ | ❌ |
| `Trajectory.TargetSnapDistance` | ✔️ | ✔️ | ✔️ | ❌ | ✔️ | ❌ |
| `Trajectory.EarlyDetonation` | ❌ | ✔️ | ❌ | ❌ | ✔️ | ❌ |
| `Trajectory.DetonationHeight` | ❌ | ✔️ | ❌ | ❌ | ✔️ | ❌ |
| `Trajectory.AllowFirerTurning` | ❌ | ❌ | ❌ | ✔️ | ❌ | ✔️ |
| `Inaccurate` | ✔️ | ✔️ | ✔️ | ❌ | ✔️ | ❌ |
| `BallisticScatter` | ✔️ | ✔️ | ✔️ | ❌ | ✔️ | ❌ |
| `Gravity` | ❌ | ✔️ | ✔️ | ❌ | ✔️ | ❌ |
| `SubjectToGround` | ✔️ | ✔️ | ❌ | ❌ | ❌ | ❌ |
| `ProjectileRange(Weapon's)` | ✔️ | ✔️ | ✔️ | ❌ | ✔️ | ❌ |
| `LifeDuration` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `NoTargetLifeTime` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `CreateCapacity` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `ApplyRangeModifiers` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `RetargetRadius` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `Synchronize` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `PeacefulVanish` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `PassDetonate` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `ProximityImpact` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `PassThroughVehicles` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `PassThroughBuilding` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `DamageEdgeAttenuation` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `DamageCountAttenuation` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `UseDisperseCoord` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |
| `DisperseWeapons` | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ | ✔️ |

```{note}
- 注意 `SubjectToGround` 能够使得 `Trajectory=Straight` 的抛体在全程中或 `Trajectory=Bombard` 的抛体在上升阶段中，因撞击到地面而提前引爆
```

 > 以下为 `Straight` 弹道抛体的独有功能

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; `Straight` 弹道抛体
Trajectory.Straight.PassThrough=no           ; 布尔值，抛体是否穿过目标，与 `Trajectory.DetonationDistance` 存在联动
Trajectory.Straight.ConfineAtHeight=0        ; 整数型，抛体会在离当前地面多高处飞行，非正数不启用，要求速度小于256（若 `SubjectToCliffs=yes` ，则其在悬崖高处都不会往下移动）
```

```{note}
- 若在使用时 `Trajectory.Straight.ConfineAtHeight` 时，同时启用了 `PassDetonateLocal` ，则抛体垂直方向速度不受限制
```

 > 以下为 `Bombard` 弹道抛体的独有功能

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; `Bombard` 弹道抛体
Trajectory.Bombard.Height=0.0                ; 浮点型，抛体转向位置相对于目标的垂直高度
Trajectory.Bombard.FallPercent=1.0           ; 浮点型，抛体转向位置占发射位置与目标位置间水平距离的百分比，为0时转向位置是在发射位置上方，为1时转向位置是在目标位置上方
Trajectory.Bombard.FallPercentShift=0.0      ; 浮点型，抛体转向位置的最大随机距离间偏移量，随机值会与 `Trajectory.Bombard.FallPercent` 相加得到最终转向位置的水平距离百分比，为0时禁用功能
Trajectory.Bombard.FallScatter.Max=0.0       ; 浮点型，抛体转向位置将以 `Trajectory.Bombard.FallPercent` 和随机 `Trajectory.Bombard.FallPercentShift` 相加得到的位置为圆心，随机散布在以该值为半径的大圆内，为0时禁用功能
Trajectory.Bombard.FallScatter.Min=0.0       ; 浮点型，抛体转向位置将以 `Trajectory.Bombard.FallPercent` 和随机 `Trajectory.Bombard.FallPercentShift` 相加得到的位置为圆心，随机散布在以该值为半径的小圆外
Trajectory.Bombard.FallScatter.Linear=no     ; 布尔值，抛体转向位置的随机范围是否仅限于垂直于发射位置与目标位置之间连线的直线上
Trajectory.Bombard.FreeFallOnTarget=yes      ; 布尔值，抛体是在转向位置重新发射并自由落体还是正常从转向位置行进到预定目标，为是即前者，为否则后者
Trajectory.Bombard.NoLaunch=no               ; 布尔值，抛体是否跳过前往转向位置的过程直接从转向位置落下
Trajectory.Bombard.FallSpeed=0.0             ; 浮点型，抛体在经过转向位置后的初始速度，如果为0会使用 `Trajectory.Speed` ，不能和 `Trajectory.Bombard.FreeFallOnTarget` 同时使用
Trajectory.Bombard.TurningPointAnims=        ; Anim列表，如果设置，将在抛体到达转向位置时播放随机动画，即在下降阶段的初始位置播放
```

```{note}
- 若使用了 `Trajectory.Bombard.FreeFallOnTarget` ，那么抛射体在飞行中段时实际上是瞬移过去的
```

 > 以下为 `Missile` 弹道抛体的独有功能

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; `Missile` 弹道抛体
Trajectory.Missile.UniqueCurve=no            ; 布尔值，是否启用模拟钱学森弹道，启用后除附加功能外其它设置无效，最高速度固定为192（应该不会有人范围很小的情况下还拿这个做弹道吧）
Trajectory.Missile.PreAimCoord=0,0,0         ; FLH坐标，请写完整，抛体目标位置的额外偏移，抛体初始会朝着这个坐标飞行，而后转向，为0,0,0时会直接朝着目标前进
Trajectory.Missile.FacingCoord=no            ; 布尔值，抛体 `Trajectory.Missile.PreAimCoord` 的朝向是否跟随单位朝向
Trajectory.Missile.ReduceCoord=yes           ; 布尔值，抛体 `Trajectory.Missile.PreAimCoord` 所指定的坐标长度是否代表每10格攻击距离的长度，其实际长度与攻击距离会成正比，当攻击距离很小的时候，指定的方向也会向目标缩减，以提高近距离命中率
Trajectory.Missile.LaunchSpeed=0             ; 浮点型，抛体【初始】飞行速度
Trajectory.Missile.Acceleration=10.0         ; 浮点型，抛体飞行速度的加速度，按照此值每帧增加或减少速度，巡航速度会逐渐达到 `Trajectory.Speed`
Trajectory.Missile.TurningSpeed=10.0         ; 浮点型，抛体转向速度，即抛体的方向每帧最多能旋转多少角度
Trajectory.Missile.LockDirection=no          ; 布尔值，抛体在完成初次转向后是否锁定运动方向不再调整
Trajectory.Missile.CruiseEnable=no           ; 布尔值，抛体在达到 `Trajectory.Missile.PreAimCoord` 后的初次转向是否需要保持高度并朝目标前进
Trajectory.Missile.CruiseUnableRange=5.0     ; 浮点型，距离目标多远时结束巡航阶段，不再保持巡航高度，并开始朝向目标前进，如果在达到 `Trajectory.Missile.PreAimCoord` 时已经小于该值，会跳过巡航阶段（如果此值过小，则可能导致抛体永久处在巡航状态）
Trajectory.Missile.CruiseAltitude=800        ; 整数型，抛体的巡航高度
Trajectory.Missile.CruiseAlongLevel=no       ; 布尔值，抛体的巡航高度是否根据地形进行计算
Trajectory.Missile.SuicideAboveRange=-3.0    ; 浮点型，抛体本体在飞行距离达到该值后直接自爆，为0代表不因此自爆，当设置为负数时其绝对值代表初始距离的倍数
Trajectory.Missile.SuicideShortOfROT=no      ; 布尔值，抛体本体是否在发现转向能力不足时自爆
```

```{note}
- 使用 `Trajectory.Missile.UniqueCurve` 时，将会无视其它的设置
```

 > 以下为 `Engrave` 弹道抛体的独有功能

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; `Engrave` 弹道抛体
Trajectory.Engrave.SourceCoord=0,0           ; FL坐标，请写完整，抛体会从目标的这个相对坐标开始切割，即切割线段的起点坐标，H坐标会吸附地面（目标在空中时不会吸附地面），特殊值为0,0，指从开火位置开始
Trajectory.Engrave.TargetCoord=0,0           ; FL坐标，请写完整，抛体会朝目标的这个相对坐标进行切割，即切割线段的指向坐标，H坐标会吸附地面（目标在空中时不会吸附地面）
Trajectory.Engrave.AttachToTarget=no         ; 布尔值，切割激光的中心位置是否随目标位置变化
Trajectory.Engrave.UpdateDirection=no        ; 布尔值，切割激光是否随发射者和目标位置更新角度
```

```{note}
- 为了视觉效果，最好不要设置让 `Trajectory=Engrave` 可被拦截
- 在使用 `Trajectory=Engrave` 时，武器的 `IsLaser` 会持续连接发射位置和抛射体位置
- 直接使用 `Trajectory=Engrave` 中的激光绘制并使用 `PassDetonateWarhead` 相比使用 `DisperseWeapons` 更节约性能，自行斟酌使用
```

 > 以下为 `Parabola` 弹道抛体的独有功能

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; `Parabola` 弹道抛体
Trajectory.Parabola.OpenFireMode=Speed       ; 发射模式枚举类，抛体的发射模式，包括 Speed/Height/Angle/SpeedAndHeight/HeightAndAngle/SpeedAndAngle
Trajectory.Parabola.ThrowHeight=600          ; 整数型，抛体的抛射高度，抛体的水平速度将因距离发生改变，具体的高度将由发射位置的高度和目标位置的高度取较大值并增加该值，不支持非正数
Trajectory.Parabola.LaunchAngle=30           ; 浮点型，抛体的发射角度，抛体的水平速度和最高高度将因距离发生改变，使用模式2或模式5时支持(-90.0,90.0)，使用模式4时支持(0.0,90.0)
Trajectory.Parabola.DetonationAngle=-90.0    ; 浮点型，抛体在当前速度方向与水平面之间的夹角小于该值时会提前引爆，当该值处在(-90.0,90.0)时生效，该值为零时（即需要抛体在最高点时引爆）会简化判断节约性能
Trajectory.Parabola.BounceTimes=0            ; 整数型，抛体的弹跳次数，抛体速度过快时使用弹跳会产生问题
Trajectory.Parabola.BounceOnTarget=land      ; 目标枚举类，抛体可以在哪些格子或单位上发生弹跳，包括 none|land|water|empty|infantry|units|buildings|all
Trajectory.Parabola.BounceOnHouses=all       ; 所属枚举类，抛体可以在哪些所属的单位上发生弹跳，包括 none|owner/self|allies/ally|team|enemies/enemy|all
Trajectory.Parabola.BounceDetonate=no        ; 布尔值，抛体是否可以在每次弹跳时额外引爆一次弹头
Trajectory.Parabola.BounceAttenuation=0.8    ; 浮点型，抛体弹跳伤害的衰减系数，即每次弹跳后下一次弹跳造成伤害会是本次伤害的多少倍
Trajectory.Parabola.BounceCoefficient=0.8    ; 浮点型，抛体弹跳的弹性系数，即每次弹跳后的速度是弹跳前速度的多少倍
```

- 选项 `Trajectory.Parabola.OpenFireMode` 可选类型：

| 类型             | 模式                       | 简述                             | 计算                                                                                     |
|:-----------------|:---------------------------|:---------------------------------|:-----------------------------------------------------------------------------------------|
| *Speed*          | 固定水平速度的自动计算模式 | 抛体的飞行时间是相对固定的       | 使用 `Trajectory.Speed` 和目标的坐标作为计算条件                                         |
| *Height*         | 固定最高高度的自动计算模式 | 抛体的引爆时间是相对固定的       | 使用 `Trajectory.Parabola.ThrowHeight` 和目标的坐标作为计算条件                          |
| *Angle*          | 固定发射角度的自动计算模式 | 抛体的发射角度是永久固定的       | 使用 `Trajectory.Parabola.LaunchAngle` 和目标的坐标作为计算条件                          |
| *SpeedAndHeight* | 固定水平速度和最高高度模式 | 轨迹将会随目标的高度发生高度变化 | 使用 `Trajectory.Speed` 和 `Trajectory.Parabola.ThrowHeight` 作为计算条件                |
| *HeightAndAngle* | 固定最高高度和发射角度模式 | 轨迹将会随目标的高度发生水平变化 | 使用 `Trajectory.Parabola.ThrowHeight` 和 `Trajectory.Parabola.LaunchAngle` 作为计算条件 |
| *SpeedAndAngle*  | 固定水平速度和发射角度模式 | 轨迹将会是永久固定的             | 使用 `Trajectory.Speed` 和 `Trajectory.Parabola.LaunchAngle` 作为计算条件                |

```{note}
- 在 `Trajectory.Parabola.OpenFireMode=Angle` 模式下，性能消耗较高，且由于可能攻击目标过高而无解，不建议使用此模式时启用 `SubjectToCliffs` 或启用 `AA` 并使用较小的 `MinimumRange`
- `Trajectory.Parabola.BounceTimes` 默认只能在地面、斜坡和大部分悬崖处反弹，弹跳功能和 `Trajectory.DetonationDistance` 并不冲突会同时生效，如需要抛体只会在弹跳次数耗尽后再爆炸，请将 `Trajectory.DetonationDistance` 设置为非正数
```

 > 以下为 `Tracing` 弹道抛体的独有功能

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; `Tracing` 弹道抛体
Trajectory.Tracing.TraceMode=Connection      ; 追踪模式枚举类，抛体的追踪模式，包括 Connection/Global/Body/Turret/RotateCW/RotateCCW
Trajectory.Tracing.TrackTarget=yes           ; 布尔值，抛体追踪的目标是否为抛体的目标，否则为追踪发射者，如果追踪发射者，那么当发射者死亡时抛体也会一同引爆
Trajectory.Tracing.CreateAtTarget=no         ; 布尔值，抛体是否在目标位置上直接生成
Trajectory.Tracing.StableRotation=no         ; 布尔值，抛体的追踪模式为 `RotateCW` 或 `RotateCCW` 时，是否减慢旋转速度并自动计算抛体间夹角
Trajectory.Tracing.CreateCoord=0,0,0         ; FLH坐标，请写完整，抛体会在原生成位置的基础上偏移这个坐标生成，不由 `Trajectory.Tracing.TraceMode` 决定具体位置
Trajectory.Tracing.AttachCoord=0,0,0         ; FLH坐标，请写完整，抛体会追踪其追踪目标的这个位置，由 `Trajectory.Tracing.TraceMode` 决定具体位置
Trajectory.Tracing.ChasableDistance=0        ; 浮点型，抛体追击目标的中心与发射者之间的距离不会超过此值，当发射者死亡时，若为正数，则抛射体会平静地消失，若为负数，将会引爆抛体，当为零时，使用武器射程并视为正数
```

- `Trajectory.Tracing.TraceMode` 可选类型：（用于计算 `Trajectory.Tracing.AttachCoord` 的坐标轴，原点位于追踪目标上，H轴不随追踪目标的倾斜偏转影响，永远朝向地图正上方）

| 类型         | 模式           | 简述                                        |
|:-------------|:---------------|:--------------------------------------------|
| *Connection* | 连线向量模式   | 以发射位置与目标位置之间向量的水平分量为F轴 |
| *Global*     | 地图方向模式   | 以地图的右下方方向为F轴                     |
| *Body*       | 跟随身体模式   | 以追踪目标的身体朝向为F轴                   |
| *Turret*     | 跟随炮塔模式   | 以追踪目标的炮塔朝向为F轴                   |
| *RotateCW*   | 顺时针旋转模式 | 以FL方向偏移的合成量为半径，绕H轴顺时针旋转 |
| *RotateCCW*  | 逆时针旋转模式 | 以FL方向偏移的合成量为半径，绕H轴逆时针旋转 |

```{note}
- 在使用 `Trajectory=Tracing` 时，武器的 `IsLaser` 会持续连接发射位置和抛射体位置
- `Trajectory.Tracing.StableRotation` 需要 `CreateCapacity` 配合进行进行记录才能生效
```

---

#### 抛射体散布增强

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMEPROJECTILE]                             ; 任意抛体（暂不支持弹道抛体）
BallisticScatter.IncreaseByRange=no          ; 布尔值，散布是否随距离提升，默认检查是否为 [Inviso=yes] 且 [FlakScatter=yes]
BallisticScatter.MinRange=                   ; 浮点型，计算最小散布的距离
BallisticScatter.MaxRange=                   ; 浮点型，计算最大散布的距离
BallisticScatter.Min.InMinRange=             ; 浮点型，距离最小时的散布范围下限，单位为格
BallisticScatter.Min.InMaxRange=             ; 浮点型，距离最小时的散布范围上限，单位为格
BallisticScatter.Max.InMinRange=             ; 浮点型，距离最大时的散布范围下限，单位为格
BallisticScatter.Max.InMaxRange=             ; 浮点型，距离最大时的散布范围上限，单位为格，如果散布范围太大可能会散到射程之外开不出火。VXL会立即再次选择目标，而SHP如果具有开火序列的话就得先播放序列再选择目标
BallisticScatter.Chance=1.0                  ; 浮点型，启用不精准后产生散布的概率
```

---

#### 无形抛体无延迟引爆

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
InvisoLatencyFix=no                          ; 布尔值，是否启用修复
```

```{note}
- 当目标是快速移动的空军时，`Inviso=yes` 的抛射体有很大概率需要1帧以上的时间才能命中敌人，甚至无法命中敌人而最终掉到地上
- 这个修复本质上是强制 `Inviso=yes` 的抛射体 `Ranged=yes` ，没有仔细检查原理，但它确实能工作，感谢 NetsuNegi 告诉我这个信息
```

---

#### 无形抛体阻挡问题修复

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
InvisoBlockageFix=no                         ; 布尔值，是否启用修复
```

```{note}
- 原本单位会寻路以试图避开可以拦截抛射体的障碍，这个计算是根据单位本身算的，但开火时抛射体从FLH处发射，与单位本身位置并不一致，这导致单位有可能判定可以开火，但抛射体最终打在了障碍上
- 现在你可以让 `Inviso=yes` 的抛射体在开火时先把抛射体放在单位所在位置再移动过去，这可以避免这样的问题
```

---

### 动画

---

#### 屏幕外依旧绘制

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `artmd.ini` 之中：

```ini
[SOMEANIM]                                   ; 任意动画
RenderIfOutOfScreen=no                       ; 布尔值，是否让动画在超出绘制范围时仍然绘制
```

```{note}
- 在原本游戏中，只会绘制屏幕矩形范围及往左右上下再扩大6格这么大范围内的实体，超过这个范围的实体将不会被绘制，即使它们图像的一部分仍然应该可见
- 对于别的类型如果有确切的需求可以提
```

---

### 电脑

---

#### 玩家队友电脑智力恢复

  > - *pr #1510 by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
AIAngerOnAlly=yes                            ; 布尔值，是否允许AI向友方施加仇恨，用于修复玩家的队友AI会表现非常被动、只有在敌人进家之后才会开始正常行动的问题
```

```{note}
- 是否存在副作用需要测试
```

---

#### 脚本巡逻至路径点增强

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

- 现在可以通过Phobos攻击和移动脚本类似的方式自动推算一个格子来代替路径点，单位会按照原有的逻辑巡逻过去，使用**小于0**的脚本参数会自动使用新逻辑，参数 `-n` 的解析方式如下（先将其去掉负号，并转换为**二进制数**得到 `ABCDDDEEEEEEEE` ，相反的，你可以通过计算器的程序员模式先将二进制数写出，再转换成十进制数）
  - `-n` 的二进制1-8位 EEEEEEEE
    > 目标类型，填 `AITargetType` 中值的索引或 `TargetType#` 的类型，具体是索引还是类型根据a4决定，具体参考Phobos攻击和移动脚本中n的写法（由于只有8位，所以没法填超过256的索引，不过256应该也够用了）
  - `-n` 的二进制9-11位 DDD
    > 目标选择优先级计算算法，0 - 距离近且高仇恨单位优先，1 - 距离远且高仇恨，2 - 距离近，3 - 距离远，4 - 更能保持作战方存活的优先（具体来说就是基地和MCV最高，其次战车工厂，然后其它工厂，再次其它 `KeepAlive` 的单位，最后是其它不 `KeepAlive` 的单位，若同一类型有多个，优先选择距离近的）
  - `-n` 的二进制第12位 C
    > 选择友军还是敌军，0 - 敌军，1 - 友军
  - `-n` 的二进制第13位 B
    > 选择目标使用 `AITargetType` 还是 `TargetType#` ，两者的区别参考Phobos已有的说明书，0 - `TargetType#` ，1 - `AITargetType`
  - `-n` 的二进制第14位 A
    > 是否需要目标可以被小队首领攻击，0 - 不需要，1 - 需要

In `aimd.ini`:

```ini
[SOMESCRIPTTYPE]                             ; 任意脚本类型
X=16,-n                                      ; 脚本
```

```{note}
- 没有仔细检查16号脚本的细节，目前可能有不小的bug，小心使用，遇到问题请反馈
```

---

#### 新的火卫脚本目标选项

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

- 增加了 *TargetType#* 可选类型：

| *TargetType#* | 其代表的类型 |
|:-------------:|:------------:|
|     *37*      | 陆地上的工厂 |
|     *38*      | 陆地上的建筑 |
|     *39*      | 陆地上的单位 |

---

#### 基地节点尝试次数恢复

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

- 仅优化，无ini
- `MaximumBuildingPlacementFailures` 现在对战役生效，当尝试超过该次数之后AI将会移除那个建筑节点

---

#### 作战方被击败行为扩展

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
ReorganizeToWhenDefeated=none                ; 所属枚举类，被击败作战方剩下的单位和金钱转送给一个随机的哪类作战方，包括 none|owner/self|allies/ally|team|enemies/enemy|all
```

```{note}
- 如果找不到合适的作战方，那就会执行原本的摧毁行为
- 会优先选择玩家控制的作战方
- 部分取值有可能选到自己，这时候的效果等同于跳过了摧毁
```

---

#### 电脑建筑节点距离限制

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[AI]                                         ; AI逻辑设置
AIAdjacentMax=-1                             ; 整数型，电脑的基地节点只有在其这么长的距离范围内拥有建筑时，才能够拍下建筑，单位：格，非负数时启用
AIAdjacentMax.Campaign=                      ; 整数型，战役模式下电脑的基地节点只有在其这么长的距离范围内拥有建筑时，才能够拍下建筑，单位：格，非负数时启用，默认值为 `AIAdjacentMax` 的值
```

---

### 拓展

---

#### 附加单位

  > - *pr #352 by Metadorius*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
AttachmentTopLayerMinHeight=500              ; 整数型，附加单位在顶部图层时的最低高度
AttachmentUndergroundLayerMaxHeight=-256     ; 整数型，附加单位在地底图层时的最高高度

[AttachmentTypes]                            ; 附加类注册表
0=SOMEATTACHMENT                             ; 附加类型

[SOMEATTACHMENT]                             ; 附加类型
TechnoType=                                  ; Techno类型，目前只能附加车辆单位
FLH=0,0,0                                    ; FLH坐标，请写完整，附加单位在父单位上的位置
IsOnTurret=no                                ; 布尔值，附加单位是否在父单位的炮塔上
RotationAdjust=0                             ; DirType类型，在父单位上额外的旋转角度，范围是(-255,255)
RespawnAtCreation=yes                        ; 布尔值，附加单位是否在父单位创建时重生
RespawnDelay=-1                              ; 整数型，附加单位重生所需的时间，非负数时启用
InheritExperience=no                         ; 布尔值，附加单位的击杀信息是否被记录到其父单位上（如经验）
InheritTarget=no                             ; 布尔值，附加单位是否会在射程允许的情况下继承父单位的攻击目标并支持 `Turret.PointToMouse`
InheritTarget.Force=no                       ; 布尔值，附加单位是否会强制继承父单位的攻击目标，且不再会自动索敌和接受命令
InheritOwner=yes                             ; 布尔值，附加单位是否跟随父单位变更所属
InheritStateEffects=yes                      ; 布尔值，附加单位是否继承父单位状态效果（如混乱、铁幕等）
InheritCommands=yes                          ; 布尔值，命令父单位时，附加单位是否同样接收
InheritCommands.StopCommand=yes              ; 布尔值，命令父单位停止时，附加单位是否同样停止
InheritCommands.DeployCommand=yes            ; 布尔值，命令父单位部署时，附加单位是否同样部署
LowSelectionPriority=yes                     ; 布尔值，附加单位是否处在低选择优先级
TransparentToMouse=no                        ; 布尔值，附加单位是否不能被鼠标点击
YSortPosition=default                        ; 图层位置枚举类，附加单位的图层位置，包括 default/underparent/overparent
InheritDestruction=yes                       ; 布尔值，附加单位是否随父单位被摧毁
InheritHeightStatus=yes                      ; 布尔值，附加单位是否继承父单位的图层和高度信息
OccupiesCell=yes                             ; 布尔值，附加单位是否占据地面格子位置
DestructionWeapon.Child=                     ; Weapon类型，当父单位被毁时在附加单位上发射的武器
DestructionWeapon.Parent=                    ; Weapon类型，当附加单位被毁时在父单位上发射的武器
ParentDestructionMission=                    ; 任务名，附加单位在父单位被毁后进入的任务
ParentDetachmentMission=                     ; 任务名，附加单位从父单位上移除后进入的任务

[SOMEVEHICLE]                                ; 任意作为附加单位的车辆
Locomotor=Attachment                         ; 移动方式，如有需要可以不设置此移动方式 {C5D54B98-8C98-4275-8CE4-EF75CB0CBE3E}
AttachmentTopLayerMinHeight=                 ; 整数型，附加单位在顶部图层时的最低高度，默认值为 `General` 中 `AttachmentTopLayerMinHeight` 的值
AttachmentUndergroundLayerMaxHeight=         ; 整数型，附加单位在地底图层时的最高高度，默认值为 `General` 中 `AttachmentUndergroundLayerMaxHeight` 的值

[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
AttachmentTypes=                             ; AttachmentType列表，单位上有哪些附加类

[SOMEWARHEAD]                                ; 任意弹头
AttachmentTransformX.Types=                  ; AttachmentType列表，受到影响的附加类
AttachmentTransformX.AffectedHouses=all      ; 所属枚举类，哪些所属方的附加类会受到以下影响，包括 none|owner/self|allies/ally|team|enemies/enemy|all
AttachmentTransformX.FLHTime=-1              ; 整数型，附加类变更位置所需的时间，非负数时启用
AttachmentTransformX.FLH=0,0,0               ; FLH坐标，请写完整，附加类需要变更到哪个位置
AttachmentTransformX.RotationTime=-1         ; 整数型，附加类变更角度所需的时间，非负数时启用
AttachmentTransformX.Rotation=0              ; DirType类型，附加类需要变更到哪个角度

[SOMESW]                                     ; 任意超武
AttachmentTransformX.Types=                  ; AttachmentType列表，受到影响的附加类
AttachmentTransformX.AffectedHouses=owner    ; 所属枚举类，哪些所属方的附加类会受到以下影响，包括 none|owner/self|allies/ally|team|enemies/enemy|all
AttachmentTransformX.FLHTime=-1              ; 整数型，附加类变更位置所需的时间，非负数时启用
AttachmentTransformX.FLH=0,0,0               ; FLH坐标，请写完整，附加类需要变更到哪个位置
AttachmentTransformX.RotationTime=-1         ; 整数型，附加类变更角度所需的时间，非负数时启用
AttachmentTransformX.Rotation=0              ; DirType类型，附加类需要变更到哪个角度
```

```{note}
- 单位可使用此逻辑以树状互相连接，附加的单位不会处理任何运动相关的代码，像是其父单位结构上的一部分
- 目前，可附加单位的类型只能是车辆
- 附加单位会自动使用 `Locomotor=Attachment` 作为其移动方式，如果附加单位在游戏中同时需要作为普通单位自己移动，可以不手动设置
```

---

### 界面

---

#### 拓展居中字幕

  > - *pr #1792 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `ra2md.ini` 之中：

```ini
[Phobos]                                     ; Phobos设置
MessageDisplayInCenter.LabelsCount=4         ; 整数型，居中字幕在未展开时，能最多显示几条信息
MessageDisplayInCenter.RecordsCount=12       ; 整数型，居中字幕在已展开时，能最多显示几条信息
```

- 拥有快捷键功能，位于 [界面] 面板
- 背景和按钮的主题色使用和消息相同的颜色，支持单击、长按、拖动和滚轮调整显示的信息

```{note}
- 字幕信息的可存储数量不限，且可以在存读档后正常显示
- 关于1个开关快捷键：
  - 开关快捷键默认名为 `Toggle Message Label` ，可以在csf中添加名为 `TXT_TOGGLE_MESSAGE` 和 `TXT_TOGGLE_MESSAGE_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
```

---

#### 底边栏选择信息

  > - 暂无pr *by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `ra2md.ini` 之中：

```ini
[Phobos]                                     ; Phobos设置
SelectedDisplay.Enable=no                    ; 布尔值，底边栏在开始游戏时默认是否开启
SelectedDisplay.Expand=no                    ; 布尔值，底边栏在开始游戏时默认是否显示所有单位的图标（不会排序），否则会自动收纳相同类型单位并在左上角显示数量（会排序）
SelectedDisplay.MaxCameo=10                  ; 整数型，底边栏最多同时显示多少图标，有效值为5-20，会受到显示宽度影响
```

在 `rulesmd.ini` 之中：

```ini
[SOMESIDE]                                   ; 任意主阵营
SelectedInfo.Main=                           ; SHP文件名，需要后缀，玩家属于该阵营时，底边栏的主要信息背景素材，仅在单选单位时会使用，素材大小为固定 203×79 ，需要1帧
SelectedInfo.Buff=                           ; SHP文件名，需要后缀，玩家属于该阵营时，底边栏的动态加成比例图标素材，仅在单选单位时会使用，素材大小为固定 14×14 ，需要15帧，顺序为：无火力增益、低火力减益、高火力减益、低火力增益、高火力增益、无防御增益、低防御减益、高防御减益、低防御增益、高防御增益、无速度增益、低速度减益、高速度减益、低速度增益、高速度增益
SelectedInfo.Button=                         ; SHP文件名，需要后缀，玩家属于该阵营时，底边栏的按钮使用的图标素材，仅在单选单位时会使用，素材大小为固定 30×30 ，需要7帧，顺序为：按钮背景、侵略模式不可用、正处在侵略模式、未处在侵略模式、手动装弹不可用、正处在空弹装填、未处在空弹装填
SelectedInfo.Bottom=                         ; SHP文件名，需要后缀，玩家属于该阵营时，底边栏的帧率时间背景素材，素材大小为固定 289×21 ，需要3帧，分别为：关闭时的（左对齐 17×21）、打开时的（左对齐 253×21）、打开并多选时的（289×21）
SelectedInfo.Toggle=                         ; SHP文件名，需要后缀，玩家属于该阵营时，底边栏的开关按钮、展开按钮和滚动按钮的素材，素材大小为固定 10×14 ，需要8帧，顺序为：打开、关闭、展开、收纳、向左、不可向左、向右、不可向右
SelectedInfo.Palette=anim.pal                ; PAL文件名，需要后缀，玩家属于该阵营时，以上这些底边栏的素材使用的色盘

[AudioVisual]                                ; 视听通用设置
SelectedInfantryMissingPCX=                  ; PCX文件名，需要后缀，仅用于底边栏，确认该步兵使用的是 `MissingCameo` 时，会替换显示的图标
SelectedVehicleMissingPCX=                   ; PCX文件名，需要后缀，仅用于底边栏，确认该车辆使用的是 `MissingCameo` 时，会替换显示的图标
SelectedAircraftMissingPCX=                  ; PCX文件名，需要后缀，仅用于底边栏，确认该飞机使用的是 `MissingCameo` 时，会替换显示的图标
SelectedBuildingMissingPCX=                  ; PCX文件名，需要后缀，仅用于底边栏，确认该建筑使用的是 `MissingCameo` 时，会替换显示的图标

[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
SelectedInfo.UpperType=Shield                ; InfoType类型（和 `DigitalDisplayType` 中的 `InfoType` 相同），单选单位时底边栏显示在名称下偏上方的信息类型
SelectedInfo.UpperIndex=0                    ; 整数型，`SelectedInfo.UpperType` 类型下具体显示哪个参数
SelectedInfo.UpperColor=153,153,255          ; RGB颜色，请写完整，显示 `SelectedInfo.UpperType` 的颜色，为 0,0,0 时会根据当前值和最大值的比例以及 `AudioVisual` 中 `ConditionYellow` 和 `ConditionRed` 的比例显示红黄绿的颜色
SelectedInfo.UpperDivisor=1                  ; 整数型，大于1时启用，显示 `SelectedInfo.UpperType` 的倍减比例，会按此倍数降低显示的值
SelectedInfo.BelowType=Health                ; InfoType类型（和 `DigitalDisplayType` 中的 `InfoType` 相同），单选单位时底边栏显示在名称下偏下方的信息类型
SelectedInfo.BelowIndex=0                    ; 整数型，`SelectedInfo.BelowType` 类型下具体显示哪个参数
SelectedInfo.BelowColor=0,0,0                ; RGB颜色，请写完整，显示 `SelectedInfo.BelowType` 的颜色，为 0,0,0 时会根据当前值和最大值的比例以及 `AudioVisual` 中 `ConditionYellow` 和 `ConditionRed` 的比例显示红黄绿的颜色
SelectedInfo.BelowDivisor=1                  ; 整数型，大于1时启用，显示 `SelectedInfo.BelowType` 的倍减比例，会按此倍数降低显示的值
SelectedInfo.CameoType=Ammo                  ; InfoType类型（和 `DigitalDisplayType` 中的 `InfoType` 相同），选中单位时底边栏显示在图标上方血条下的信息类型
SelectedInfo.CameoIndex=0                    ; 整数型，`SelectedInfo.CameoType` 类型下具体显示哪个参数
SelectedInfo.Button=                         ; SHP文件名，需要后缀，选中该单位时底边栏的按钮使用的图标素材，素材大小为固定 30×30 ，需要7帧，顺序为：按钮背景、侵略模式不可用、正处在侵略模式、未处在侵略模式、手动装弹不可用、正处在空弹装填、未处在空弹装填，默认值为 `SOMESIDE` 中 `SelectedInfo.Button` 的值
UIDescription.HoveredInfo=                   ; CSF条目，鼠标悬浮在底部栏主图标上时显示的文本内容，默认使用 `UIDescription`
```

- 拥有快捷键功能，位于 [界面] 面板
- 默认关闭状态，若没有素材则无法开启，单选时若鼠标悬浮在单位图标上，则会显示其简介，悬浮在buff图标上时可以显示其加成系数，右侧上方是切换侵略模式的按钮，右侧下方是手动装填弹药的按钮；多选时可以点击图标，图标顺序按照注册表中的顺序从左到右排列，不同类型则从左到右为建筑、车辆、飞机、步兵，左键点击：选择其中随机一个该类型单位，shift+左键：选择其中所有该类型单位，右键点击：取消选中随机一个该类型单位，shift+右键：取消选中所有该类型单位，数量较多时可以使用滚轮调整图标；会读取 `GroupAs` 判断是否为同一类型，时间提示在使用该功能时会一直显示

```{note}
- 启用 `SelectedDisplay.Enable` 意味着消耗更多的画面渲染性能，请自行决定是否使用
- 关于可本地化内容：
  - 可以在csf中添加名为 `Status:XXXX` 的项目来定义显示的文本，其中 XXXX 为状态名，包括：
  - 可以在csf中添加名为 `TIP:AggressiveStance` 和 `TIP:ManualReloadAmmo` 的项目来分别重写鼠标悬浮在两个大按钮上时的说明
  - 可以在csf中添加名为 `TIP:PowerMult` 、`TIP:ArmorMult` 和 `TIP:SpeedMult` 的项目来分别重写鼠标悬浮在三个状态标上时的说明
- 关于1个开关快捷键：
  - 开关快捷键默认名为 `Selected technos display` ，可以在csf中添加名为 `TXT_SELECTED_INFO` 和 `TXT_SELECTED_INFO_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
- 关于1个展开快捷键：
  - 展开快捷键默认名为 `Selected technos display expand` ，可以在csf中添加名为 `TXT_SELECTED_EXPAND` 和 `TXT_SELECTED_EXPAND_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
- 关于右下角各种计时器的背景：
  - 当按下 `Alt` 键时，可以增加其背景的不透明度
```

---

#### 独特单位独立显示

  > - 暂无pr *by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
UniqueTechno=no                              ; 布尔值，该单位是否被视为独特单位，独特单位会在右上角显示一个独立图标，最多同时显示八个独特单位
```

- 拥有快捷键功能，位于 [控制] 面板
- 默认开启状态，可以点击图标单独选中单位（若处在载具中则会选中载具），若已单独选中，则会居中视野；会显示血条、护盾条和铁幕剩余时间（若单位处在生产状态，会改为显示生产进度条）；若单位正处在铁幕状态下，会持续显示一层半透明黑色，而若单位血量较低，会浮动显示一层较浅的红色；当单位受到攻击时会短暂显示一层半透明红色；当单位正在攻击时会短暂显示一层半透明黄色；当单位正被空袭指示时会持续显示一层半透明红色；当单位正被超时空武器锁定时会持续显示一层半透明蓝色；当单位正被飞碟汲取或被磁电控制或被寄生时会持续显示一层半透明紫色；当单位处于EMP中时会持续显示一层半透明灰色；当单位处于载具车辆或驻军建筑中时会在血条外显示一圈代表载具车辆或驻军建筑血量的橙色，且护盾和铁幕时间会被载具车辆或驻军建筑所替换；当单位在坦克碉堡中时会在血条外显示一圈代表坦克碉堡血量的紫色，护盾和铁幕时间不会被替换；其它除了正在生产外的不在地图上的状态会在血条外显示一圈完整的白色

```{note}
- 关于1个开关快捷键：
  - 开关快捷键默认名为 `Heros display` ，可以在csf中添加名为 `TXT_HEROS_INFO` 和 `TXT_HEROS_INFO_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
  - 开关时显示的提示信息，可以在csf中添加名为 `TXT_HEROS_VISIBLE` 和 `TXT_HEROS_INVISIBLE` 的项目来分别定义开和关时显示的文本
```

---

#### 图标预览和叠加显示

  > - *pr #1477 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `ra2md.ini` 之中：

```ini
[Phobos]                                     ; Phobos设置
ShowBuildingStatistics=no                    ; 布尔值，是否给建筑类型图标的左上角增加一个数字显示，显示当前拥有的该类型建筑的数量
```

在 `rulesmd.ini` 之中：

```ini
[AudioVisual]                                ; 视听通用设置
Cameo.AlwaysExist=no                         ; 布尔值，所有可建造类型是否永远不会不显示图标，即在不满足任意建造条件时仍会显示灰色图标，且会改变播放EVA语音的时机（注意这受到所属方影响，且科技等级必须要合法）
Cameo.OverlayShapes=                         ; SHP文件名，需要后缀，在建造图标之上绘制图案时使用的文件
Cameo.OverlayFrames=                         ; 四维整数型，分别代表当前拥有该类型的实体时、可以自动建造该类型时、当前类型暂时不能造时和当前类型无前提条件时分别使用SHP文件中的第几帧，为负数时代表对应条件下不需要绘制
Cameo.OverlayPalette=                        ; PAL文件名，需要后缀，在建造图标之上绘制图案时使用的色盘

[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
CameoPriority.Houses=                        ; 子阵营列表，如果填写，则玩家属于这些阵营中时，该图标会优先显示在前面，优先级高于 `CameoPriority`
Cameo.AlwaysExist=                           ; 布尔值，该类型是否能够一直显示图标，即在不满足任意建造条件（仅建造条件， 不包括`TechLevel` 、`Owner` & `Cameo.RequiredHouses` 、`RequiredHouses` 、`ForbiddenHouses` 、`FactoryOwners` 、`StolenTech` 、`SecretLab` 和 `RequiredTheaters` 等其它限制条件，当满足 `Cameo.OverrideTechnos` 时会覆盖前面的 `Owner` & `Cameo.RequiredHouses` 条件）时仍会显示灰色图标，且会改变播放EVA语音的时机，默认值为 `General` 中 `Cameo.AlwaysExist` 的值
Cameo.RequiredHouses=                        ; 子阵营列表，如果填写，则会与 `Owner` 一起共同对 `Cameo.AlwaysExist` 进行判断
Cameo.OverrideTechnos=                       ; Techno列表，如果填写，则当拥有这些类型中任意一种时就会视为 `Owner` & `Cameo.RequiredHouses` 的条件通过
UIDescription.Unbuildable=                   ; CSF条目，因 `Cameo.AlwaysExist` 原因使得图标为灰色时显示的文本

[SOMEBUILDING]                               ; 只能是建筑
Cameo.ShouldCount=                           ; 布尔值，是否需要被 `ShowBuildingStatistics` 所统计，默认值为 `BuildLimit` 有设置或不是 `BuildCat=Combat`

[SOMESW]                                     ; 任意超武
CameoPriority.Houses=                        ; 子阵营列表，如果填写，则玩家属于这些阵营中时，该图标会优先显示在前面，优先级高于 `CameoPriority`
```

在 `artmd.ini` 之中：

```ini
[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
GreyCameoPCX=                                ; PCX文件名，需要后缀，当前类型无前提条件时在建造图标上方额外绘制的图像，注意这同样会覆盖灰色半透明图层，有需要请直接处理该图像文件为灰色
```

---

#### 统一雷达和单位颜色

  > - 暂无pr *by TaranDahl & CrimRecya*
  > - [返回目录](#额外功能目录)

在 `ra2md.ini` 之中：

```ini
[Phobos]                                     ; Phobos设置
UnifiedTechnoColor=no                        ; 布尔值，在开始游戏时默认是否使用统一的单位颜色，而非使用每个单位自身的所属色
```

在 `rulesmd.ini` 之中：

```ini
[AudioVisual]                                ; 视听通用设置
UnifiedRadarColor=no                         ; 布尔值，地形是否使用统一的雷达颜色，而非使用每个物件自身的雷达颜色
UnifiedRadarColor.Land=255,127,0             ; RGB颜色，请写完整，陆地的颜色
UnifiedRadarColor.Water=95,127,207           ; RGB颜色，请写完整，水面的颜色
UnifiedRadarColor.Cliff=63,63,63             ; RGB颜色，请写完整，悬崖的颜色
; 以下部分的颜色受 `UnifiedTechnoColor` 及其快捷键影响
UnifiedRadarColor.Self=0,255,0               ; RGB颜色，请写完整，己方的颜色
UnifiedRadarColor.Ally=255,255,0             ; RGB颜色，请写完整，友方的颜色
UnifiedRadarColor.Enemy=255,0,0              ; RGB颜色，请写完整，敌方的颜色
UnifiedRadarColor.Neutral=255,255,255        ; RGB颜色，请写完整，中立方的颜色
UnifiedTechnoColor.Self=Green                ; 颜色方案，己方的颜色
UnifiedTechnoColor.Ally=Gold                 ; 颜色方案，友方的颜色
UnifiedTechnoColor.Enemy=Red                 ; 颜色方案，敌方的颜色
UnifiedTechnoColor.Neutral=LightGrey         ; 颜色方案，中立的颜色
```

- 拥有快捷键功能，位于 [界面] 面板
- 默认关闭状态，若单位对应方案无效，使用原本的所属色

```{note}
- 颜色方案即 [Colors] 注册表下的那些键名，当找不到对应颜色方案时，会使用原本单位自身的所属色
- 关于1个开关快捷键：
  - 开关快捷键默认名为 `Unify techno color` ，可以在csf中添加名为 `TXT_UNIFY_COLOR` 和 `TXT_UNIFY_COLOR_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
```

---

#### 观战者计分板显示电脑

  > - *pr #1386 by CrimRecya*
  > - [返回目录](#额外功能目录)

- 仅优化，无ini
- 现在电脑玩家也能出现在观战计分板上了

```{note}
- 该功能会在 `YRpp-Spawner` 合并后移除
```

---

### 操作

---

#### 切换侵略模式

  > - *pr #1464 by Aephiex*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
EnableAggressiveStance=no                    ; 布尔值，是否全局启用该功能，只有该值设置为是才会启用下面的功能

[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
AggressiveStance=no                          ; 布尔值，单位默认的侵略模式状态
AggressiveStance.Togglable=                  ; 布尔值，单位是否能够切换侵略模式状态
AggressiveStance.Exempt=no                   ; 布尔值，单位是否能够不受侵略模式影响
VoiceEnterAggressiveStance=-1                ; sound条目，进入侵略模式状态时单位会发出的声音
VoiceExitAggressiveStance=-1                 ; sound条目，退出侵略模式状态时单位会发出的声音
```

- 拥有快捷键功能，位于 [控制] 面板
- 如果选中的单位中存在部分单位未进入侵略模式，则按下快捷键时会先让这些单位进入侵略模式，只有当所有选中的单位都处在侵略模式时，才会让他们都退出侵略模式
- `AggressiveStance.Togglable` 的默认值为有 `Primary` 或 `OpenTopped` 且没用 `Engineer` 或 `Agent` 或 `EMPulseCannon`

```{note}
- 在已有目标的情况下，单位不会自动切换目标
- 关于1个开关快捷键：
  - 开关快捷键默认名为 `Aggressive Stance` ，可以在csf中添加名为 `TXT_AGGRESSIVE_STANCE` 和 `TXT_AGGRESSIVE_STANCE_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
  - 开关时显示的提示信息，可以在csf中添加名为 `MSG:AGGRESSIVE_STANCE_ON` 和 `MSG:AGGRESSIVE_STANCE_OFF` 的项目来分别定义开和关时显示的文本，文本中可以使用 `%i` 以显示切换状态的单位的数量
```

---

#### 切换停火模式

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
EnableCeaseFireStance=no                     ; 布尔值，是否全局启用该功能，只有该值设置为是才会启用下面的功能

[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
CeaseFireStance=no                           ; 布尔值，单位默认的停火模式状态
CeaseFireStance.Togglable=                   ; 布尔值，单位是否能够切换停火模式状态
VoiceEnterCeaseFireStance=-1                 ; sound条目，进入停火模式状态时单位会发出的声音
VoiceExitCeaseFireStance=-1                  ; sound条目，退出停火模式状态时单位会发出的声音
```

- 拥有快捷键功能，位于 [控制] 面板
- 如果选中的单位中存在部分单位未进入停火模式，则按下快捷键时会先让这些单位进入停火模式，只有当所有选中的单位都处在停火模式时，才会让他们都退出停火模式
- `CeaseFireStance.Togglable` 的默认值为有 `Primary` 或 `OpenTopped` 且没用 `Engineer` 或 `Agent` 或 `EMPulseCannon`

```{note}
- 在已有目标的情况下，单位会自动清除目标
- 关于1个开关快捷键：
  - 开关快捷键默认名为 `Cease Fire Stance` ，可以在csf中添加名为 `TXT_CEASEFIRE_STANCE` 和 `TXT_CEASEFIRE_STANCE_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
  - 开关时显示的提示信息，可以在csf中添加名为 `MSG:CEASEFIRE_STANCE_ON` 和 `MSG:CEASEFIRE_STANCE_OFF` 的项目来分别定义开和关时显示的文本，文本中可以使用 `%i` 以显示切换状态的单位的数量
```

---

#### 平均分配模式

  > - *pr #1453 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[GlobalControls]                             ; 全局控制设置
AllowSwitchNoMoveCommand=false               ; 布尔值，是否启用切换不可移动行为的快捷键
AllowDistributionCommand=no                  ; 布尔值，是否启用目标分配功能的快捷键
AllowDistributionCommand.SpreadMode=true     ; 布尔值，是否启用目标分配功能中调整选择范围的快捷键
AllowDistributionCommand.SpreadModeScroll=true; 布尔值，是否启用目标分配功能中调整选择范围的滚轮功能
AllowDistributionCommand.FilterMode=true     ; 布尔值，是否启用目标分配功能中调整筛选范围的快捷键
AllowDistributionCommand.AffectsAllies=true  ; 布尔值，在目标为友军时，是否允许进行目标分配
AllowDistributionCommand.AffectsEnemies=true ; 布尔值，在目标为敌军时，是否允许进行目标分配

[AudioVisual]                                ; 视听通用设置
StartDistributionModeSound=                  ; sound条目，进入分配模式时的音效
EndDistributionModeSound=                    ; sound条目，退出分配模式时的音效
AddDistributionModeCommandSound=             ; sound条目，在分配模式下发出命令时的音效
```

在 `ra2md.ini` 之中：

```ini
[Phobos]                                     ; Phobos设置
DefaultApplyNoMoveCommand=yes                ; 布尔值，如果当前单位将要执行的行为与鼠标显示的行为不同，且将要执行的行为会让单位移动到目标附近时，是否将该行为更换为原地警戒
DefaultDistributionSpreadMode=2              ; 整数型，0 - r=0 , 1 - r=4 , 2 - r=8 , 3 - r=16
DefaultDistributionFilterMode=2              ; 整数型，0 - None , 1 - Auto , 2 - Type , 3 - Name
```

- 拥有快捷键功能，位于 [控制] 面板
- 如果功能未打开，则不会有快捷键

```{note}
- 是否使用切换不可移动行为的快捷键不会影响其功能的使用，即只是会在游戏内不能随时切换两种状态
- 在目标分配模式中，范围为0时为游戏原本默认行为，通过快捷键可调整范围到4格、8格或16格，这时将会给选择的单位平均分配范围内的目标，只有当前单位将要执行的行为与鼠标显示的行为相同时才会分配，否则将会回到游戏原本默认行为（不会对空中单位生效），会通过一个指示圈来提示范围。当然，你也可以在按住开关快捷键时通过鼠标滚轮来调整范围
- 开关快捷键是按住时生效
- 模式为 `None` 时为游戏原本默认行为，此时若范围不为0，则会显示一个绿圈，通过快捷键可调整模式到：
  - `Like` ——在范围分配的目标中只会选择相同护甲的目标，此时会显示一个蓝圈
  - `Type` ——在范围分配的目标中只会选择相同类型的目标（步兵、车辆、建筑），此时会显示一个黄圈
  - `Name` ——在范围分配的目标中只会选择同类目标（同名，或拥有相同的 `GroupAs` 名称），此时会显示一个红圈
- 关于3个模式快捷键：
  - 模式快捷键默认名为 `Switch no-move` 、`Distribution spread` 与 `Distribution filter`，可以在csf中添加名为 `TXT_SWITCH_NOMOVE` 、`TXT_DISTR_SPREAD` 与 `TXT_DISTR_FILTER` 和 `TXT_SWITCH_NOMOVE_DESC` 、`TXT_DISTR_SPREAD_DESC` 与 `TXT_DISTR_FILTER_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
- 关于1个开关快捷键：
  - 开关快捷键默认名为 `Distribution hold down` ，可以在csf中添加名为 `TXT_DISTR_HOLDDOWN` 和 `TXT_DISTR_HOLDDOWN_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
- 关于单局游戏内首次使用时的提示：
  - 可以在csf中添加名为 `MSG:DistributionModeOn` 和 `MSG:DistributionModeOff` 的项目来分别重写其提示信息
```

---

#### 手动回复弹药

  > - *pr #1493 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMETECHNO]                                 ; 任意步兵、车辆或建筑
CanManualReload=no                           ; 布尔值，该单位是否能够手动回复弹药
CanManualReload.WhenFull=no                  ; 布尔值，该单位是否能够在弹药满时手动回复弹药
CanManualReload.ResetROF=yes                 ; 布尔值，该单位手动回复弹药时是否重置ROF
CanManualReload.DetonateWarhead=             ; Warhead类型，该单位手动回复弹药时在自己的位置引爆的一个伤害为当前Ammo数量的弹头，不填则没有
CanManualReload.DetonateConsume=0            ; 整数型，只有当手动回复弹药前的弹药大于等于该值时才会引爆 `CanManualReload.DetonateWarhead`
```

- 拥有快捷键功能，位于 [控制] 面板
- 单位响应手动回复弹药时会将弹药清零，并进入空弹回复状态

```{note}
- 关于1个功能快捷键：
  - 功能快捷键默认名为 `Manual Reload Ammo` ，可以在csf中添加名为 `TXT_MANUAL_RELOAD` 和 `TXT_MANUAL_RELOAD_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
```

---

#### 共享盟友控制

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
AllyShareControl=no                          ; 布尔值，是否共享盟友控制
```

```{note}
- 现在你可以让非战役模式中结盟的作战方共享盟友控制
P.S.1: 非战役模式是因为战役模式中有另外的方式让玩家控制其它作战方
P.S.2: 这个功能只是个雏形。可以有更多的选项，如让玩家自主选择共享给谁之类的功能，但制作它们的前提是有很多人对它们感兴趣
```

---

#### 自动化建筑放置

  > - 暂无pr *by CrimRecya & TaranDahl*
  > - [返回目录](#额外功能目录)

在 `ra2md.ini` 之中：

```ini
[Phobos]                                     ; Phobos设置
AutomaticPlacingBuilding=yes                 ; 布尔值，是否默认启用自动放置建筑
AutomaticPlacingCombatBuilding=yes           ; 布尔值，是否默认启用自动放置防御建筑
```

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
AutoBuilding=no                              ; 布尔值，所有建筑是否不需要手动点击图标放置，而是在完成建造后自动放置

[SOMEBUILDING]                               ; 只能是建筑
AutoBuilding=                                ; 布尔值，该建筑是否不需要手动点击图标放置，而是在完成建造后自动放置，默认值为 `General` 中 `AutoBuilding` 的值
AutoBuilding.Gap=1                           ; 整数型，建筑自动放置时需要隔最近的建筑多少格距离
```

- 拥有快捷键功能，位于 [控制] 面板，可以分别控制科技界面和防御界面的建筑能否自动放置
- 只有 `AutoBuilding=yes` 的建筑会受到快捷键影响而被禁止自动放置，而 `AutoBuilding=no` 的建筑不会因此而能够自动放置。默认开启状态

```{note}
- 启用 `AutoBuilding` 会在建筑完成时增加少量性能消耗，如果找不到地方放不下去，则会每隔一段时间再次进行尝试，且不支持激光围墙、闸门和地形
- 自动放置的科技建筑会被随机放置在集结点附近5格内，防御建筑会被随机放置在第二集结点附近5格内。具体设置方法请查看 `集结点增强`。
- 关于1个开关快捷键：
  - 开关快捷键默认名为 `Toggle Auto Building` ，可以在csf中添加名为 `TXT_AUTO_BUILD` 和 `TXT_AUTO_BUILD_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
  - 开关快捷键默认名为 `Toggle Auto Building Combat` ，可以在csf中添加名为 `TXT_AUTO_BUILD_COMBAT` 和 `TXT_AUTO_BUILD_COMBAT_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
```

---

#### 鼠标忽略的物件

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
IgnoreByMouse=no                             ; 布尔值，鼠标是否忽略
IgnoreByMouse.ToSelf=                        ; 布尔值，己方鼠标是否忽略，默认值为 `IgnoreByMouse` 的值
IgnoreByMouse.ToAlly=                        ; 布尔值，友方鼠标是否忽略，默认值为 `IgnoreByMouse` 的值
IgnoreByMouse.ToEnemy=                       ; 布尔值，敌方鼠标是否忽略，默认值为 `IgnoreByMouse` 的值

[SOMETERRAIN]                                ; 任意地形物件
IgnoreByMouse=no                             ; 布尔值，鼠标是否忽略

[SOMEOVERLAY]                                ; 任意覆盖物件
IgnoreByMouse=no                             ; 布尔值，鼠标是否忽略
```

```{note}
- 现在你可以让一些物件被鼠标忽略，被忽略的物件在鼠标指针指向它时不会影响指针的行为，指针点击也无法以这些物件为目标；被忽略的物件可以被框选T选等选中；这个效果仅影响指针，单位仍然完全正常地处于地图上，索敌弹头等效果都能正常对其产生效果
```

---

#### 选中心控的单位

  > - *pr #1314 by TaranDahl*
  > - [返回目录](#额外功能目录)

- 快捷键功能，位于 [选择] 面板，无ini
- 现在可以在游戏内设置指定快捷键，通过该快捷键选中所有被临时心控的单位（即不包括Ares永控弹头和超武控制）

```{note}
- 关于1个使用快捷键：
  - 使用快捷键默认名为 `Select Captured Units` ，可以在csf中添加名为 `TXT_SELECT_CAPTURED` 和 `TXT_SELECT_CAPTURED_DESC` 的项目来分别重写其在快捷键页面中的选项名称和详细说明
  - 使用时显示的提示信息，可以在csf中添加名为 `MSG:SelectCaptured` 和 `MSG:NothingSelected` 的项目来分别定义有选中单位和未选中单位时显示的文本
```

---

#### 同帧指令上限优化

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

- 仅优化，无ini
- 同一帧只能对最多128个单位下达指令的限制现在被扩大到了无上限（当128个的缓冲区被占满的情况下，单位会记录这次指令，并在缓冲区有足够空间的情况下发送该事件），且能够在联机下生效

```{note}
- 执行列表仍有16384的上限。不过实际情况下应该不太可能触碰到这个上限
- 路径点模式下仍然被限制，这可能在将来会改动
- 鼠标点击和 `S`/`X`/`D`/`G` 等几个常用快捷键应该都可以生效。对欢呼无效，如果有没注意到的请反馈
```

---

### 杂项

---

#### 渐隐弹坑

  > - 暂无pr *by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[AudioVisual]                                ; 视听通用设置
SmudgeUpdateTime=0                           ; 整数型，大于0时启用，弹坑会在经过这么长时间后渐隐1层（额外的25透明度），经过4倍长的时间后会完全移除
```

---

#### 减少绘制帧率

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `ra2md.ini` 之中：

```ini
[Phobos]                                     ; Phobos设置
SkipFrameDelay=0                             ; 整数型，每多少帧跳过一帧，填大于等于2的整数（其它值表示不启用逻辑）
```

```{note}
- 会在游戏帧率低于 `DetailMinFrameRateNormal` 时真正启用，将部分CPU资源留给逻辑运算
P.S.1: 会降低游戏观感，除非别的方面实在优化不动了，否则不建议启用
P.S.2: 如果设置 `SkipFrameDelay=3` ，那就是每 3 帧少绘制 1 帧，即 “绘制-绘制-不绘制” 循环
```

---

#### 额外建筑进度条

  > - *pr #1309 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[AudioVisual]                                ; 视听通用设置
FactoryProgressDisplay=no                    ; 布尔值，开启后会在生产单位的主要工厂的血条下方显示当前产品的生产进度，注意当所属方为玩家时只有设定为主要的工厂才会显示，建筑中心的格子没有迷雾或被鼠标选中才会显示
FactoryProgressDisplay.Offset=0,0            ; 二维整数型，工厂生产进度显示的偏移坐标，分别为向右、向下
FactoryProgressDisplay.Pips=3                ; 整数型，工厂生产进度显示绘制的格子是使用SHP文件中的第几帧，注意和shpb不同，这里从第0帧开始
MainSWProgressDisplay=no                     ; 布尔值，开启后会在挂载超武的支援建筑的血条下方显示主要超武的冷却进度，只能是 `SuperWeapon` 中定义的超武且条件满足才会显示，建筑中心的格子没有迷雾或被鼠标选中才会显示
MainSWProgressDisplay.Offset=0,0             ; 二维整数型，建筑超武冷却显示的偏移坐标，分别为向右、向下
MainSWProgressDisplay.Pips=5                 ; 整数型，建筑超武冷却显示绘制的格子是使用SHP文件中的第几帧，注意和shpb不同，这里从第0帧开始
InvulnerableDisplay=no                       ; 布尔值，开启后会在无敌状态的任意单位的血条位置覆盖一层无敌的剩余时间，单位所处或建筑中心的格子没有迷雾或被鼠标选中才会显示
InvulnerableDisplay.Others.Offset=0,0        ; 二维整数型，单位无敌状态显示的偏移坐标，分别为向右、向下
InvulnerableDisplay.Buildings.Offset=0,0     ; 二维整数型，建筑无敌状态显示的偏移坐标，分别为向右、向下
InvulnerableDisplay.Others.Pips=17,18        ; 二维整数型，单位无敌状态显示绘制的格子是使用SHP文件中的第几帧，注意和shpb不同，这里从第0帧开始，分别是力场护盾、铁幕
InvulnerableDisplay.Buildings.Pips=5,4       ; 二维整数型，建筑无敌状态显示绘制的格子是使用SHP文件中的第几帧，注意和shpb不同，这里从第0帧开始，分别是力场护盾、铁幕
TemporalLifeDisplay=no                       ; 布尔值，开启后会在受到超时空武器攻击的任意单位的血条位置覆盖一层状态的剩余时间，单位所处或建筑中心的格子没有迷雾或被鼠标选中才会显示
TemporalLifeDisplay.Others.Offset=0,0        ; 二维整数型，单位超时空状态显示的偏移坐标，分别为向右、向下
TemporalLifeDisplay.Buildings.Offset=0,0     ; 二维整数型，建筑超时空状态显示的偏移坐标，分别为向右、向下
TemporalLifeDisplay.Others.Pips=17           ; 整数型，单位超时空状态显示绘制的格子是使用SHP文件中的第几帧，注意和shpb不同，这里从第0帧开始
TemporalLifeDisplay.Buildings.Pips=5         ; 整数型，建筑超时空状态显示绘制的格子是使用SHP文件中的第几帧，注意和shpb不同，这里从第0帧开始
ProgressDisplay.Others.PipsShape=PIPS.shp    ; SHP文件名，需要后缀，额外建筑进度条中所有单位类绘制格子时使用的文件，注意色盘和背景各与原版相同
ProgressDisplay.Buildings.PipsShape=PIPS.shp ; SHP文件名，需要后缀，额外建筑进度条中所有建筑类绘制格子时使用的文件，注意色盘和空格各与原版相同
```

---

#### 单位默认透明度

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `artmd.ini` 之中：

```ini
[SOMETECHNO]                                 ; 任意步兵、车辆、飞机或建筑
DefaultVisualCharacter=0                     ; 整数型，0-5，单位默认透明度
DefaultVisualCharacterToSelf=                ; 整数型，0-5，对自己的单位默认透明度，默认值为 `DefaultVisualCharacter` 的值
DefaultVisualCharacterToAlly=                ; 整数型，0-5，对友方的单位默认透明度，默认值为 `DefaultVisualCharacter` 的值
DefaultVisualCharacterToEnemy=               ; 整数型，0-5，对敌人的单位默认透明度，默认值为 `DefaultVisualCharacter` 的值
```

```{note}
- 在原本游戏中，单位会在隐形时产生透明度变化，现在你可以让单位在默认情况下也具有透明度
P.S.1: 这个并非单纯的视觉效果，例如当透明度处于隐藏状态时单位就无法被选中，遇到任何意料之外的情况请反馈
P.S.2: 本效果只影响默认情况下的透明度，当单位进入隐形等状态时就会使用本来应当处于的透明度
P.S.3: 透明度是0到5之间的整数，并非完全线性变化，具体效果请自行尝试
```

---

#### 修改侧栏滚动方式

  > - *pr #1522 by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `ra2md.ini` 之中：

```ini
[Phobos]                                     ; Phobos设置
ScrollSidebarStripInTactical=yes             ; 布尔值，是否能够在鼠标在战术地图中时使用滚轮滚动侧边栏
ScrollSidebarStripWhenHoldAlt=yes            ; 布尔值，是否能够在按下 `Alt` 时使用滚轮滚动侧边栏
ScrollSidebarStripWhenHoldCtrl=yes           ; 布尔值，是否能够在按下 `Ctrl` 时使用滚轮滚动侧边栏
ScrollSidebarStripWhenHoldShift=yes          ; 布尔值，是否能够在按下 `Shift` 时使用滚轮滚动侧边栏
```

---

#### 玩家建筑自动维修

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
PlayerAutoRepair=no                          ; 布尔值，是否无视 `IQ` 使玩家能够自动维修
```

---

#### 占据位空气墙修复

  > - 暂无pr *by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[General]                                    ; 全局通用设置
CleanUpAirBarrier=0                          ; 整数型，大于0时启用，每隔多久刷新全图格子的占据位状态，如果当前格子占据位不为空但链表上没有单位，且周围不存在任何移动中的单位，则会刷新占据位状态
```

```{warning}
- 实验性功能，使用暴力解法，频率过高会影响性能，并可能导致车辆在移动时重叠
```

---

#### 占领建筑副官语音

  > - 暂无pr *by TaranDahl*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[AudioVisual]                                ; 视听通用设置
EVA.WeCaptureABuilding=                      ; EVA条目，占领敌人建筑时的EVA
EVA.OurBuildingIsCaptured=                   ; EVA条目，建筑被敌人占领时的EVA
```

---

### 开发

---

#### 调试模式

  > - 无pr *by CrimRecya*
  > - [返回目录](#额外功能目录)

在 `rulesmd.ini` 之中：

```ini
[GlobalControls]                             ; 全局控制设置
DebugToolEnabled=                            ; 布尔值，是否启用调试模式，会启用一个快捷键 `CrimViewerEx` ，用于输出各类信息（部分显示效果可通过按住ctrl、alt或shift打开）
```

```{note}
- 推荐搭配 `DebugKeysEnabled` 使用，在出现部分已知问题时能够聚焦问题发生位置并进入逐帧步进模式（FrameByFrame）
```

```{hint}
- 有不适配 Phobos 拓展信息的独立 dll ，如确实有需要，请联系作者
```

---
